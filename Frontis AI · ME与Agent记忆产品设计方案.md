# Frontis AI · ME 与 Agent 记忆产品设计方案

## 1. 背景与目标

Frontis AI 的核心体验是用户通过 ME 发起任务，由 ME 理解目标并调度 AI 专家协作。当前缺少稳定的记忆机制，导致用户在不同会话、不同任务中反复说明自己的偏好、企业规则、业务背景和已采纳结论。

记忆能力要解决的不是“把历史聊天都塞给模型”，而是让 ME 和 AI 专家在正确权限下持续使用有价值的信息，同时保证不同会话之间的上下文隔离。

目标效果：

- 用户不需要反复说明稳定偏好、职责背景和工作习惯。
- ME 能在新会话中自动使用已生效的个人记忆、企业记忆、专家记忆和业务对象记忆。
- AI 专家能继承必要上下文，并避免重复犯用户已经纠正过的错误。
- 企业管理员可以管理企业共享规则、术语、流程和风险策略。
- 长对话、长文档和大工具结果不会因为上下文窗口限制而丢失关键内容。

## 2. 设计原则

### 2.1 默认自动沉淀，不打断用户

记忆应在交互过程中自动识别、提炼和沉淀。用户明确说“记住”时，系统直接保存并给出轻反馈；低风险个人偏好、稳定职责背景、明确纠错、已采纳任务结论默认自动生效。

不应把“确认保存”作为默认路径。只有以下情况才进入治理或低频确认：

- 涉及隐私、合同金额、薪酬、客户敏感信息。
- 内容和已有记忆冲突。
- 系统无法判断作用范围。
- 个人内容可能升级为企业共享内容。
- 普通成员表达了可能影响多人决策的企业级规则。

### 2.2 自动沉淀不等于自动全员生效

个人记忆低风险自动生效，用户可以在“我的记忆”中编辑、停用、删除。

企业记忆可以自动沉淀，但是否全员生效要看来源和风险：

- 管理员维护或管理员对话中产生的企业规则，可以按策略直接生效。
- 普通成员说出的企业级规则，进入企业记忆治理，由管理员处理后再共享。
- 敏感或冲突内容不自动全员生效。

### 2.3 会话隔离，记忆共享

用户点击“新任务”并在首页发送第一句话后，创建一个独立会话。左侧会话列表中的每一项都是用户主动创建的独立会话，不是 ME 自动拆出来的 Topic。

不同会话之间隔离：

- 不共享会话原文。
- 不共享临时文件。
- 不共享工具调用明细。
- 不共享未采纳方案和中间推理过程。

跨会话只共享已生效的长期记忆、任务结论和业务对象记忆。新会话不能直接读取旧会话全文。

### 2.4 上下文压缩是运行时机制

当前任务摘要是系统为了管理模型上下文窗口而自动维护的运行时上下文，不作为用户侧内容资产管理。

压缩不是删除。原始对话记录一直保留在当前会话历史数据中，只是不常驻当前模型上下文窗口。用户追问早期内容时，ME 先查当前任务摘要，再从当前会话原始历史记录、工作记录或任务记录中按需召回。

## 3. 用户场景

### 3.1 普通用户：ME 记住个人偏好

用户多次要求“方案先给结论，再给理由”“周报用表格”“不要写太长”。系统应自动沉淀为个人记忆，后续相关任务直接生效。

用户可见效果：

- ME 给出轻提示：“已记住：方案输出时先给结论，再写理由。”
- 后续写方案时自动应用。
- 用户可在“我的记忆”中查看、编辑、停用或删除。

### 3.2 普通用户：继续已讨论过的任务

用户上周和 ME 讨论过 A 客户续约策略，本周新开会话继续推进。ME 不能读取旧会话全文，但可以召回已生效的任务结论、客户对象记忆和用户偏好。

用户可见效果：

- ME 能延续已采纳策略，而不是从零开始。
- 回复下方可展示“使用了 3 条记忆”。
- 点击后可查看使用了哪些记忆来源。

### 3.3 普通用户：纠正 ME 或专家

用户指出“这个客户不是低风险客户，以后不要按普通续约流程处理”。系统应自动形成纠错记忆。

用户可见效果：

- 当前用户后续处理同一客户或类似场景时，优先使用纠错内容。
- 如果纠错涉及专家公共方法，只对当前用户先自动生效，专家公共记忆进入治理。

### 3.4 企业管理员：维护企业统一口径

企业有统一统计口径，例如“有效客户按近 90 天有成交或活跃记录统计”。管理员希望 ME 和专家在客户分析、报表解读、经营分析中统一使用。

用户可见效果：

- 管理员在企业管理后台维护企业记忆。
- 设置适用范围：全公司、部门、角色或专家。
- 企业用户发起相关任务时，ME 和专家按权限使用。

### 3.5 长对话用户：上下文接近上限

用户与 ME 连续推进复杂项目，包含多轮澄清、文件分析、专家协作和方案修改。系统应在上下文接近上限时自动整理当前任务摘要，保证继续对话时不丢失主线。

用户可见效果：

- 系统弱提示“内容较多，已整理当前任务摘要后继续”。
- ME 继续保留任务目标、关键约束、已采纳结论和未完成事项。
- 用户追问早期细节时，ME 从当前会话历史记录中按需找回。

## 4. 记忆体系

| 记忆类型 | 服务对象 | 主要内容 | 生效方式 | 管理者 |
| -------- | -------- | -------- | -------- | ------ |
| 个人记忆 | 单个用户 | 输出偏好、沟通习惯、职责背景、个人纠错 | 低风险自动生效 | 用户本人 |
| 企业记忆 | 企业租户 | 术语、流程、统计口径、企业规则、禁用事项 | 按来源和风险生效或治理 | 企业管理员 |
| 专家记忆 | AI 专家 | 专业方法、工具经验、禁用做法 | 创建者维护，纠错进入治理 | 专家创建者 / 管理员 |
| 任务记忆 | 当前任务 | 目标、约束、已采纳结论、未完成待办 | 任务内自动维护，长期价值自动沉淀 | 任务有权限用户 |
| 业务对象记忆 | 客户、项目、合同、文档等 | 对象事实、历史结论、关键偏好 | 继承对象权限 | 对象有权限用户 |
| 纠错记忆 | 用户 / 专家 / 对象 | 用户明确指出的错误和修正方式 | 个人低风险自动生效，公共范围进入治理 | 发起人 / 管理员 |

### 4.1 个人长期记忆

进入个人长期记忆的内容：

- 用户明确说“以后都……”“记住我……”。
- 用户多次表达的输出格式、语气、结构偏好。
- 用户职责、关注范围、长期工作目标。
- 用户对 ME 或专家的个人纠错。

不进入个人长期记忆的内容：

- 临时表达、玩笑、一次性要求。
- 企业统一规则。
- 影响多人协作的业务口径。
- 不应被长期保存的敏感信息。

### 4.2 企业长期记忆

进入企业长期记忆的内容：

- 管理员维护的企业规则。
- 企业知识库、制度、流程中明确的组织级口径。
- 企业级术语、数据口径、风险控制要求。
- 需要多个成员、多个专家共同遵循的方法。

普通成员表达企业级规则时，不在对话中打断用户，但不会直接全员生效。系统自动进入企业记忆治理，管理员处理后再共享。

### 4.3 个人记忆与企业记忆的关系

| 场景 | 处理规则 |
| ---- | -------- |
| 企业规则与个人表达偏好冲突 | 企业业务规则优先，个人表达风格保留 |
| 企业统计口径与用户临时口径冲突 | 默认采用企业口径，用户可在当前任务临时指定 |
| 个人纠错与企业规则冲突 | 个人纠错只影响个人使用，不覆盖企业强规则 |
| 普通成员提出企业级规则 | 进入企业记忆治理，不在对话中频繁确认 |

## 5. 核心流程

### 5.1 用户新建会话

1. 用户点击“新任务”回到首页。
2. 用户发送第一句话。
3. 系统创建独立会话。
4. 会话标题默认取第一句话。
5. 新会话写入左侧会话列表。
6. 系统初始化当前会话上下文。
7. 系统召回已生效且有权限的长期记忆。
8. ME 基于当前输入、当前会话上下文和共享记忆处理请求。

### 5.2 记忆自动沉淀

1. 当前会话内产生用户偏好、纠错、已采纳结论、企业规则线索或业务对象结论。
2. 系统在交互后自动提炼结构化记忆。
3. 系统判断是否具备长期价值。
4. 系统判断归属：个人、企业、专家、任务、业务对象。
5. 系统判断风险和权限。
6. 低风险个人记忆自动生效。
7. 企业级、专家级、对象级或高风险内容按规则生效或进入治理。
8. 新会话只召回已生效且有权限的记忆。

### 5.3 用户管理个人记忆

用户可以通过 ME 或“我的记忆”管理个人记忆。

支持动作：

- 查看：看 ME 记住了什么、来源是什么、最近是否使用。
- 编辑：修正不准确的个人记忆。
- 停用：暂时不使用，但保留记录。
- 删除：后续不再召回。
- 标记不准确：降低同类记忆自动生效优先级。

### 5.4 企业管理员治理企业记忆

企业管理员在企业管理后台管理企业记忆。

支持动作：

- 新建企业记忆。
- 设置适用范围。
- 启用、停用、删除。
- 处理系统自动沉淀出的企业记忆候选。
- 查看记忆来源和操作记录。

### 5.5 ME 调用 AI 专家

ME 调用专家属于当前会话内协作，不属于跨会话。

ME 给专家的上下文包只包含：

- 当前任务目标。
- 本轮用户问题。
- 当前会话内已采纳结论。
- 与专家任务相关的文件、业务对象和工具结果摘要。
- 必要的用户偏好。
- 专家有权限使用的企业记忆。
- 专家自身专业记忆。

专家不能直接获得：

- 用户全部个人记忆。
- 当前会话完整历史。
- 其他会话原文。
- 与专家任务无关的客户、合同、项目细节。
- 无权限业务对象记忆。

专家返回结果后，ME 负责汇总到当前会话摘要。具备长期价值的内容按归属自动沉淀或进入治理。

## 6. 上下文窗口管理

### 6.1 当前任务摘要

当前任务摘要是系统自动维护的运行时上下文，用于保证长对话持续推进，不是用户手动编辑的页面内容。

摘要保留：

- 当前任务目标。
- 关键约束和禁止事项。
- 用户已采纳结论。
- 当前文件、客户、项目等业务对象。
- 未完成待办。
- 参与专家和专家结论。

用户如果发现摘要偏差，通过对话纠正即可，系统自动更新摘要。

### 6.2 触发压缩的场景

| 场景 | 系统动作 | 用户感知 |
| ---- | -------- | -------- |
| 当前会话接近模型上下文上限 | 自动生成或更新当前任务摘要 | 弱提示“已整理当前任务摘要” |
| 同一任务经过多轮对话、多个专家或多个文件处理 | 汇总阶段结论和未完成待办 | ME 继续按主线推进 |
| 上传或解析内容过长 | 生成文件结构摘要和分段摘要 | 提示选择全文、章节或问题范围 |
| 工具结果过大 | 生成工具结果摘要和关键命中项 | 展示摘要和完整结果入口 |
| ME 准备调用专家 | 生成专家上下文包 | 专家只拿到必要上下文 |

### 6.3 压缩后找回早期内容

压缩不是删除。原始对话记录一直保留在当前会话历史数据中，只是不常驻当前模型上下文窗口。

找回规则：

1. ME 先查当前任务摘要。
2. 摘要不足时，从当前会话原始历史记录、工作记录或任务记录中检索。
3. 如果早期内容已经沉淀为长期记忆，优先使用长期记忆并展示来源。
4. 找不到时，ME 明确说明无法确认早前原文，不根据摘要猜测。

跨会话不做原文引用。新会话只能召回已生效长期记忆、任务结论和业务对象记忆。

### 6.4 长输入与大工具结果

长输入、长文档和大工具结果不能静默截断。

处理规则：

- 长文本：提示用户选择全文处理、按章节处理或围绕问题处理。
- 长文档：先识别结构，再分段分析，最后汇总。
- 工具大结果：先生成摘要和关键命中项，完整结果保留为可展开明细。
- 用户要求基于完整内容判断时，系统以分批任务处理，并展示处理进度。

## 7. 页面与交互

### 7.1 我的记忆

入口：

- ME 对话页右上角“记忆”。
- 账户菜单“我的记忆”。
- ME 回复下方“使用了 X 条记忆”。

页面内容：

| 内容 | 说明 |
| ---- | ---- |
| 记忆列表 | 展示个人记忆、任务记忆、纠错记忆 |
| 搜索筛选 | 按类型、来源、更新时间筛选 |
| 记忆详情 | 展示内容、来源、作用范围、最近使用 |
| 操作 | 编辑、停用、删除、标记不准确 |
| 治理区 | 展示冲突、敏感或作用范围不清的记忆 |

### 7.2 本次使用的记忆

当 ME 或专家使用了记忆，回复下方展示轻量入口：“使用了 X 条记忆”。

点击后展示：

- 记忆摘要。
- 来源。
- 作用范围。
- 本次为什么使用。
- 有用 / 不相关 / 不准确 / 删除。

### 7.3 企业记忆

企业管理后台提供企业记忆管理。

页面内容：

- 企业记忆开关。
- 企业记忆列表。
- 新建企业记忆。
- 适用范围配置。
- 企业记忆治理。
- 操作记录。

### 7.4 专家记忆

AI 专家详情页提供“记忆”Tab。

页面内容：

- 专业方法。
- 工具经验。
- 禁用做法。
- 用户纠错候选。
- 专家记忆治理。
- 编辑和启停操作。

### 7.5 会话列表

左侧会话列表展示用户主动创建的独立会话。

规则：

- 用户点击“新任务”并发送第一句话后创建新会话。
- 会话标题默认取第一句话。
- 用户点击会话列表项时，只恢复该会话自己的上下文。
- 不同会话之间不直接共享聊天原文、临时文件和工具明细。

## 8. 规则与验收标准

### 8.1 关键规则

| 规则 | 说明 |
| ---- | ---- |
| 记忆默认自动沉淀 | 低风险个人偏好、职责背景、纠错和已采纳结论自动生效 |
| 企业记忆需要治理 | 普通成员表达的企业级规则不能直接全员生效 |
| 会话原文不跨会话 | 新会话不能读取旧会话完整原文 |
| 当前任务摘要不手动编辑 | 摘要是运行时上下文，由系统自动维护 |
| 压缩不删除原记录 | 原始对话记录仍在当前会话历史数据中 |
| 专家只拿必要上下文 | ME 按任务裁剪专家上下文包 |
| 敏感内容不自动共享 | 隐私、合同金额、薪酬等按企业策略治理 |

### 8.2 验收标准

| 场景 | 操作 | 预期结果 |
| ---- | ---- | -------- |
| 用户显式记住偏好 | 用户说“记住我以后方案先给结论” | 系统自动保存为个人记忆并轻提示，后续自动使用 |
| 自动沉淀偏好 | 用户多次表达同一输出偏好 | 系统自动沉淀为个人记忆，用户可在“我的记忆”管理 |
| 用户删除记忆 | 用户在“我的记忆”删除某条个人记忆 | 后续 ME 不再使用该记忆 |
| 本次使用记忆 | ME 回复使用了记忆 | 回复下方展示“使用了 X 条记忆”，可查看来源 |
| 新建会话 | 用户点击新任务并发送第一句话 | 系统创建独立会话，标题取第一句话，进入会话列表 |
| 会话隔离 | 用户进入另一个会话 | 系统只恢复该会话上下文，不读取其他会话原文 |
| 跨会话召回 | 用户在新会话提到历史任务 | 系统只召回已生效记忆、任务结论或业务对象记忆 |
| 长对话压缩 | 当前会话接近上下文上限 | 系统自动更新当前任务摘要并继续任务 |
| 追问早期内容 | 用户问很早之前说过的限制 | ME 从当前会话摘要、原始历史记录或任务记录找回 |
| 工具结果过大 | 工具返回大量明细 | 系统展示结果摘要和完整结果入口 |
| 专家协作 | ME 调用专家 | 专家只收到当前任务相关上下文和授权记忆 |
| 企业规则治理 | 普通成员表达企业级规则 | 不打断对话，进入企业记忆治理，不直接全员生效 |

### 8.3 待定事项

- 企业记忆是否允许普通成员查看完整内容，还是只在使用时展示摘要。
- 敏感内容策略是只做默认拦截，还是提供企业后台配置。
- 用户个人记忆是否跨租户共享，建议不跨租户。
- 当前会话原始历史记录召回的检索范围和排序策略。
- 专家上下文包是否向用户完整展示，建议只展示摘要。
- 会话标题是否允许用户手动修改。
