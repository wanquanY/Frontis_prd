# Frontis AI · V0.8 PRD

## 1. 版本范围

### 1.1 本期需求

- 企业专区「我发布的」AI 专家删除。
- ME 记忆系统。
- 工作台多 session 与任务记录。
- 工作台成果与对话分享。
- 运营后台内部积分包与席位包发放撤销。
- 登录页用户协议与隐私协议确认。
- 团队席位支付规则展示优化。
- 企业管理后台组织成员与席位额度规则优化。
- 模型缓存计量计费规则优化。
- 企业驾驶舱图表时间规则优化。

### 1.2 不做范围

- 不做渠道码、大观销售、企业销售管理、运营渠道码管理。
- 不做购买席位填写渠道码、渠道码优惠、渠道码消耗追踪。
- 分享功能本期只生成链接，不做权限配置、访问统计、分享管理。
- 记忆系统本期不做记忆看板、团队共享记忆、记忆审计日志、外部记忆引擎集成。
- 用户协议和隐私协议正文由独立协议文档提供，本 PRD 不定义协议正文内容。

### 1.3 原型截图标记说明

- PRD 中使用「图 X-XX / 标记 A」标识原型截图位置和截图内需要标注的节点。
- 截图来源以现有 V0.8 原型为准；研发评审时按这些图号补充截图或在原型截图上加标记。
- 每个流程节点都需要能对应到一个页面、弹窗、列表状态或操作反馈。

---

## 2. 新增需求

### 2.1 ME 记忆系统

#### 2.1.1 目标

- 让 ME 在跨会话场景下记住用户偏好、关键事实和工作成果。
- 用户不需要打开设置页，主要通过自然对话触发记忆写入、更新和删除。
- 记忆完全个人化，团队成员和管理员不可见。

#### 2.1.2 任务拆解

##### 任务一：偏好记忆

- 每个用户拥有独立偏好文件。
- 新 ME 会话启动时自动加载偏好文件。
- 用户纠正 ME 回复方式时，ME 判断是否写入偏好。
- 同类偏好更新时替换旧记录，不重复新增。
- 用户说「不用再这样了」时，ME 删除对应偏好。

##### 任务二：事实记忆

- ME 自动识别有复用价值的事实。
- 可写入内容包括用户角色、关注指标、固定工作习惯、长期偏好。
- 临时任务、闲聊、一次性问题不写入。
- 事实过期或冲突时，ME 需在对话中确认后更新。
- 用户说「忘了这件事」时，ME 删除对应事实。

##### 任务三：工作成果记忆

- ME 生成报告、图表、方案等文件后自动存入用户沙盒。
- 用户可在 ME 侧边栏「我的文件」查看、打开、下载、删除产物。
- 用户要求更新历史产物时，ME 找到原文件并生成新版本。

#### 2.1.3 业务示例

##### 示例一：偏好记忆

- 用户在会话 A 中说：「以后回复简短一点。」
- ME 写入偏好：回复风格偏简洁。
- 用户在会话 B 中发起新任务时，ME 自动使用简洁风格。

##### 示例二：事实记忆

- 用户说：「我是电商运营，主要看 GMV 和转化率。」
- ME 写入事实：用户角色为电商运营，关注 GMV、转化率。
- 下次用户说「帮我看看最近数据」，ME 直接围绕 GMV 和转化率分析。

##### 示例三：工作成果记忆

- ME 生成「Q4 销售分析报告」。
- 用户新会话说：「更新上次 Q4 报告。」
- ME 从沙盒找到原文件，基于原结构生成新版本。

#### 2.1.4 规则清单

- 偏好和事实按账号、租户隔离。
- 记忆写入需判断是否具备跨会话复用价值。
- 用户可通过自然语言主动写入、删除、清空记忆。
- 清空全部记忆需二次确认。
- 管理员不可查看成员个人偏好和事实记忆。

#### 2.1.5 流程与原型截图标记

- 图 ME-01 / 标记 A：ME 对话页，用户输入「以后回复简短一点」。
- 图 ME-01 / 标记 B：ME 回复确认偏好已记住。
- 图 ME-02 / 标记 A：新建 ME 会话，系统自动加载偏好和事实。
- 图 ME-02 / 标记 B：ME 按已记忆偏好输出结果。
- 图 ME-03 / 标记 A：「我的文件」侧栏展示历史产物。
- 图 ME-03 / 标记 B：用户打开历史产物并要求更新。
- 图 ME-04 / 标记 A：用户输入「清除我的所有记忆」。
- 图 ME-04 / 标记 B：二次确认弹窗。

#### 2.1.6 验收标准

- 用户表达回复偏好后，新会话中 ME 按该偏好回复。
- 用户表达长期事实后，新会话相关任务能调用该事实。
- 用户要求删除某条记忆后，新会话不再使用该记忆。
- 用户要求清空全部记忆并确认后，偏好和事实均不再加载。
- ME 生成文件后，文件进入「我的文件」列表。
- 管理员查看成员工作空间时，不可见成员个人记忆。

---

### 2.2 企业专区「我发布的」AI 专家删除

#### 2.2.1 目标

- 用户可删除自己开发、发布或可管理的 AI 专家。
- 删除前必须校验工作台使用关系和专家广场商品关系，避免影响正在使用或售卖的专家。

#### 2.2.2 任务拆解

##### 任务一：展示删除入口

- 删除入口只出现在「我发布的」列表。
- 团队共享专家、平台商品专家不使用该删除入口。

##### 任务二：删除前校验

- 校验当前账号是否已添加该专家到工作台。
- 校验团队成员是否已添加该专家到工作台。
- 校验专家是否存在未下架的专家广场商品。

##### 任务三：删除执行

- 无依赖时展示二次确认。
- 用户确认后，从「我发布的」列表移除该专家。
- 删除不影响历史会话、成果记录和已沉淀数据。

##### 任务四：不可删除反馈

- 命中任一依赖时阻断删除。
- 多个原因同时存在时，只提示一个主要原因。

#### 2.2.3 业务示例

##### 示例一：允许删除

- 用户发布的「销售话术专家」未被任何人添加到工作台，也未上架商品。
- 用户点击删除并确认。
- 专家从「我发布的」列表移除，历史对话仍可在原记录中查看。

##### 示例二：不可删除

- 「合同摘要专家」已被团队成员添加到工作台。
- 发布者点击删除。
- 系统阻断删除，并提示仍有团队成员正在使用。

#### 2.2.4 流程与原型截图标记

- 图 EXP-01 / 标记 A：「我发布的」列表中的删除按钮。
- 图 EXP-02 / 标记 A：点击删除后触发依赖校验。
- 图 EXP-03 / 标记 A：无依赖时展示删除二次确认弹窗。
- 图 EXP-04 / 标记 A：有依赖时展示不可删除提示。
- 图 EXP-05 / 标记 A：删除成功后列表移除对应专家。

#### 2.2.5 验收标准

- 无工作台、团队使用和上架商品依赖时，用户确认后可删除专家。
- 当前账号已添加该专家到工作台时，不允许删除。
- 团队成员已添加该专家到工作台时，不允许删除。
- 专家存在未下架专家广场商品时，不允许删除。
- 用户取消二次确认时，不执行删除。

---

### 2.3 工作台多 session 与任务记录

#### 2.3.1 目标

- ME 和单个 AI 专家均支持多个独立 session。
- 用户切换 session 或页面时，运行中任务不中断。
- 任务记录明确展示运行、排队、完成、终止、未读、已读状态。

#### 2.3.2 任务拆解

##### 任务一：session 创建

- 用户点击「新任务」并发送第一句话后创建 session。
- session 标题默认取第一句话。
- session 写入左侧任务记录。

##### 任务二：session 隔离

- 每个 session 拥有独立聊天原文、临时文件和工具明细。
- 不同 session 不直接共享上下文。
- 跨 session 只能召回已生效且有权限的长期记忆、任务结论或业务对象记忆。

##### 任务三：任务并发与排队

- 当前最多 3 个会话任务同时运行。
- 第 4 个及之后的任务进入排队。
- 有运行任务完成或终止后，排队任务按进入顺序开始。

##### 任务四：任务切换与恢复

- 用户切走运行中 session 后，任务继续执行。
- 用户切回时，展示当前进度和已产生内容。
- 运行中 session 支持终止操作。

##### 任务五：任务记录管理

- 任务记录支持按 ME 或 AI 专家分组。
- 任务记录支持重命名和删除。
- 删除需二次确认，删除后不可恢复。

#### 2.3.3 业务示例

##### 示例一：多任务并行

- 用户同时提交 3 个任务：写报告、查竞品、整理会议纪要。
- 3 个 session 均进入运行中。
- 用户提交第 4 个任务时，该 session 进入排队中。

##### 示例二：切换不中断

- 用户在「写报告」session 运行中切到「查竞品」session。
- 写报告任务继续执行。
- 用户切回后，看到已生成的大纲和当前进度。

#### 2.3.4 流程与原型截图标记

- 图 SES-01 / 标记 A：左侧任务记录入口。
- 图 SES-01 / 标记 B：「新任务」按钮。
- 图 SES-02 / 标记 A：新 session 生成默认标题。
- 图 SES-03 / 标记 A：运行中、排队中、已完成、已终止状态标签。
- 图 SES-04 / 标记 A：运行中 session 的终止按钮。
- 图 SES-05 / 标记 A：任务记录操作菜单中的重命名和删除。
- 图 SES-06 / 标记 A：未读状态提示。

#### 2.3.5 验收标准

- 用户发送新任务第一句话后，系统创建独立 session 并写入任务记录。
- 用户切换 session 后，只恢复当前 session 的上下文。
- 已有 3 个任务运行时，第 4 个任务进入排队状态。
- 运行中任务在用户切走页面后继续执行。
- 用户终止任务后，session 状态变为已终止，并保留已产生内容。
- session 产生新内容且用户未查看时展示未读状态。
- 用户可重命名和删除任务记录，删除前需二次确认。

---

### 2.4 工作台成果与对话分享

#### 2.4.1 目标

- 用户可将成果文件生成分享链接。
- 用户可选择部分对话消息生成分享链接。
- 收件人通过链接在线预览分享内容，并能看到 Frontis AI 产品入口。

#### 2.4.2 任务拆解

##### 任务一：成果文件分享

- 分享入口只出现在成果文件预览面板。
- 已生成、未删除、可预览的成果文件可生成分享链接。
- 分享内容保存为生成链接时的文件快照。

##### 任务二：对话消息分享

- 对话页右上角提供分享按钮。
- 点击后进入消息选择模式。
- 用户可选择用户消息、AI 回复、结果卡片和成果文件卡片。
- 未选择消息、工具明细、隐藏上下文、其他 session 内容不得进入分享页。

##### 任务三：分享链接生成

- 文件或对话内容校验通过后生成不可枚举 token 链接。
- 生成后展示复制链接入口。
- 本期不做复制文本、生成图片、渠道发送、权限配置、访问统计。

##### 任务四：分享页预览

- 文件分享页展示文件名称、文件类型、分享来源和在线预览内容。
- 对话分享页按原对话顺序展示用户选中的内容。
- 分享页不展示原工作台侧边栏、未选择消息、内部工具日志、文件真实路径。
- 分享页展示 Frontis AI 产品入口。

#### 2.4.3 业务示例

##### 示例一：文件分享

- 用户在成果文件预览面板打开「AI 发展趋势报告.docx」。
- 点击分享并复制链接。
- 收件人打开链接后，只能在线预览该文件快照。

##### 示例二：对话分享

- 用户点击对话页分享按钮。
- 勾选 3 条关键消息和 1 个成果文件卡片。
- 生成链接后，收件人只看到被选中的内容。

#### 2.4.4 流程与原型截图标记

- 图 SHR-01 / 标记 A：成果文件预览面板分享按钮。
- 图 SHR-02 / 标记 A：文件分享链接生成结果。
- 图 SHR-03 / 标记 A：对话页右上角分享按钮。
- 图 SHR-04 / 标记 A：消息选择模式。
- 图 SHR-04 / 标记 B：底部「生成分享链接」和「取消」。
- 图 SHR-05 / 标记 A：对话分享链接生成结果。
- 图 SHR-06 / 标记 A：文件分享预览页。
- 图 SHR-07 / 标记 A：对话分享预览页和产品入口。

#### 2.4.5 验收标准

- 可预览成果文件可生成分享链接。
- 已删除或不可预览文件不可生成分享链接。
- 对话分享进入选择模式后，用户可选择要分享的消息。
- 未选择消息时，不允许生成对话分享链接。
- 分享页只展示被分享内容，不展示未选择上下文。
- 原会话或源文件后续变化不影响已生成分享页快照。
- 分享链接无效时，页面提示链接不存在或已失效，并保留产品入口。

---

### 2.5 运营后台内部积分包与席位包发放撤销

#### 2.5.1 目标

- 运营给租户发放内部积分包或席位包后，系统沉淀发放记录。
- 发放错误、合同调整或业务取消时，运营可在规则允许时撤销发放。
- 撤销为反向冲正，不删除原记录。

#### 2.5.2 任务拆解

##### 任务一：发放记录

- 积分包和席位包发放成功后，均生成权益发放记录。
- 记录字段包括类型、发放内容、关联订单号、状态、发放人、发放时间、撤销人、撤销时间、撤销原因。
- 状态包含已生效、已撤销。

##### 任务二：积分包撤销

- 仅已生效记录可撤销。
- 当前积分余额需大于等于本次发放积分。
- 撤销成功后扣减积分余额，生成支出流水，关闭关联积分订单。
- 当前积分余额不足时，不允许撤销。

##### 任务三：席位包撤销

- 仅已生效记录可撤销。
- 撤销后租户总席位需大于等于已用席位。
- 如席位包包含赠送积分，当前积分余额需大于等于赠送积分。
- 撤销成功后扣减席位额度，扣回赠送积分，关闭关联订阅订单。

##### 任务四：撤销记录

- 撤销原因必填。
- 已撤销记录不可再次撤销。
- 发放和撤销均保留操作记录，不覆盖原订单和原流水。

#### 2.5.3 业务示例

##### 示例一：积分包可撤销

- 运营发放 10,000 积分，租户当前余额为 12,000。
- 运营填写撤销原因并确认。
- 系统扣回 10,000 积分，生成支出流水，发放记录变为已撤销。

##### 示例二：席位包不可撤销

- 运营发放 10 个席位，租户已有 9 个席位被成员占用。
- 撤销后总席位会小于已用席位。
- 系统阻断撤销，并提示席位已被占用。

#### 2.5.4 流程与原型截图标记

- 图 GRANT-01 / 标记 A：租户详情中的发放记录列表。
- 图 GRANT-02 / 标记 A：内部积分包发放弹窗。
- 图 GRANT-03 / 标记 A：内部席位包发放弹窗。
- 图 GRANT-04 / 标记 A：发放记录上的撤销按钮。
- 图 GRANT-05 / 标记 A：撤销原因填写弹窗。
- 图 GRANT-06 / 标记 A：不可撤销原因提示。
- 图 GRANT-07 / 标记 A：已撤销记录状态和撤销信息。

#### 2.5.5 验收标准

- 发放内部积分包成功后，租户详情展示对应发放记录。
- 积分余额足够时，积分包可撤销并生成支出流水。
- 积分余额不足时，积分包不可撤销。
- 撤销后总席位不小于已用席位时，席位包可撤销。
- 席位已被占用导致额度不足时，席位包不可撤销。
- 赠送积分已被消耗导致余额不足时，席位包不可撤销。
- 已撤销记录不提供再次撤销操作。

---

### 2.6 登录页协议确认

#### 2.6.1 目标

- 登录页展示用户协议和隐私协议确认入口。
- 用户同意两份协议后，才可继续登录或自动注册。
- 协议正文由独立协议文档接入，PRD 只定义页面入口和交互规则。

#### 2.6.2 任务拆解

##### 任务一：展示协议入口

- 登录页手机号输入区域下方展示「我已阅读并同意《用户协议》和《隐私协议》」勾选项。
- 《用户协议》和《隐私协议》均为可点击入口。
- 默认不勾选。

##### 任务二：协议查看

- 用户点击《用户协议》后，新开浏览器标签页打开用户协议页面。
- 用户点击《隐私协议》后，新开浏览器标签页打开隐私协议页面。
- 协议页面承载对应协议正文，原型阶段使用 mock 协议数据，正式上线前替换为法务确认后的正式协议正文。
- 协议查看页不承担勾选动作，用户回到登录页后需主动勾选协议确认项。

##### 任务三：登录拦截

- 用户未勾选同意两份协议时，不允许进入验证码下一步。
- 手机号有效时「下一步」保持可点击；用户未勾选协议并点击「下一步」时，系统提示“请先阅读并同意用户协议和隐私协议。”。
- 用户未勾选同意两份协议时，不允许未注册手机号自动注册。
- 用户勾选协议确认项后，才可继续登录或自动注册。

#### 2.6.3 业务示例

##### 示例一：未勾选协议

- 用户输入手机号，但未勾选协议确认项。
- 用户点击「下一步」。
- 系统提示“请先阅读并同意用户协议和隐私协议。”，不进入验证码步骤。

##### 示例二：查看并同意协议

- 用户点击《用户协议》。
- 系统新开浏览器标签页展示用户协议页面。
- 用户也可点击《隐私协议》，新开浏览器标签页展示隐私协议页面。
- 用户阅读后回到登录页，手动勾选协议确认项。

#### 2.6.4 流程与原型截图标记

- 图 LOGIN-01 / 标记 A：登录页协议勾选项。
- 图 LOGIN-01 / 标记 B：《用户协议》查看入口。
- 图 LOGIN-01 / 标记 C：《隐私协议》查看入口。
- 图 LOGIN-02 / 标记 A：用户协议独立页面。
- 图 LOGIN-03 / 标记 A：隐私协议独立页面。
- 图 LOGIN-04 / 标记 A：未勾选协议时点击「下一步」后的提示状态。

#### 2.6.5 验收标准

- 登录页默认展示协议勾选项。
- 协议确认项默认不勾选。
- 手机号有效但未勾选协议确认项时，「下一步」可点击。
- 未勾选协议确认项时，用户点击「下一步」后系统提示“请先阅读并同意用户协议和隐私协议。”，且不能进入验证码下一步。
- 未勾选协议确认项时，未注册手机号不能自动注册。
- 点击《用户协议》可新开浏览器标签页打开用户协议页面。
- 点击《隐私协议》可新开浏览器标签页打开隐私协议页面。
- 用户阅读协议后回到登录页，需主动勾选协议确认项，系统不通过协议页面自动勾选。

---

## 3. 优化项

### 3.1 团队席位支付规则展示优化

#### 3.1.1 目标

- 购买、增加席位、续约时清楚展示规格、金额和到期规则。
- 管理员能理解为什么增购不能切换规格，以及金额如何折算。

#### 3.1.2 任务拆解

##### 任务一：首次购买

- 首次购买可选择商品中心启用的月付或年付规格。
- 支付金额按所选规格和购买席位数计算。
- 支付成功后生成新的统一到期日。

##### 任务二：增加席位

- 已有有效团队席位订阅时，增加席位只能沿用当前有效订阅周期。
- 增加席位金额按新增席位数和剩余服务天数折算。
- 增加席位后统一到期日不变。

##### 任务三：续约

- 续约按当前总席位统一续费。
- 续约可选择月付或年付规格。
- 未到期续约从当前统一到期日顺延。
- 已过期续约从支付成功日重新计算。

#### 3.1.3 业务示例

##### 示例一：增加席位

- 当前团队有 10 个席位，到期日为 2027-03-12。
- 管理员在 2026-12-12 增加 2 个席位。
- 新增 2 个席位按剩余服务天数折算，到期日仍为 2027-03-12。

##### 示例二：未到期续约

- 当前团队 10 个席位，到期日为 2027-03-12。
- 管理员选择年付续约。
- 新到期日从 2027-03-12 顺延一年。

#### 3.1.4 流程与原型截图标记

- 图 PAY-01 / 标记 A：组织管理顶部「增加席位」按钮。
- 图 PAY-01 / 标记 B：组织管理顶部「续约」按钮。
- 图 PAY-02 / 标记 A：首次购买弹窗中的月付/年付规格。
- 图 PAY-03 / 标记 A：增加席位弹窗中的锁定规格提示。
- 图 PAY-03 / 标记 B：折算规则说明。
- 图 PAY-04 / 标记 A：续约弹窗中的续约席位和规格选择。
- 图 PAY-05 / 标记 A：支付确认页中的统一到期日。

#### 3.1.5 验收标准

- 未开通团队席位时，管理员可选择启用的月付或年付规格。
- 已有有效订阅时，增加席位不可切换规格。
- 增加席位金额展示剩余天数折算规则。
- 续约按当前总席位计算续费金额。
- 支付确认页展示统一到期日和折算规则。

---

### 3.2 组织成员与席位额度规则

#### 3.2.1 目标

- 组织管理明确展示最大有效席位、已占用席位、可添加成员和席位到期时间。
- 管理员能理解禁用不释放额度，移除账号才释放额度。

#### 3.2.2 任务拆解

##### 任务一：席位类型

- 当前席位只有长期免费席位和付费席位。
- 长期免费席位只绑定租户归属人。
- 自注册用户默认为租户归属人。
- 运营创建租户时填写的初始管理员为租户归属人。
- 除租户归属人外，其他成员均使用付费席位。

##### 任务二：成员添加

- 添加后的成员数量必须小于等于最大有效席位数。
- 每添加 1 名成员，占用 1 个有效席位额度。
- 新添加成员账号状态默认为已启用。
- 超过额度时阻断添加。

##### 任务三：禁用与启用

- 禁用成员只改变账号状态，不释放席位额度。
- 已禁用账号不可继续登录和使用工作台。
- 启用成员不额外占用席位，继续使用原额度。

##### 任务四：移除成员

- 移除成员后，该账号从当前租户组织中移除。
- 移除账号释放 1 个有效席位额度。

#### 3.2.3 业务示例

##### 示例一：添加成员

- 最大有效席位为 40，已占用 39。
- 管理员添加 1 名成员。
- 添加成功后已占用席位变为 40，可添加成员数变为 0。

##### 示例二：禁用成员

- 最大有效席位为 40，已占用 40。
- 管理员禁用 1 名成员。
- 已占用席位仍为 40，可添加成员数仍为 0。

##### 示例三：移除成员

- 最大有效席位为 40，已占用 40。
- 管理员移除 1 名成员。
- 已占用席位变为 39，可添加成员数变为 1。

#### 3.2.4 流程与原型截图标记

- 图 ORG-01 / 标记 A：组织管理顶部有效席位统计。
- 图 ORG-01 / 标记 B：成员搜索框。
- 图 ORG-02 / 标记 A：「添加成员」按钮。
- 图 ORG-03 / 标记 A：成员列表中的账号状态。
- 图 ORG-03 / 标记 B：成员列表中的席位有效期。
- 图 ORG-04 / 标记 A：禁用成员二次确认。
- 图 ORG-05 / 标记 A：移除成员二次确认。
- 图 ORG-06 / 标记 A：席位额度不足提示。

#### 3.2.5 验收标准

- 已占用席位小于最大有效席位时，管理员可添加成员。
- 已占用席位等于最大有效席位时，系统阻断添加成员。
- 禁用成员后，账号状态变为已禁用，已占用席位数不变。
- 启用成员后，账号状态变为已启用，已占用席位数不变。
- 移除成员后，成员从组织中移除，已占用席位数减 1。
- 成员列表展示租户归属人和其他成员的席位有效期。

---

### 3.3 模型缓存计量计费规则优化

#### 3.3.1 目标

- 大模型计量支持普通输入、Cache 写入、Cache 读取、输出四类 tokens。
- 按供应商 usage 口径归一化后再计费，避免 cached tokens 重复按普通输入计费。

#### 3.3.2 任务拆解

##### 任务一：模型资源配置

- 大模型资源配置支持四类成本单价：普通输入、Cache 写入、Cache 读取、输出。
- 大模型资源配置支持四类计量单价：普通输入、Cache 写入、Cache 读取、输出。

##### 任务二：usage 归一化

- 服务商原始 usage 字段不得直接进入计费公式。
- 供应商适配器需先归一化为普通输入 tokens、Cache 写入 tokens、Cache 读取 tokens、输出 tokens。
- 总输入 tokens 仅用于对账展示，等于三类输入 tokens 之和。

##### 任务三：金额与积分计算

- 四类 tokens 分别乘以对应单价后汇总成本金额。
- 四类 tokens 分别乘以对应计量单价后汇总计量金额。
- 积分扣减基于计量金额、积分汇率、最小扣减和取整单位计算。

##### 任务四：异常处理

- usage 字段缺失、字段冲突、归一化结果不一致时，不进入正常扣费流程。
- 需记录异常 usage 原文和适配错误原因。

#### 3.3.3 供应商 usage 适配口径

- OpenAI Chat Completions / Responses：
  - `prompt_tokens` 或 `input_tokens` 为总输入。
  - `prompt_tokens_details.cached_tokens` 或 `input_tokens_details.cached_tokens` 为 Cache 读取。
  - 普通输入 tokens = 总输入 tokens - Cache 读取 tokens。
  - OpenAI 当前不返回独立 Cache 写入 tokens，Cache 写入 tokens 记为 0。
- Anthropic Claude Messages：
  - `input_tokens` 为普通输入 tokens。
  - `cache_creation_input_tokens` 为 Cache 写入 tokens。
  - `cache_read_input_tokens` 为 Cache 读取 tokens。
  - 不得用 `input_tokens` 再扣减 Cache 字段。
- DeepSeek Chat Completions：
  - `prompt_cache_miss_tokens` 为普通输入 tokens。
  - `prompt_cache_hit_tokens` 为 Cache 读取 tokens。
  - Cache 写入 tokens 记为 0。
- 阿里云百炼 DashScope OpenAI 兼容接口：
  - Chat Completions 未返回缓存字段时，`prompt_tokens` 计入普通输入 tokens。
  - Responses 返回 `input_tokens_details.cached_tokens` 时，按 OpenAI 口径处理。
- 本地部署或无缓存字段供应商：
  - 输入 tokens 全部计入普通输入 tokens。
  - Cache 写入和 Cache 读取 tokens 均为 0。

#### 3.3.4 计算公式

- 总输入 tokens = 普通输入 tokens + Cache 写入 tokens + Cache 读取 tokens。
- 当服务商返回的总输入 tokens 已包含 Cache 写入 tokens 和 Cache 读取 tokens 时，普通输入 tokens = 总输入 tokens - Cache 写入 tokens - Cache 读取 tokens。
- 本次计量金额 = （普通输入 tokens × 普通输入单价 + Cache 写入 tokens × Cache 写入单价 + Cache 读取 tokens × Cache 读取单价 + 输出 tokens × 输出单价）÷ 1,000,000。
- 本次成本金额使用同样公式，但使用四类成本单价。
- 扣减积分 = 本次计量金额 × 积分汇率，再按取整单位和最小扣减积分处理。

#### 3.3.5 业务示例

##### 示例一：OpenAI

- OpenAI 返回总输入 100,000，cached 40,000，输出 20,000。
- 平台归一化为普通输入 60,000，Cache 读取 40,000，Cache 写入 0，输出 20,000。
- Cache 读取按 Cache 读取单价计费，不再按普通输入单价重复计费。

##### 示例二：Anthropic

- Anthropic 返回 input 30,000，cache creation 10,000，cache read 50,000，output 15,000。
- 平台归一化为普通输入 30,000，Cache 写入 10,000，Cache 读取 50,000，输出 15,000。
- 不再用 input 30,000 扣减 Cache 字段。

#### 3.3.6 调研依据

- OpenAI：[官方 Prompt Caching 文档](https://platform.openai.com/docs/guides/prompt-caching)说明 `cached_tokens` 位于 `prompt_tokens_details`，且 `prompt_tokens` 包含缓存 tokens。
- Anthropic：[官方 Prompt Caching 文档](https://docs.anthropic.com/en/docs/build-with-claude/prompt-caching)说明 usage 返回 `input_tokens`、`cache_creation_input_tokens`、`cache_read_input_tokens` 和 `output_tokens`。
- DeepSeek：[官方 KV Cache 文档](https://api-docs.deepseek.com/guides/kv_cache)说明 usage 返回 `prompt_cache_hit_tokens` 和 `prompt_cache_miss_tokens`。
- 阿里云百炼：[官方 DashScope OpenAI 兼容接口文档](https://help.aliyun.com/document_detail/2713153.html/)示例返回 `prompt_tokens`、`completion_tokens`、`total_tokens`，Responses usage 可返回 `input_tokens_details.cached_tokens`。

#### 3.3.7 流程与原型截图标记

- 图 MTR-01 / 标记 A：大模型资源配置弹窗。
- 图 MTR-01 / 标记 B：普通输入、Cache 写入、Cache 读取、输出成本字段。
- 图 MTR-02 / 标记 A：四类售价预览。
- 图 MTR-03 / 标记 A：消耗对账列表中的输入 tokens 拆分。
- 图 MTR-03 / 标记 B：输出 tokens、计量金额、成本金额和扣减积分。
- 图 MTR-04 / 标记 A：usage 适配异常记录。

#### 3.3.8 验收标准

- 模型配置弹窗展示四类成本配置项。
- 成本倍率可生成四类计量单价预览。
- OpenAI cached tokens 计入 Cache 读取 tokens。
- Anthropic input tokens 直接计入普通输入 tokens，不再扣减 Cache 字段。
- DeepSeek hit tokens 计入 Cache 读取 tokens，miss tokens 计入普通输入 tokens。
- 百炼未返回缓存字段时，prompt tokens 计入普通输入 tokens。
- usage 归一化异常时，系统阻断正常扣费并记录原因。
- 消耗对账展示普通输入、Cache 写入、Cache 读取和输出 tokens。

---

### 3.4 企业驾驶舱图表时间规则优化

#### 3.4.1 目标

- 驾驶舱「今日 / 本周 / 本月」筛选规则统一。
- 趋势图、排行图、分布图和明细入口使用同一统计时间范围。
- 修正本周、本月图表横轴时间不准确的问题。

#### 3.4.2 任务拆解

##### 任务一：统一时间范围

- 时间计算以租户业务时区为准，默认使用北京时间。
- 今日：当前自然日 00:00 至当前时刻。
- 本周：当前自然周周一 00:00 至当前时刻。
- 本月：当前自然月 1 日 00:00 至当前时刻。
- 后端查询使用左闭右开区间，避免边界时间重复统计。

##### 任务二：趋势图横轴规则

- 企业积分/成本消耗趋势按当前筛选范围生成连续时间点。
- 趋势值展示当前时间桶内的实际消耗，不展示累计消耗。
- 今日按小时聚合，横轴从 00:00 展示到当前小时。
- 本周按日聚合，横轴从本周周一展示到当天。
- 本月按日聚合，横轴从本月 1 日展示到当天。
- 无数据的时间点补 0 或显示为空点，但不得直接丢失横轴日期。
- 趋势图不得只取周期起点和当前日期两个点。

##### 任务三：排行和分布规则

- 成员用量排行按当前筛选范围内的用量汇总排序。
- AI 专家用量排行按当前筛选范围内的调用用量汇总排序。
- AI 专家开发分布按当前筛选范围内的 AI 专家创建时间统计开发者分布。
- 排行和分布的详情入口必须带上当前时间筛选参数。

##### 任务四：统计时间字段

- 用量类图表以调用完成并完成计量入账的时间为准。
- 开发类图表以 AI 专家创建成功时间为准。
- 明细页与驾驶舱图表必须使用同一时间字段和同一筛选范围。

#### 3.4.3 业务示例

##### 示例一：本周

- 当前日期为 2026-06-08，星期一。
- 用户选择「本周」。
- 统计范围从 2026-06-08 00:00 开始，不应出现 2026-06-01 的数据点。

##### 示例二：本月

- 当前日期为 2026-06-08。
- 用户选择「本月」。
- 趋势图横轴应按日展示 6/1 至 6/8 的连续日期，不应只展示 6/1 和 6/8 两个端点。

#### 3.4.4 流程与原型截图标记

- 图 DASH-01 / 标记 A：驾驶舱顶部时间筛选「今日 / 本周 / 本月」。
- 图 DASH-02 / 标记 A：Agent 用量趋势横轴日期。
- 图 DASH-03 / 标记 A：成员用量排行。
- 图 DASH-04 / 标记 A：AI 专家用量排行。
- 图 DASH-05 / 标记 A：AI 专家开发分布。
- 图 DASH-06 / 标记 A：用量明细或开发明细继承当前时间筛选。

#### 3.4.5 验收标准

- 选择今日时，趋势图按小时展示当天 00:00 至当前小时。
- 选择本周时，趋势图从本周周一开始按日展示，不包含上周日期。
- 选择本月时，趋势图从本月 1 日开始按日展示，不包含上月日期。
- 企业积分/成本消耗趋势展示时间桶实际消耗，不按累计消耗展示。
- 本周和本月趋势图展示连续日期，不只展示起止两个点。
- 成员用量排行、AI 专家用量排行和 AI 专家开发分布均使用当前筛选范围。
- 点击用量明细、开发明细后，明细页时间范围与驾驶舱当前筛选一致。
