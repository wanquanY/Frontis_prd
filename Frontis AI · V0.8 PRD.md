# Frontis AI · V0.8 PRD

## 1. 版本范围

### 1.1 本期需求

- 专家广场基础能力。
- 企业专区基础能力。
- 开发空间（进化实验室）发布 AI 专家时支持描述、使用指南和场景标签生成与编辑。
- 工作台多 session 与对话记录。
- 工作台成果分享。
- 运营后台内部积分包与席位包发放撤销。
- 登录页用户协议与隐私协议确认。
- 商品中心席位包有效时间与团队席位购买规则优化。
- 企业管理后台组织成员与席位额度规则优化。
- 模型缓存计量计费规则优化。
- 企业驾驶舱图表时间规则优化。

---

## 2. 新增需求

### 2.1 工作台多 session 与对话记录

#### 2.1.1 目标

- ME 和单个 AI 专家均支持多个独立 session。
- 用户切换 session 或页面时，运行中任务不中断。
- 对话记录明确展示运行、排队、完成、终止、未读、已读状态。
- 对话记录需展示对应 ME 或 AI 专家的头像，帮助用户识别会话来源。

#### 2.1.2 任务拆解

##### 任务一：session 创建

- 用户从新对话入口提交首条任务内容后创建 session。
- session 标题默认取第一句话。
- session 写入左侧对话记录。

##### 任务二：session 隔离

- 每个 session 拥有独立聊天原文、临时文件、附件引用、工具调用明细和执行过程状态。
- 不同 session 不直接共享上下文。
- session 上下文由当前 session 内的用户输入、附件、系统生成内容、工具调用结果和全局配置共同构成。

##### 任务三：既有能力继承与全局配置

- 多 session 不改变既有工具调用、附件上传、成果文件生成、成果预览能力的业务规则。
- 附件上传仍作为当前 session 的任务输入处理；附件不会自动进入其他 session 上下文。
- 工具调用能力保持既有调用链路；工具调用明细按 session 记录，作为当前 session 的过程数据沉淀。
- 成果文件仍按既有能力生成、预览和回溯；成果文件默认归属于产出它的 session，不自动进入其他 session 上下文。
- 成果分享属于 V0.8 本期新增需求，按 2.5 独立实现；分享内容按生成链接时的成果文件快照处理，不作为多 session 的既有能力继承项。
- Agent 人设、Agent 可调用工具、ME 可调度专家列表属于账号、租户和智能体维度的全局配置，配置变化对该范围内的 session 全局生效。
- 全局配置不回写修改历史消息、历史工具调用明细和已生成成果文件；运行中任务的已执行步骤不重算，后续新步骤和新会话轮次读取最新可用配置。

##### 任务四：任务并发与排队

- 当前最多 3 个会话任务同时运行。
- 第 4 个及之后的任务进入排队。
- 有运行任务完成或终止后，排队任务按进入顺序开始。

##### 任务五：任务切换与恢复

- 用户切走运行中 session 后，任务继续执行。
- 用户切回时，展示当前进度和已产生内容。
- 运行中 session 支持终止操作。

##### 任务六：对话记录管理

- ME 和 AI 专家的 session 统一沉淀为对话记录资产。
- 每条对话记录需包含对应 ME 或 AI 专家的头像、会话标题和更新时间。
- 对话记录支持重命名和删除。
- 删除需二次确认，删除后不可恢复。

##### 任务七：可调度专家添加入口

- 可调度 AI 专家支持从专家广场商品版和企业专区内部版两个来源添加。
- 添加专家入口内按来源切换和添加状态筛选候选专家，候选专家条目不重复展示来源归属。
- 未添加专家可加入可调度专家列表；已添加专家可从可调度专家列表移除，并可再次添加。
- 专家来源仍按专家广场商品版或企业专区内部版保存，不互相覆盖。
- 同一底层 AI 专家同时以企业专区内部版和专家广场商品版加入工作台时，只在商品版专家实例上标记「来自专家广场」。
- 专家广场商品下架后，已添加到工作台的商品版专家实例保留并标记为已下架；用户选中该专家时提示“已下架不可继续对话，可以查看历史对话数据”。
- 添加专家入口切换到「专家广场 / 已添加」时，已下架商品版专家需展示「已下架」状态标签。

#### 2.1.3 业务示例

##### 示例一：多任务并行

- 用户同时提交 3 个任务：写报告、查竞品、整理会议纪要。
- 3 个 session 均进入运行中。
- 用户提交第 4 个任务时，该 session 进入排队中。

##### 示例二：切换不中断

- 用户在「写报告」session 运行中切到「查竞品」session。
- 写报告任务继续执行。
- 用户切回后，看到已生成的大纲和当前进度。

#### 2.1.4 规则清单

- session 按当前账号、租户和智能体隔离。
- ME session 与单个 AI 专家 session 分别保持独立上下文，并统一进入对话记录资产。
- session 隔离范围包括聊天原文、临时文件、附件引用、工具调用明细、执行过程状态和当前任务上下文。
- Agent 人设、Agent 可调用工具、ME 可调度专家列表为全局配置，不随单个 session 复制或分叉。
- 工具调用、附件上传、成果文件等既有能力保持原业务规则，仅在数据归属上绑定到对应 session。
- 对话记录头像取该 session 绑定的 ME 或 AI 专家头像；专家广场商品版使用已审核 AI 专家版本快照头像，企业专区内部版使用开发者发布的头像。
- 同一用户最多 3 个会话任务同时运行，超出后进入排队。
- 用户切换 session、切换智能体或离开当前页面时，运行中任务不中断。
- 删除对话记录只删除当前用户入口，不影响已经生成并被单独保存或分享的成果文件快照。
- 已下架专家广场商品版专家的历史对话、附件、工具调用结果和成果文件保留可查看；用户不可在该专家的新对话或历史 session 中继续发送消息。

#### 2.1.5 流程与原型截图标记

- 图 SES-01 / 标记 A：左侧对话记录入口。
- 图 SES-01 / 标记 B：新对话入口。
- 图 SES-02 / 标记 A：新 session 生成默认标题。
- 图 SES-03 / 标记 A：运行中、排队中、已完成、已终止状态标签。
- 图 SES-04 / 标记 A：运行中 session 的终止操作。
- 图 SES-05 / 标记 A：对话记录操作菜单中的重命名和删除。
- 图 SES-06 / 标记 A：未读状态提示。
- 图 SES-07 / 标记 A：可调度 AI 专家列表中的添加入口。
- 图 SES-08 / 标记 A：添加专家能力中的来源、添加状态和候选专家列表。

---

### 2.2 专家广场

#### 2.2.1 目标

- 专家广场用于发现运营上架的 AI 专家，用户可按专区和专区分类筛选、查看详情，并将可用专家添加到工作台。
- 从专家广场添加到工作台的 AI 专家，进入 ME 下方可调度专家列表，并支持用户选择后单独对话。
- 运营人员在商品中心维护专家广场商品的专区、专区分类、排序、上架状态和租户可见范围。
- 商品中心商品详情需区分商品运营配置和绑定 AI 专家上架版本快照；头像、名称、描述、使用指南和场景标签直接使用已审核 AI 专家版本快照，技能和核心文件为只读快照。
- AI 专家详情需包含绑定 AI 专家的头像、名称、版本号、描述、使用指南和场景标签，内容来自 AI 专家上架版本快照；场景标签数量为 1-5 个。
- 专家广场商品是稳定商品对象，绑定某个已审核通过的 AI 专家版本快照；AI 专家版本升级后，如需进入专家广场，必须重新提交版本更新上架申请。

#### 2.2.2 任务拆解

##### 任务一：发现与筛选

- 专家广场展示运营后台商品中心已上架、且当前租户可见的 AI 专家商品。
- 用户可按专家广场专区和专区分类筛选专家。
- 专家商品列表信息需包含头像、名称、简介和场景标签；这些展示信息来自已审核 AI 专家版本快照，场景标签数量为 1-5 个。
- 专家广场空状态提示当前分类下暂无可添加的 AI 专家。

##### 任务二：详情与添加

- 专家广场专家详情包含身份、技能和核心文件，不包含工具列表。
- 专家广场普通用户侧信息使用已审核 AI 专家版本快照中的头像、名称、简介和使用指南；身份信息只包含使用指南，不暴露版本号、模型、运行环境、发布时间、场景标签等元信息。
- 技能列表和核心文件来自该商品绑定的已审核 AI 专家上架版本快照。
- 专家广场专家详情不包含进化信息。
- 直接可添加且尚未加入工作台的专家支持添加到工作台；已加入工作台的专家支持移出工作台。
- 添加到工作台后，系统按专家广场商品生成工作台安装实例，既可被 ME 调度，也可被用户单聊；移出工作台后取消当前用户的工作台商品版实例，专家恢复为可重新添加状态。
- AI 专家商品获取方式不提供「免费试用」。

##### 任务三：运营后台商品配置

- 运营人员在商品中心维护 AI 专家商品的运营配置。
- 运营人员可维护专家广场专区、专区分类和排序。
- 运营人员可配置商品上架状态和租户可见范围。
- 商品详情需覆盖商品信息、AI 专家详情、技能列表和核心文件四类内容。
- 商品信息中的头像、名称、简介、场景标签和使用指南直接使用已审核 AI 专家版本快照，不在商品中心编辑。
- AI 专家上架审批通过后进入商品中心可绑定 AI 专家池，不自动生成商品列表记录。
- 运营新建 AI 专家商品时，需要从已审批通过且未商品化的 AI 专家中选择绑定对象。
- 运营选定绑定 AI 专家后，商品展示信息继承 AI 专家基础信息；运营只配置专区、二级分类、排序、获取方式、可见范围和上架状态。
- 商品特有字段包括专区、二级分类、排序、获取方式、可见范围和上架状态。
- AI 专家商品获取方式只支持「免费使用」和「联系客服」，不提供「免费试用」或「自定义客服」。
- 同一底层 AI 专家已经存在专家广场商品时，不再出现在新建 AI 专家商品的可选列表中。
- 版本更新审批通过时，默认更新同一底层 AI 专家对应的既有商品绑定版本快照，不生成新的 AI 专家商品，且保留原商品的专区、分类、排序、可见范围和上架状态。
- 绑定 AI 专家、技能列表和核心文件来自已审核上架版本快照，只读展示，不允许在商品中心直接编辑；AI 专家详情需展示专家头像、名称、版本号、描述、使用指南和场景标签，场景标签数量为 1-5 个。
- 运营人员在 AI 专家上架审批列表可区分「首次上架」和「版本更新」申请；审批详情需覆盖 AI 专家详情、上架理由、技能列表和核心文件，审核前可核对专家头像、基础信息、技能和核心文件。
- AI 专家商品获取方式不保留「免费试用」。

##### 任务四：商品下架后的工作台实例处理

- AI 专家商品下架后，专家广场不再展示该商品，新用户不可再从专家广场添加到工作台。
- 已添加到工作台的专家广场商品版实例不自动删除，工作台专家列表保留该专家并显示已下架状态。
- 用户选中已下架专家时，系统提示“已下架不可继续对话，可以查看历史对话数据”。
- 已下架商品版实例不再参与 ME 调度，也不允许用户发起新对话或在历史 session 中继续发送消息。
- 历史对话、附件、工具调用结果、成果文件、用量记录和计费记录保留可查看。
- 用户可手动移出工作台；移出只取消当前用户的工作台商品版实例，不删除历史对话和成果数据。
- 同一商品 ID 后续重新上架，且当前租户仍在可见范围内时，原工作台商品版实例恢复可用；如果运营新建另一个商品 ID，旧实例不自动迁移。

#### 2.2.3 业务示例

##### 示例一：从专家广场添加专家

- 用户在专家广场按专区和专区分类找到「销售话术专家」。
- 用户查看详情，确认头像、名称、简介、身份、技能和核心文件信息，不看到进化信息。
- 用户将该专家添加到工作台。
- 专家进入工作台可调度专家列表，可被 ME 调度，也可被用户单聊。

##### 示例二：联系客服获取专家

- 用户在专家广场看到配置为联系客服模式的 AI 专家。
- 用户发起人工开通联系。
- 系统提供客服二维码联系方式。
- 用户通过客服完成后续咨询或开通。

##### 示例三：已添加专家商品下架

- 用户已将「客户成功续约专家」从专家广场添加到工作台，并产生过历史对话和成果文件。
- 运营人员在商品中心将该 AI 专家商品下架。
- 专家广场不再展示该商品；用户工作台专家列表继续保留该专家并标记为已下架。
- 用户选中该专家时，系统提示“已下架不可继续对话，可以查看历史对话数据”；用户可查看历史对话和成果，但不可继续发送消息。

#### 2.2.4 规则清单

- 专家广场分类数据来自运营后台商品中心的专家广场专区和专区分类配置。
- 专家广场只展示已上架且对当前租户可见的 AI 专家商品。
- 专家广场详情不包含进化信息。
- 「添加到工作台」是专家广场商品进入使用体系的统一动作。
- 添加到工作台后的专家同时支持两种使用方式：ME 调度和用户单聊。
- 已添加到工作台的专家广场商品支持移出工作台；移除后不删除专家广场商品，只取消当前用户的工作台安装实例。
- 专家广场商品版工作台实例按商品 ID 去重，不与企业专区内部版互相覆盖。
- 同一底层 AI 专家同时存在企业专区内部版和专家广场商品版工作台实例时，只在专家广场商品版实例标记「来自专家广场」；企业专区内部版不额外标记来源。
- 添加专家入口已经按企业专区和专家广场切换候选范围，候选专家条目不重复展示来源归属。
- 添加专家入口的专家广场已添加列表中，已下架商品版专家展示「已下架」状态标签。
- 普通用户从专家广场添加后，工作台和对话页展示已审核 AI 专家版本快照中的头像、名称、简介和场景标签。
- 专家广场详情中的头像、名称、简介使用已审核 AI 专家版本快照；身份信息只包含使用指南。
- 专家广场商品不单独维护头像、名称、描述、使用指南、场景标签、技能和核心文件，只引用已审核上架版本快照。
- 同一商品有新版本上架申请待审核或被驳回时，专家广场继续使用当前已通过版本快照，不受未通过版本影响。
- 版本更新审核通过后，原专家广场商品切换到新的已审核版本快照；商品 ID、商品配置和已添加到工作台的商品版实例不重复生成。
- 商品下架只影响专家广场商品版实例，不影响同一底层 AI 专家在企业专区内部版的展示、添加和对话。
- 商品下架不清理历史对话和成果数据，但已添加的商品版实例不可再运行新对话，也不可被 ME 调度。
- 商品重新上架且商品 ID 不变时，已添加到工作台的商品版实例按原商品关系恢复可用。
- AI 专家商品获取方式不出现「免费试用」。

#### 2.2.5 流程与原型截图标记

- 图 PLAZA-01 / 标记 A：专家广场专区筛选。
- 图 PLAZA-01 / 标记 B：专家广场专区分类筛选。
- 图 PLAZA-02 / 标记 A：专家广场专家卡片和「添加到工作台」入口。
- 图 PLAZA-03 / 标记 A：专家广场专家详情内容范围，不包含进化信息。
- 图 PLAZA-04 / 标记 A：专家广场客服联系方式。
- 图 PLAZA-05 / 标记 A：商品中心新建 AI 专家商品时选择已审批通过且未商品化的 AI 专家。
- 图 PLAZA-06 / 标记 A：工作台专家列表中的已下架状态，以及双来源同专家时商品版「来自专家广场」标识。
- 图 PLAZA-05 / 标记 B：商品中心商品详情中的绑定 AI 专家头像和只读信息。
- 图 PLAZA-05 / 标记 C：商品中心商品详情中的技能列表和核心文件只读快照。
- 图 APPROVAL-01 / 标记 A：AI 专家上架审批详情中的 AI 专家详情、上架理由、技能列表和核心文件。

---

### 2.3 开发空间（进化实验室）AI 专家发布字段生成

#### 2.3.1 目标

- 降低开发者发布 AI 专家时填写展示信息的成本。
- 开发空间（进化实验室）发布 AI 专家时，系统支持生成描述、使用指南和场景标签建议。
- 生成内容只是草稿建议，开发者必须可编辑，最终发布版本以开发者确认后的字段为准。

#### 2.3.2 任务拆解

##### 任务一：字段生成

- 开发者在开发空间（进化实验室）发布 AI 专家时，可对描述、使用指南和场景标签触发生成。
- 系统可基于开发空间（进化实验室）已保存的 AI 专家配置和开发者已填写内容生成字段建议，不要求开发者重复输入生成上下文。
- 场景标签生成结果仍按 AI 专家场景标签规则校验，数量必须为 1-5 个。
- 生成失败不阻断开发者手动填写和发布。

##### 任务二：开发者编辑

- 开发者可编辑系统生成的描述、使用指南和场景标签。
- 生成结果不得静默覆盖开发者已编辑内容；再次生成时，应以草稿候选或明确覆盖提示承接。
- 开发者确认发布前，系统按字段必填、长度和场景标签数量规则校验。

##### 任务三：发布版本沉淀

- 发布成功后，开发者确认后的描述、使用指南和场景标签进入当前 AI 专家发布版本基础信息。
- 企业专区使用开发者发布版本中的头像、名称、描述、使用指南和场景标签，其中描述作为企业内部专家简介。
- 后续申请上架时，系统自动带入当前发布版本的描述、使用指南和场景标签，进入运营审批快照。
- 商品中心使用已审核 AI 专家版本快照展示头像、名称、描述、使用指南和场景标签，不反向覆盖开发者在企业专区看到的 AI 专家发布版本信息。

#### 2.3.3 业务示例

##### 示例一：生成后编辑发布

- 开发者完成「合同风控 Agent」配置后进入发布流程。
- 开发者触发生成描述、使用指南和场景标签。
- 系统生成字段建议，开发者调整描述措辞并删除不合适的标签。
- 开发者确认发布后，编辑后的字段写入该 AI 专家发布版本。

##### 示例二：生成失败后手动填写

- 开发者触发使用指南生成失败。
- 系统提示生成失败，保留当前已填写内容。
- 开发者手动填写使用指南并完成发布。

#### 2.3.4 规则清单

- 描述、使用指南和场景标签的生成能力只辅助开发者填写，不自动替开发者确认发布。
- 开发者编辑后的字段优先级高于系统生成建议。
- 场景标签为 AI 专家场景标签，发布版本、企业专区展示、上架申请字段和专家广场商品展示字段使用同一类标签；成为商品后不在商品中心单独编辑标签，专家广场展示已审核 AI 专家版本快照中的标签。
- 场景标签必须 1-5 个；少于 1 个或超过 5 个时，不允许发布 AI 专家版本，也不允许提交上架申请。
- 发布版本字段变更只对新发布版本生效，不自动改写历史发布版本、历史上架申请或已通过的商品版本快照。

#### 2.3.5 流程与原型截图标记

- 图 DEV-01 / 标记 A：开发空间（进化实验室）发布 AI 专家时的描述生成入口。
- 图 DEV-01 / 标记 B：开发空间（进化实验室）发布 AI 专家时的使用指南生成入口。
- 图 DEV-01 / 标记 C：开发空间（进化实验室）发布 AI 专家时的场景标签生成入口和标签编辑状态。
- 图 DEV-02 / 标记 A：开发者编辑并确认发布版本字段。

---

### 2.4 企业专区

#### 2.4.1 目标

- 企业专区用于管理和使用当前用户发布或团队共享的 AI 专家。
- 企业专区只按「我发布的」和「团队共享」两个来源组织，不承载专家广场已添加商品。
- 从企业专区添加到工作台的 AI 专家按企业内部专家生成工作台安装实例，进入 ME 下方可调度专家列表，并支持用户选择后单独对话。
- 企业专区「我发布的」AI 专家支持删除，但删除前必须校验工作台使用关系和专家广场商品关系，避免影响正在使用或售卖的专家。

#### 2.4.2 任务拆解

##### 任务一：来源与资产管理

- 企业专区只展示「我发布的」和「团队共享」两个来源分类。
- 企业专区不展示「全部」来源，不展示第二排业务分类。
- 「我发布的」展示当前用户自己开发、发布或可管理的 AI 专家。
- 「团队共享」展示组织内共享给当前用户的 AI 专家。
- 从专家广场添加到工作台的专家进入工作台可调度列表，不作为企业专区来源分类展示。
- 企业专区空状态提示当前来源下暂无企业专区专家。
- 「我发布的」专家支持删除；删除前校验当前账号使用、团队成员使用和未下架专家广场商品依赖。
- 无依赖时二次确认后删除；命中任一依赖时阻断删除并提示主要原因。
- 删除仅从「我发布的」列表移除专家，不影响历史会话、成果记录和已沉淀数据。

##### 任务二：详情与添加

- 企业专区专家详情包含身份、技能、进化和核心文件，不包含工具列表。
- 企业专区保留进化信息；进化内容来自用户使用该专家后产生的数据，并在执行进化后生成。
- 未使用或使用后尚未执行进化时，企业专区详情中的进化内容为空。
- 企业专区尚未加入工作台的专家支持添加到工作台，已加入工作台的专家支持移出工作台；不提供「免费使用」或「去使用」状态。
- 添加到工作台后，系统按企业内部专家 ID 生成或更新工作台内部版实例，既可被 ME 调度，也可被用户单聊；移出工作台后取消当前用户的工作台内部版实例，企业专区专家恢复为可重新添加状态。

##### 任务三：申请上架

- 对当前用户自己发布且具备上架申请权限的专家，企业专区提供申请上架能力。
- 申请上架时，申请端只填写上架理由。
- AI 专家基础信息包括头像、名称、版本号、描述、使用指南和场景标签；场景标签必须为 1-5 个，少于 1 个或超过 5 个时不可作为发布版本提交上架。
- 系统随申请自动提交当前 AI 专家发布版本的上述基础信息和上架理由；基础信息不在申请端重复展示，运营后台审批详情使用同一份版本快照数据。
- 同一版本已有待审核申请时，用户可修改申请；同一版本已驳回申请可重新提交；同一版本已通过申请不允许重复提交。
- AI 专家发布新版本后，若旧版本已通过上架审批，企业专区卡片提示当前版本需重新申请，开发者可对新版本发起「版本更新」上架申请。
- 申请提交后进入运营后台 AI 专家上架审批列表，运营人员在审批详情中查看同一份基础信息和上架理由。
- 是否可上架和后续商品化配置由运营后台商品中心与上架申请流程承接。

#### 2.4.3 业务示例

##### 示例一：查看团队共享专家

- 用户进入企业专区并切换到「团队共享」。
- 页面展示组织内共享给当前用户的 AI 专家。
- 用户查看详情，确认头像、名称、简介、身份、技能、进化和核心文件信息。
- 用户将该专家添加到工作台后，该专家进入或保持在工作台可调度专家列表。

##### 示例二：允许删除

- 用户发布的「销售话术专家」未被任何人添加到工作台，也未上架为专家广场商品。
- 用户发起删除并确认。
- 专家从「我发布的」列表移除，历史对话仍可在原记录中查看。

##### 示例三：不可删除

- 「合同摘要专家」已被团队成员添加到工作台。
- 发布者点击删除。
- 系统阻断删除，并提示仍有团队成员正在使用。

#### 2.4.4 规则清单

- 企业专区只按来源分类，不混入专家广场已添加商品。
- 企业专区详情保留进化。
- 企业专区详情中的头像、名称、简介使用开发者发布的企业内部专家信息；身份信息只包含使用指南。
- 「添加到工作台」是企业专区内部专家进入使用体系的统一动作。
- 添加到工作台后的专家同时支持两种使用方式：ME 调度和用户单聊。
- 已添加到工作台的企业专区专家支持移出工作台；移除后不删除企业专区专家资产，只取消当前用户的工作台安装实例。
- 企业专区内部版工作台实例按企业内部专家 ID 去重，不与专家广场商品版互相覆盖。
- 开发者从企业专区添加后，工作台和对话页展示开发者发布的企业内部专家头像、名称、简介和场景标签。
- 同一 AI 专家上架到专家广场后，企业专区仍展示开发者发布的企业内部专家信息；专家广场展示已审核 AI 专家版本快照中的专家信息。
- AI 专家版本升级不会自动同步到专家广场；旧版本已通过上架审批且当前发布版本更新时，企业专区卡片提示当前版本需重新申请，并由运营审核通过后生效。
- 企业专区删除仅作用于当前「我发布的」列表展示，不清理历史会话和成果。
- 专家存在工作台、团队成员使用或未下架专家广场商品依赖时，不允许删除。
- 上架申请提交对象与运营后台审批对象一致，申请端只填写上架理由，系统自动带入当前发布版本的头像、名称、版本号、描述、使用指南和场景标签；场景标签按 1-5 个校验并进入审批快照。

#### 2.4.5 流程与原型截图标记

- 图 TEAM-01 / 标记 A：企业专区「我发布的 / 团队共享」来源筛选。
- 图 TEAM-02 / 标记 A：企业专区专家详情内容范围，保留进化信息。
- 图 TEAM-03 / 标记 A：「我发布的」列表中的删除能力。
- 图 TEAM-04 / 标记 A：发起删除后的依赖校验。
- 图 TEAM-05 / 标记 A：无依赖时的删除二次确认。
- 图 TEAM-06 / 标记 A：有依赖时展示不可删除提示。
- 图 TEAM-07 / 标记 A：删除成功后列表移除对应专家。
- 图 TEAM-08 / 标记 A：申请上架弹窗仅填写上架理由，版本快照信息由系统自动随申请提交。
- 图 TEAM-09 / 标记 A：旧版本已通过上架审批且当前发布版本更新时，企业专区卡片提示当前版本需重新申请。

---

### 2.5 工作台成果分享

#### 2.5.1 目标

- 用户可将成果文件生成分享链接。
- 收件人通过链接在线预览分享内容，并能看到 Frontis AI 产品入口。

#### 2.5.2 任务拆解

##### 任务一：成果文件分享

- 分享入口只出现在成果文件预览面板。
- 已生成、未删除、可预览的成果文件可生成分享链接。
- 分享内容保存为当前成果文件快照；同一成果文件再次分享时，复用原分享链接并刷新该链接对应的预览快照。

##### 任务二：支持文件类型

- 本期成果分享仅支持常用可在线预览文件类型。
- 文档类：`md`、`markdown`、`txt`、`doc`、`docx`。
- 数据类：`json`、`csv`。
- 表格类：`xls`、`xlsx`。
- 演示文稿类：`ppt`、`pptx`。
- 网页类：`html`、`htm`。
- 图片类：`png`、`jpg`、`jpeg`、`webp`、`svg`。
- PDF：`pdf`。
- 未命中以上类型的成果文件不生成分享链接，页面提示当前文件类型暂不支持分享。

##### 任务三：分享链接生成

- 成果文件校验通过后生成不可枚举 token 链接。
- hover 成果预览页分享入口时展示分享浮层，浮层包含「分享文档」标题、「互联网分享已开启 / 已关闭」状态、公开阅读说明、分享链接展示、复制链接按钮和分享开关。
- 分享开关关闭后，链接不可复制，外部访问应按已关闭状态处理；再次开启后复用同一成果分享链接。
- 同一成果文件已经生成分享链接时，再次点击分享应复用已有链接，并用当前文件内容刷新该链接对应的预览快照。
- 分享链接默认永久有效，除非用户主动关闭互联网分享。
- 本期不做对话分享、复制文本、生成图片、渠道发送、权限配置、查看次数、访问统计和分享管理。

##### 任务四：分享页预览

- 文件分享页直接展示文件快照的主体内容，不额外展示「成果分享」标签、文件名称标题、文件类型或分享来源说明。
- 分享页不展示原工作台侧边栏、内部工具日志、文件真实路径。
- 分享页展示 Frontis AI 产品入口。
- 如果成果文件在分享后发生更新，用户再次点击分享并复制链接后，分享页展示更新后的文件快照内容。
- 分享链接过期后，访问原链接展示不可访问状态，不展示文件快照内容。

#### 2.5.3 业务示例

##### 示例一：文件分享

- 用户基于「AI 发展趋势报告.docx」生成分享链接并复制。
- 收件人打开链接后，只能在线预览该文件快照。
- 用户更新该成果文件后再次点击分享入口，系统复用已有分享链接，并让该链接展示更新后的文件快照。
- 用户关闭互联网分享后，收件人再次打开原链接时无法访问该文件快照。

#### 2.5.4 流程与原型截图标记

- 图 SHR-01 / 标记 A：成果文件分享入口。
- 图 SHR-02 / 标记 A：文件分享链接生成结果。
- 图 SHR-06 / 标记 A：文件分享预览页。
- 图 SHR-07 / 标记 A：文件分享页产品入口。

---

### 2.6 运营后台内部积分包发放、席位包分配续约与撤销

#### 2.6.1 目标

- 运营给租户发放内部积分包、分配内部席位包或使用内部席位包续约后，系统沉淀可追踪记录。
- 发放错误、合同调整或业务取消时，运营可在规则允许时撤销发放。
- 撤销为反向冲正，不删除原记录；席位续约记录只延长到期时间，不进入发放撤销范围。

#### 2.6.2 任务拆解

##### 任务一：发放记录

- 积分包发放、席位分配和席位续约成功后，均生成权益记录。
- 记录字段包括类型、动作、内容、关联订单号、状态、操作人、操作时间、撤销人、撤销时间、撤销原因。
- 状态包含已生效、已撤销。
- 内部席位包动作必须选择商品中心已启用的内部席位包，并保存席位包名称、每席价格、赠送积分、有效时间、席位数、操作前到期时间和操作后到期时间快照。

##### 任务二：内部席位分配

- 租户详情提供「分配席位」入口，用于新增席位数量，不用于续约已有席位。
- 租户没有有效团队席位时，到期时间按当前日期加所选内部席位包有效时间计算。
- 租户已有有效团队席位时，本次新增席位到期时间必须对齐当前全部有效席位的最终到期时间，不允许运营手动改成新的到期日。
- 分配成功后增加租户总席位，生成线下订阅订单和席位分配权益记录。

##### 任务三：内部席位续约

- 租户详情提供「席位续约」入口，用于给当前全部有效团队席位统一续约，不新增席位数量。
- 当前没有有效团队席位时，不允许执行席位续约。
- 续约必须选择商品中心已启用的内部席位包，续约后到期时间 = 当前全部有效席位最终到期时间 + 所选内部席位包有效时间。
- 续约成功后不改变租户总席位，更新租户统一到期时间，生成线下订阅订单和席位续约权益记录。

##### 任务四：积分包撤销

- 仅已生效记录可撤销。
- 当前积分余额需大于等于本次发放积分。
- 撤销成功后扣减积分余额，生成支出流水，关闭关联积分订单。
- 当前积分余额不足时，不允许撤销。

##### 任务五：席位包分配撤销

- 仅已生效记录可撤销。
- 席位续约记录不进入发放撤销范围，不执行扣减席位。
- 撤销后租户总席位需大于等于已用席位。
- 如席位包包含赠送积分，当前积分余额需大于等于赠送积分。
- 撤销成功后扣减席位额度，扣回赠送积分，关闭关联订阅订单。

##### 任务六：撤销记录

- 撤销原因必填。
- 已撤销记录不可再次撤销。
- 发放、分配、续约和撤销均保留操作记录，不覆盖原订单和原流水。

#### 2.6.3 业务示例

##### 示例一：积分包可撤销

- 运营发放 10,000 积分，租户当前余额为 12,000。
- 运营填写撤销原因并确认。
- 系统扣回 10,000 积分，生成支出流水，发放记录变为已撤销。

##### 示例二：席位包不可撤销

- 运营发放 10 个席位，租户已有 9 个席位被成员占用。
- 撤销后总席位会小于已用席位。
- 系统阻断撤销，并提示席位已被占用。

##### 示例三：内部席位续约

- 租户当前有 20 个有效团队席位，最终到期时间为 2027-03-12。
- 运营选择「线下合同席位包 15 个月」执行席位续约。
- 系统把 20 个有效席位统一续约至 2028-06-12，不新增席位数量。

#### 2.6.4 流程与原型截图标记

- 图 GRANT-01 / 标记 A：租户详情中的发放记录列表。
- 图 GRANT-02 / 标记 A：内部积分包发放信息。
- 图 GRANT-03 / 标记 A：租户详情中的「分配席位」和「席位续约」入口。
- 图 GRANT-04 / 标记 A：内部席位分配中的到期规则和只读到期时间。
- 图 GRANT-05 / 标记 A：内部席位续约中的当前到期、有效时间和续约后到期。
- 图 GRANT-06 / 标记 A：发放记录撤销入口。
- 图 GRANT-07 / 标记 A：撤销原因填写。
- 图 GRANT-08 / 标记 A：不可撤销原因提示。
- 图 GRANT-09 / 标记 A：已撤销记录状态和撤销信息。

---

### 2.7 登录页协议确认

#### 2.7.1 目标

- 登录页展示用户协议和隐私协议确认入口。
- 用户同意两份协议后，才可继续登录或自动注册。
- 协议正文由独立协议文档接入，PRD 只定义协议确认规则和查看能力。

#### 2.7.2 任务拆解

##### 任务一：展示协议入口

- 登录页手机号输入区域下方展示「我已阅读并同意《用户协议》和《隐私协议》」勾选项。
- 《用户协议》和《隐私协议》均为可点击入口。
- 默认不勾选。

##### 任务二：协议查看

- 用户协议需通过独立浏览器标签页承载用户协议页面。
- 隐私协议需通过独立浏览器标签页承载隐私协议页面。
- 协议页面承载对应协议正文，原型阶段使用 mock 协议数据，正式上线前替换为法务确认后的正式协议正文。
- 协议查看页不承担勾选动作，用户回到登录页后需主动勾选协议确认项。

##### 任务三：登录拦截

- 用户未勾选同意两份协议时，不允许进入验证码下一步。
- 手机号有效时允许用户发起下一步；用户未勾选协议时，系统提示“请先阅读并同意用户协议和隐私协议。”。
- 用户未勾选同意两份协议时，不允许未注册手机号自动注册。
- 用户勾选协议确认项后，才可继续登录或自动注册。

#### 2.7.3 业务示例

##### 示例一：未勾选协议

- 用户输入手机号，但未勾选协议确认项。
- 用户发起下一步。
- 系统提示“请先阅读并同意用户协议和隐私协议。”，不进入验证码步骤。

##### 示例二：查看并同意协议

- 用户查看《用户协议》。
- 系统通过浏览器新标签页展示用户协议页面。
- 用户也可查看《隐私协议》，系统通过浏览器新标签页展示隐私协议页面。
- 用户阅读后回到登录页，手动勾选协议确认项。

#### 2.7.4 流程与原型截图标记

- 图 LOGIN-01 / 标记 A：登录页协议勾选项。
- 图 LOGIN-01 / 标记 B：《用户协议》查看入口。
- 图 LOGIN-01 / 标记 C：《隐私协议》查看入口。
- 图 LOGIN-02 / 标记 A：用户协议独立页面。
- 图 LOGIN-03 / 标记 A：隐私协议独立页面。
- 图 LOGIN-04 / 标记 A：未勾选协议时点击「下一步」后的提示状态。

---

## 3. 优化项

### 3.1 商品中心席位包有效时间与团队席位购买规则优化

#### 3.1.1 目标

- 商品中心席位包以可售或可发放 SKU 为单位维护，支持月付、年付以外的任意有效时间配置。
- 运营可配置内部包或公开售卖包，并维护启用状态、每席价格、赠送积分和有效时间。
- 购买、增加席位、续约时清楚展示席位包、金额、赠送积分和到期规则。
- 管理员能理解为什么增购不能切换席位包，以及金额如何折算。

#### 3.1.2 任务拆解

##### 任务一：商品中心席位包配置

- 商品中心团队席位包配置字段包括席位包名称、类型、启用状态、默认席位单位、每席价格、赠送积分、有效时间和更新时间。
- 类型包括「内部包」和「公开售卖」。
- 公开售卖包适用于企业管理后台团队扩充和续约。
- 内部包仅适用于运营后台租户详情的内部席位包发放，用户侧不可见。
- 有效时间使用数值和单位配置，单位支持天、月、年；半年配置为 6 个月，一年零三个月配置为 15 个月。
- 停用席位包后，不可新增购买、续约或内部发放，但不影响历史订单和已生效权益。

##### 任务二：首次购买

- 首次购买可选择商品中心启用的公开售卖席位包。
- 支付金额按所选席位包每席价格和购买席位数计算。
- 支付成功后生成新的统一到期日。

##### 任务三：增加席位

- 已有有效团队席位订阅时，增加席位只能沿用当前生效席位包快照。
- 增加席位金额按新增席位数和剩余服务天数折算。
- 增加席位后统一到期日不变。

##### 任务四：续约

- 续约按当前总席位统一续费。
- 续约可选择商品中心启用的公开售卖席位包。
- 未到期续约从当前统一到期日顺延。
- 已过期续约从支付成功日重新计算。

##### 任务五：运营内部分配与续约

- 运营后台租户详情提供「分配席位」和「席位续约」两个入口。
- 「分配席位」用于新增席位数量；租户已有有效团队席位时，新增席位到期时间对齐当前全部有效席位最终到期时间；租户没有有效团队席位时，到期时间按所选内部席位包有效时间从当前日期生成。
- 「席位续约」用于延长已有全部有效团队席位；续约席位数只读展示为当前有效团队席位数；续约后到期时间按当前最终到期时间叠加所选内部席位包有效时间计算。
- 运营侧不提供手工填写到期时间，避免同一租户出现多套席位到期口径。

##### 任务六：订单快照

- 购买、续约、增购、内部分配或内部续约成功后，订单保存席位包快照。
- 快照字段包括席位包名称、类型、每席价格、席位数量、赠送积分、有效时间、生效时间、原到期时间、新到期时间、支付金额、操作来源。
- 后续运营修改席位包价格、赠送积分、有效时间或状态，不反向影响历史订单和租户已获得权益。

#### 3.1.3 业务示例

##### 示例一：增加席位

- 当前团队有 10 个席位，使用「团队席位包 6 个月」，到期日为 2027-03-12。
- 管理员在 2026-12-12 增加 2 个席位。
- 新增 2 个席位沿用当前生效席位包快照，并按剩余服务天数折算，到期日仍为 2027-03-12。

##### 示例二：未到期续约

- 当前团队 10 个席位，到期日为 2027-03-12。
- 管理员选择「团队席位包 15 个月」续约。
- 新到期日从 2027-03-12 顺延 15 个月。

##### 示例三：内部包发放

- 运营在商品中心创建「线下合同席位包 15 个月」，类型为内部包，启用状态为启用。
- 运营在租户详情执行分配席位，选择该内部席位包，并填写分配席位数。
- 如果该租户当前已有有效团队席位，新增席位到期时间对齐当前最终到期时间；如果没有有效团队席位，系统按席位包有效时间生成新的到期时间。

##### 示例四：内部包续约

- 租户当前有 20 个有效团队席位，到期日为 2027-03-12。
- 运营在租户详情执行席位续约，选择「线下合同席位包 15 个月」。
- 系统按 20 个有效席位统一续约，新到期日为 2028-06-12。

#### 3.1.4 流程与原型截图标记

- 图 PAY-00 / 标记 A：商品中心团队席位包列表中的类型、启用状态、每席价格、赠送积分和有效时间。
- 图 PAY-00 / 标记 B：新建或编辑席位包信息中的名称、类型、启用状态、每席价格、赠送积分和有效时间。
- 图 PAY-01 / 标记 A：组织管理增加席位入口。
- 图 PAY-01 / 标记 B：组织管理续约入口。
- 图 PAY-02 / 标记 A：首次购买中的公开售卖席位包选择。
- 图 PAY-03 / 标记 A：增加席位中的当前生效席位包锁定规则提示。
- 图 PAY-03 / 标记 B：折算规则说明。
- 图 PAY-04 / 标记 A：续约中的续约席位和公开售卖席位包选择。
- 图 PAY-05 / 标记 A：支付确认页中的统一到期日。
- 运营后台内部分配和内部续约原型节点见 2.6.4 的 GRANT-03 至 GRANT-05。

---

### 3.2 组织成员与席位额度规则

#### 3.2.1 目标

- 组织管理明确展示最大有效席位、已占用席位、可添加成员和席位到期时间。
- 管理员能理解禁用不释放额度，移除账号才释放额度。
- 成员列表聚焦成员身份、账号状态、Leadeep 权限和管理操作，不展示席位类型和席位有效期。

#### 3.2.2 任务拆解

##### 任务一：席位类型

- 当前席位只有长期免费席位和付费席位。
- 长期免费席位只绑定租户归属人。
- 自注册用户默认为租户归属人。
- 运营创建租户时填写的初始管理员为租户归属人。
- 除租户归属人外，其他成员均使用付费席位。

##### 任务二：成员添加

- 添加后的成员数量必须小于等于最大有效席位数。
- 每添加 1 名成员，占用 1 个有效席位额度。
- 新添加成员账号状态默认为已启用。
- 超过额度时阻断添加。

##### 任务三：禁用与启用

- 禁用成员只改变账号状态，不释放席位额度。
- 已禁用账号不可继续登录和使用工作台。
- 启用成员不额外占用席位，继续使用原额度。

##### 任务四：移除成员

- 移除成员后，该账号从当前租户组织中移除。
- 移除账号释放 1 个有效席位额度。

#### 3.2.3 业务示例

##### 示例一：添加成员

- 最大有效席位为 40，已占用 39。
- 管理员添加 1 名成员。
- 添加成功后已占用席位变为 40，可添加成员数变为 0。

##### 示例二：禁用成员

- 最大有效席位为 40，已占用 40。
- 管理员禁用 1 名成员。
- 已占用席位仍为 40，可添加成员数仍为 0。

##### 示例三：移除成员

- 最大有效席位为 40，已占用 40。
- 管理员移除 1 名成员。
- 已占用席位变为 39，可添加成员数变为 1。

#### 3.2.4 流程与原型截图标记

- 图 ORG-01 / 标记 A：组织管理有效席位统计。
- 图 ORG-01 / 标记 B：成员搜索框。
- 图 ORG-02 / 标记 A：添加成员入口。
- 图 ORG-03 / 标记 A：成员列表中的账号状态。
- 图 ORG-03 / 标记 B：成员列表中的 Leadeep 权限。
- 图 ORG-04 / 标记 A：禁用成员二次确认。
- 图 ORG-05 / 标记 A：移除成员二次确认。
- 图 ORG-06 / 标记 A：席位额度不足提示。

---

### 3.3 模型缓存计量计费规则优化

#### 3.3.1 目标

- 大模型计量支持普通输入、缓存、输出三类 tokens。
- 按接口格式归一化 usage 后再计费，避免 cached tokens 重复按普通输入计费。
- 资源中心模型被停用或删除后，系统需阻断绑定该模型的 AI 专家继续对话，避免调用已不可用模型。

#### 3.3.2 任务拆解

##### 任务一：模型资源配置

- 模型清单支持运营新增和配置模型，不做删除入口。
- 模型配置包含模型名称、模型 ID、模型区域、模型值、接口格式、启用状态、普通输入成本、缓存成本、输出成本、成本倍率和排序。
- 模型区域只支持国内模型和国外模型。
- 接口格式为单选，只支持 OpenAI 和 Claude；不同模型按自身接口格式完成运行时调用适配。
- 计量单价由对应成本单价乘以成本倍率生成，不单独人工录入。
- 系统调用模型时需记录模型 ID 和接口格式；计费、对账和积分扣减按接口格式完成 usage 归一化后，统一读取该模型的三类成本配置。

##### 任务二：usage 归一化

- 模型调用返回的原始 usage 字段不得直接进入计费公式。
- OpenAI / Claude 接口适配器需先归一化为普通输入 tokens、缓存 tokens、输出 tokens。
- OpenAI 的 cached tokens、Claude 的 cache creation 和 cache read tokens 均归入缓存 tokens。
- 总输入 tokens 仅用于对账展示，等于普通输入 tokens 和缓存 tokens 之和。

##### 任务三：金额与积分计算

- 三类 tokens 分别乘以对应单价后汇总成本金额。
- 三类 tokens 分别乘以对应计量单价后汇总计量金额。
- 积分扣减基于计量金额、积分汇率、最小扣减和取整单位计算。

##### 任务四：异常处理

- usage 字段缺失、字段冲突、归一化结果不一致时，不进入正常扣费流程。
- 需记录异常 usage 原文和适配错误原因。

##### 任务五：模型状态对 AI 专家运行的影响

- AI 专家发布版本需记录绑定的资源中心模型标识，不仅保存模型名称。
- 当运营后台资源中心模型状态变为停用，或模型记录被删除时，绑定该模型的已发布 AI 专家不可发起新对话，也不可继续触发运行。
- 对话触发时系统需校验 AI 专家绑定模型是否存在且处于启用状态；校验失败时不进入模型调用、工具调度和计费流程。
- 校验失败时统一提示：“模型已下架，请联系开发者更新模型”。
- 已存在的历史对话、成果文件、用量记录和计费记录保持可查看，不因模型停用或删除而清理。
- 开发者更新 AI 专家绑定模型并重新发布新版本后，该 AI 专家恢复新对话能力。

#### 3.3.3 接口格式 usage 适配口径

- OpenAI Chat Completions / Responses：
  - `prompt_tokens` 或 `input_tokens` 为总输入。
  - `prompt_tokens_details.cached_tokens` 或 `input_tokens_details.cached_tokens` 为缓存 tokens。
  - 普通输入 tokens = 总输入 tokens - 缓存 tokens。
- Anthropic Claude Messages：
  - `input_tokens` 为普通输入 tokens。
  - `cache_creation_input_tokens` + `cache_read_input_tokens` 为缓存 tokens。
  - 不得用 `input_tokens` 再扣减 Cache 字段。
- DeepSeek Chat Completions：
  - `prompt_cache_miss_tokens` 为普通输入 tokens。
  - `prompt_cache_hit_tokens` 为缓存 tokens。
- 阿里云百炼 DashScope OpenAI 兼容接口：
  - Chat Completions 未返回缓存字段时，`prompt_tokens` 计入普通输入 tokens。
  - Responses 返回 `input_tokens_details.cached_tokens` 时，按 OpenAI 口径处理。
- 本地部署或无缓存字段接口：
  - 输入 tokens 全部计入普通输入 tokens。
  - 缓存 tokens 为 0。

#### 3.3.4 计算公式

- 总输入 tokens = 普通输入 tokens + 缓存 tokens。
- 当接口返回的总输入 tokens 已包含缓存 tokens 时，普通输入 tokens = 总输入 tokens - 缓存 tokens。
- 本次计量金额 = （普通输入 tokens × 普通输入单价 + 缓存 tokens × 缓存单价 + 输出 tokens × 输出单价）÷ 1,000,000。
- 本次成本金额使用同样公式，但使用三类成本单价。
- 扣减积分 = 本次计量金额 × 积分汇率，再按取整单位和最小扣减积分处理。

#### 3.3.5 业务示例

##### 示例一：OpenAI

- OpenAI 返回总输入 100,000，cached 40,000，输出 20,000。
- 平台归一化为普通输入 60,000，缓存 40,000，输出 20,000。
- 缓存 tokens 按缓存单价计费，不再按普通输入单价重复计费。

##### 示例二：Anthropic

- Anthropic 返回 input 30,000，cache creation 10,000，cache read 50,000，output 15,000。
- 平台归一化为普通输入 30,000，缓存 60,000，输出 15,000。
- 不再用 input 30,000 扣减 Cache 字段。

##### 示例三：绑定模型被停用

- 运营人员在资源中心停用某大模型。
- 已发布且绑定该模型的 AI 专家收到对话请求。
- 系统在发起运行前完成模型状态校验，阻断本次对话，并提示“模型已下架，请联系开发者更新模型”。
- 开发者更新 AI 专家绑定模型并重新发布后，新版本恢复对话。

#### 3.3.6 调研依据

- OpenAI：[官方 Prompt Caching 文档](https://platform.openai.com/docs/guides/prompt-caching)说明 `cached_tokens` 位于 `prompt_tokens_details`，且 `prompt_tokens` 包含缓存 tokens。
- Anthropic：[官方 Prompt Caching 文档](https://docs.anthropic.com/en/docs/build-with-claude/prompt-caching)说明 usage 返回 `input_tokens`、`cache_creation_input_tokens`、`cache_read_input_tokens` 和 `output_tokens`。
- DeepSeek：[官方 KV Cache 文档](https://api-docs.deepseek.com/guides/kv_cache)说明 usage 返回 `prompt_cache_hit_tokens` 和 `prompt_cache_miss_tokens`。
- 阿里云百炼：[官方 DashScope OpenAI 兼容接口文档](https://help.aliyun.com/document_detail/2713153.html/)示例返回 `prompt_tokens`、`completion_tokens`、`total_tokens`，Responses usage 可返回 `input_tokens_details.cached_tokens`。

#### 3.3.7 流程与原型截图标记

- 图 MTR-01 / 标记 A：新增模型和配置模型表单。
- 图 MTR-01 / 标记 B：普通输入成本、缓存成本、输出成本字段。
- 图 MTR-02 / 标记 A：三类计量价预览。
- 图 MTR-03 / 标记 A：消耗对账列表中的输入 tokens 拆分。
- 图 MTR-03 / 标记 B：输出 tokens、计量金额、成本金额和扣减积分。
- 图 MTR-04 / 标记 A：usage 适配异常记录。

---

### 3.4 企业驾驶舱图表时间规则优化

#### 3.4.1 目标

- 驾驶舱「今日 / 本周 / 本月」筛选规则统一。
- 趋势图、排行图、分布图和明细入口使用同一统计时间范围。
- 修正本周、本月图表横轴时间不准确的问题。

#### 3.4.2 任务拆解

##### 任务一：统一时间范围

- 时间计算以租户业务时区为准，默认使用北京时间。
- 今日：当前自然日 00:00 至当前时刻。
- 本周：当前自然周周一 00:00 至当前时刻。
- 本月：当前自然月 1 日 00:00 至当前时刻。
- 后端查询使用左闭右开区间，避免边界时间重复统计。

##### 任务二：趋势图横轴规则

- 企业积分/成本消耗趋势按当前筛选范围生成连续时间点。
- 趋势值展示当前时间桶内的实际消耗，不展示累计消耗。
- 今日按小时聚合，横轴从 00:00 展示到当前小时。
- 本周按日聚合，横轴从本周周一展示到当天。
- 本月按日聚合，横轴从本月 1 日展示到当天。
- 无数据的时间点补 0 或显示为空点，但不得直接丢失横轴日期。
- 趋势图不得只取周期起点和当前日期两个点。

##### 任务三：排行和分布规则

- 驾驶舱顶部用量卡片中的积分或成本字段统计当前时间筛选范围内的实际消耗，积分计费租户命名为「积分消耗」，成本计费租户命名为「成本消耗」。
- 积分余额是租户账户当前剩余积分，不受驾驶舱时间筛选影响，不得与当前筛选范围内的积分消耗混用。
- 成员用量排行按当前筛选范围内的用量汇总排序。
- AI 专家用量排行按当前筛选范围内的调用用量汇总排序。
- AI 专家开发分布按当前筛选范围内的 AI 专家创建时间统计开发者分布。
- 排行和分布的详情入口必须带上当前时间筛选参数。

##### 任务四：统计时间字段

- 用量类图表以调用完成并完成计量入账的时间为准。
- 开发类图表以 AI 专家创建成功时间为准。
- 明细页与驾驶舱图表必须使用同一时间字段和同一筛选范围。

#### 3.4.3 业务示例

##### 示例一：本周

- 当前日期为 2026-06-08，星期一。
- 用户选择「本周」。
- 统计范围从 2026-06-08 00:00 开始，不应出现 2026-06-01 的数据点。

##### 示例二：本月

- 当前日期为 2026-06-08。
- 用户选择「本月」。
- 趋势图横轴应按日展示 6/1 至 6/8 的连续日期，不应只展示 6/1 和 6/8 两个端点。

#### 3.4.4 流程与原型截图标记

- 图 DASH-01 / 标记 A：驾驶舱时间筛选「今日 / 本周 / 本月」。
- 图 DASH-02 / 标记 A：Agent 用量趋势横轴日期。
- 图 DASH-03 / 标记 A：成员用量排行。
- 图 DASH-04 / 标记 A：AI 专家用量排行。
- 图 DASH-05 / 标记 A：AI 专家开发分布。
- 图 DASH-06 / 标记 A：用量明细或开发明细继承当前时间筛选。
