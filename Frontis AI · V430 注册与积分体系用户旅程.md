# Frontis AI · V430 注册与积分体系用户旅程

## 1. 文档说明

### 1.1 文档目标

本文档用于沉淀 `430` 版本中“注册、租户版本、席位与积分体系”的用户旅程设计，确保关键设计都能在用户旅程中闭环。

### 1.2 文档范围

本文档覆盖以下内容：

1. 个人注册与个人版初始化
2. 个人版升级团队版
3. 团队版席位扩容
4. 团队版成员邀请
5. AI专家广场中 FrontisAI 发布 AI 专家的试用与购买
6. 租户积分购买、使用与消耗统计
7. 平台侧的注册策略、团队套餐、积分包、AI 专家商品与资源池治理

---

## 2. 核心业务模型

### 2.1 统一租户模型

430 当前版本采用统一租户模型：

1. 用户通过手机号 + 验证码完成注册。
2. 注册后系统自动创建一个新的租户。
3. 当前注册用户默认成为该租户管理员。
4. 该租户初始版本为 `个人版`。

这意味着：

- 个人注册本质上不是单独的“个人账号模式”，而是“自助创建一个个人版租户”。
- 后续从个人版升级到团队版时，租户本身不变化，只变化版本能力。

### 2.2 租户版本模型

430 当前版本只有两种租户版本：

1. `个人版`
2. `团队版`

其中：

**个人版**

- 默认 1 个管理员席位
- 不开放组织管理
- 不开放邀请成员
- 支持积分购买和日常使用

**团队版**

- 通过年付套餐开通
- 套餐本身包含固定席位数
- 开通后解锁组织管理和邀请成员能力
- 席位不足后可按单席位年付价格扩容
- 兼容个人版所有能力，并在此基础上增加组织、成员、席位和多人消耗统计维度

### 2.3 积分模型

430 当前版本积分模型如下：

1. 一个租户只有一个统一积分余额。
2. 租户内所有成员共用这一份余额。
3. 不区分充值积分和赠送积分余额类型。
4. 积分只用于运行时消耗：
   - 大模型调用
   - 第三方接口调用
   - 集成到 Agent / Skill 中的外部工具调用
5. 积分包通过现金支付购买，支付成功后到账到当前租户积分余额。

### 2.4 积分消耗管理模型

积分消耗管理不以逐条调用流水作为默认视图，而以时间范围内的使用人聚合为核心。

默认支持三个时间范围：

- 本周
- 本月
- 全部

其中：

**个人版**

- 只有当前管理员本人一个使用人
- 积分消耗页展示本人消耗汇总
- 重点帮助用户理解自己在这段时间内消耗了多少、主要消耗在哪些能力上

**团队版**

- 同一租户下可能存在多个成员
- 积分消耗页按使用人聚合展示成员消耗排行
- 重点帮助管理员判断谁消耗最多、是否存在异常使用、是否需要补充积分或治理使用方式

逐条调用记录只作为使用人详情里的追溯信息，不作为积分消耗页的第一层展示。

### 2.5 现金与积分边界

430 当前版本严格遵循：

- 现金买版本、席位、积分包和资产资格
- 积分买运行消耗

其中：

**现金购买**

- 团队版套餐
- 单席位扩容
- 积分包
- AI 专家
- 设备

**积分消耗**

- 模型调用
- API 调用
- Agent / Skill 运行时外部工具调用

---

## 3. 用户旅程

### 3.1 旅程一：新用户注册并进入个人版

#### 用户目标

我想自己注册一个账号，进来就能开始使用 Frontis AI。

#### 旅程步骤

1. 用户进入注册页。
2. 输入手机号 + 验证码完成注册。
3. 系统自动创建：
   - 账号
   - 新租户
   - 默认管理员身份
   - `个人版`
   - 初始积分余额
4. 系统向该租户发放注册赠送积分。
5. 用户首次进入系统。
6. 首次进入引导至 `AI专家广场`。
7. 用户挑选 AI 专家体验，或返回 `MetaAgent` 发起第一项任务。
8. 系统明确提示：
   - 当前使用的是租户积分
   - 当前租户版本是个人版
   - 如需邀请成员，需要先开通团队版

#### 闭环

`注册 -> 创建租户 -> 默认进入个人版 -> 获得积分 -> 完成首次使用 -> 理解当前是单人版本`

---

### 3.2 旅程二：个人版管理员开通团队版

#### 用户目标

我已经在个人版里验证了价值，现在希望把团队拉进来一起用。

#### 旅程步骤

1. 个人版管理员进入 `管理后台`。
2. 在 `租户总览` 看到当前版本是个人版，并看到 `开通团队版` 入口。
3. 点击 `开通团队版`。
4. 系统打开团队版购买弹窗。
5. 管理员选择一个年付团队套餐，例如：
   - 团队 5 席版
   - 团队 20 席版
6. 系统展示订单摘要：
   - 当前版本
   - 套餐名称
   - 套餐包含席位数
   - 订阅周期：按年
   - 支付金额
7. 管理员扫码支付。
8. 支付成功后，系统自动：
   - 将租户从个人版升级为团队版
   - 写入团队套餐与有效期
   - 更新总席位数
9. 管理员返回后台后，看到 `组织管理` 功能已解锁。

#### 闭环

`个人版使用 -> 进入管理后台 -> 选择团队套餐 -> 扫码支付 -> 升级团队版 -> 解锁组织管理`

---

### 3.3 旅程三：团队版管理员扩容席位

#### 用户目标

我已经是团队版，但当前套餐席位不够了，希望继续扩容。

#### 旅程步骤

1. 团队版管理员进入 `组织管理`。
2. 查看当前已用席位 / 总席位。
3. 当席位不足时，点击 `扩容席位`。
4. 系统打开席位扩容弹窗。
5. 管理员选择需要新增的席位数量。
6. 系统根据单席位年付价格计算订单金额，并展示：
   - 当前总席位
   - 新增席位数
   - 扩容后总席位
   - 单席位价格
   - 支付金额
7. 管理员扫码支付。
8. 支付成功后，总席位数即时增加。
9. 管理员继续邀请成员加入。

#### 闭环

`席位不足 -> 发起扩容 -> 扫码支付 -> 席位增加 -> 继续邀请成员`

---

### 3.4 旅程四：团队版管理员邀请成员

#### 用户目标

我已经开通了团队版，现在希望把其他人拉进来一起使用。

#### 旅程步骤

1. 团队版管理员进入 `组织管理`。
2. 查看当前是否还有剩余席位。
3. 点击 `邀请成员`。
4. 录入成员手机号、姓名、所属部门和角色。
5. 系统发送邀请并创建成员入口。
6. 新成员加入租户后，在同一租户中开始使用 Frontis AI。
7. 新成员与管理员共用同一份租户积分。

#### 异常分支

1. 如果当前席位不足，系统阻止继续邀请。
2. 引导管理员先扩容席位。

#### 闭环

`团队版已开通 -> 检查剩余席位 -> 邀请成员 -> 成员加入 -> 共用租户积分`

---

### 3.5 旅程五：租户管理员购买积分包并完成到账

#### 用户目标

当前租户积分不足或预期即将不足，我希望直接购买积分，支付成功后立即到账。

#### 旅程步骤

1. 租户管理员在工作台头像浮窗或管理后台 `积分管理` 页看到当前积分余额。
2. 当余额不足或临近阈值时，管理员点击 `购买积分`。
3. 系统打开积分购买弹窗，默认展示三档积分包：
   - 基础积分包 `5,000 积分 / ¥29`
   - 标准积分包 `20,000 积分 / ¥99`
   - 增长积分包 `60,000 积分 / ¥249`
4. 管理员选择一个积分包后，系统展示：
   - 当前积分余额
   - 到账积分
   - 支付金额
   - 到账后余额
   - 当前租户
5. 管理员进入统一扫码支付页。
6. 用户扫码支付后，系统自动完成支付成功处理。
7. 支付成功后，当前租户积分余额即时增加。

#### 闭环

`余额不足 -> 选择积分包 -> 扫码支付 -> 自动到账 -> 租户继续使用`

---

### 3.6 旅程六：租户管理员查看积分消耗统计

#### 用户目标

我想知道这段时间积分主要被谁消耗了，是否需要补充积分或治理使用。

#### 旅程步骤

1. 租户管理员进入 `管理后台`。
2. 打开 `积分管理`。
3. 进入 `积分消耗`。
4. 选择时间范围：
   - 本周
   - 本月
   - 全部
5. 系统展示当前时间范围内的消耗摘要：
   - 消耗积分
   - 使用人数
   - 使用次数
   - 人均消耗
6. 如果当前是个人版，系统只展示当前管理员本人一行，包含：
   - 消耗积分
   - 使用次数
   - 主要消耗来源
   - 最近使用时间
7. 如果当前是团队版，系统按使用人展示成员消耗排行，包含：
   - 使用人
   - 部门 / 角色
   - 消耗积分
   - 占比
   - 使用次数
   - 主要消耗来源
   - 最近使用时间
8. 管理员点击某个使用人后，查看该使用人的消耗构成和少量最近明细。
9. 如果发现积分余额不足或消耗异常，管理员可进一步购买积分或调整团队使用方式。

#### 闭环

`选择时间范围 -> 查看使用人聚合消耗 -> 定位高消耗来源 -> 购买积分或治理使用`

---

### 3.7 旅程七：用户获取 FrontisAI 发布 AI 专家

#### 用户目标

我希望在 AI 专家广场里直接获得平台发布的 AI 专家，有的直接领取，有的先试用，有的按月、季度、年订阅购买。

#### 旅程步骤

1. 用户进入 `AI专家广场`。
2. 在分类中切换查看：
   - `团队分享`
   - `FrontisAI发布`
3. 浏览 `FrontisAI发布` AI 专家时，系统根据商品规则展示三种动作：
   - 免费领取
   - 免费试用
   - 立即购买
4. 如果是免费商品，用户直接领取并开通当前租户。
5. 如果是试用商品，用户可先开通试用，系统写入试用有效期。
6. 如果是付费商品，用户打开购买弹窗。
7. 系统展示商品摘要、包月 / 包季 / 包年订阅方案、试用规则和订单摘要。
8. 用户选择一个订阅方案后扫码支付。
9. 支付成功后，系统自动：
   - 为当前租户开通商品
   - 写入订单与开通记录
10. 返回 AI 专家广场后，该 AI 专家可直接添加到工作台。

#### 闭环

`进入广场 -> 选择 FrontisAI 发布商品 -> 免费领取 / 试用 / 购买 -> 开通成功 -> 添加到工作台`

---

### 3.8 旅程八：租户成员日常使用并消耗积分

#### 用户目标

我是租户成员，我想直接开始用，并知道这次扣的是谁的积分。

#### 旅程步骤

1. 成员进入某个租户。
2. 在 `MetaAgent`、`专家工作室` 或 AI 专家中发起任务。
3. 系统明确提示：
   - 当前使用的是该租户积分
4. 系统执行模型/API/第三方接口调用。
5. 任务完成后，从租户积分余额中扣减。
6. 用户可看到：
   - 本次消耗
   - 当前剩余余额

#### 异常分支：积分不足

1. 成员发起任务。
2. 系统校验发现租户积分不足。
3. 若当前用户不是管理员，则提示联系管理员购买积分。
4. 若当前用户是管理员，则直接提供购买积分入口。

#### 闭环

`成员使用 -> 租户积分扣减 -> 积分不足 -> 找管理员购买积分 -> 成员继续使用`

---

### 3.9 旅程九：平台侧配置注册与商业化规则

#### 用户目标

我是平台运营，我要控制新注册租户的默认版本，以及团队套餐、席位价格和积分包的售卖方式。

#### 旅程步骤

1. 平台运营进入平台侧。
2. 配置：
   - 是否开放个人注册
   - 默认注册版本：个人版
   - 默认赠送积分
3. 进入 `团队套餐设置`，配置：
   - 套餐名称
   - 包含席位数
   - 年付价格
   - 推荐标记
   - 单席位年付价格
4. 进入 `积分包设置`，配置：
   - 积分数量
   - 售价
   - 推荐标记
   - 上下架状态
5. 进入 `商品中心`、`订单与开通`、`资源池`，配置：
   - AI 专家商品包月 / 包季 / 包年价格
   - 试用规则
   - 订单与开通记录
   - 资源池供给能力
6. 用户侧和管理后台读取这些配置，生成真实购买旅程和开通结果。

#### 闭环

`平台配置默认版本 -> 平台配置团队套餐/积分包/AI专家商品 -> 用户注册 -> 用户升级团队版、购买积分或购买 AI 专家 -> 平台查看订单与开通`

---

## 4. 关键产品结论

1. 430 的注册，不再直接给所有新租户多人协作能力。
2. 新注册租户必须先从个人版开始。
3. 协作能力通过团队版购买来解锁。
4. 团队版不是权限勾选，而是带席位和有效期的年付套餐。
5. 席位扩容不是后台配置，而是一个真实的支付购买旅程。
6. AI 专家广场要区分 `团队分享` 和 `FrontisAI发布`。
7. FrontisAI 发布 AI 专家要支持免费、试用和付费购买三种获取方式。
8. 付费 AI 专家要支持包月、包季、包年三种订阅方案。
9. 租户积分始终只用于运行消耗，不承接团队版、席位或 AI 专家购买。
10. `MetaAgent` 依然是默认工作入口，团队能力和积分能力只是围绕租户模型叠加。

一句话概括：

**430 要把 Frontis AI 做成“个人先上手、团队再升级、AI 专家可商品化获取、积分只管消耗”的产品闭环。**
