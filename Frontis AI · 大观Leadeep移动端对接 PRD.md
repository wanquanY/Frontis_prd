# Frontis AI · 大观 Leadeep 打通 PRD

## 1. 版本范围

### 1.1 本期需求

- 企业管理后台支持为正常租户成员开放 Leadeep 移动端权限，普通员工获得权限后即可使用移动端。
- Web 左下角用户卡片提供移动端下载入口，hover 展示下载二维码，用户手机扫码下载 Leadeep App。
- 大观 `User / Tenant / Member` 与 Leadeep App 账号完成账户体系打通。
- 大观 Web 多 session 中生成或绑定一条 Leadeep 问策固定会话。
- Leadeep 问策与 Web 绑定会话的历史记录双向同步，历史记录指用户在页面上看到的聊天消息、引用文件、输出产物和消息状态。
- 建立统一记忆机制，让 Web、Leadeep 和后续飞书在同一套 Agent 记忆、文件线索和配置机制下工作。
- 对“先使用过 Leadeep、后开通大观”的交叉用户，支持将其 Leadeep 历史会话、文件型知识和产物资产迁移到大观成员身份下。
- 飞书作为后续绑定入口预留口径：与 Leadeep 使用同一套账户体系、会话和统一记忆机制，唯一差异是历史记录只从飞书单向同步到 Web，Web 续聊不回写飞书。

### 1.2 不做范围

- 不重做一个独立的大观移动端 App。
- 不把 Leadeep C 端完整产品形态搬进大观。
- 不让 Leadeep App 创建、切换或管理大观多 session。
- 不让飞书创建、切换或管理大观多 session。
- 不在 V1 做多个 Leadeep topic。
- 不在 V1 做 Web 输入框选择知识库/文件能力。
- 不在本轮设计企业级知识治理；普通成员表达的企业规则不直接全员生效。
- 不做全量 Leadeep 老用户自动迁移，只处理已完成大观账户体系打通的交叉用户。

### 1.3 核心产品口径

- Web 是大观 ME 多会话主入口，负责创建、切换和管理 session。
- Leadeep 问策只是移动端绑定入口，只进入指定 Web session，不创建新 session。
- 飞书也是绑定入口，只进入指定 Web session，不创建新 session。
- Leadeep 与飞书的唯一产品差异是历史记录同步方向：
  - Leadeep：Web 与 Leadeep 双向同步历史记录。
  - 飞书：飞书历史记录单向同步到 Web，Web 在该会话下续聊不回写飞书。
- 长期个人记忆、任务记忆、文件线索、Agent 配置文件属于统一记忆机制，不为飞书或 Leadeep 分叉一套规则。

---

## 2. 新增需求

### 2.1 企业管理后台 Leadeep 权限开放

#### 2.1.1 目标

- 企业管理员可以为当前租户内状态正常的成员开放 Leadeep 移动端权限。
- 普通员工被开放移动端权限后，可在 Leadeep App 内进入大观绑定会话。
- 管理员可查看开通状态、同步状态和异常原因。

#### 2.1.2 任务拆解

##### 任务一：权限开放入口

- 在企业管理后台增加 Leadeep 权限开放入口。
- 页面展示当前租户成员列表、成员状态、手机号、开通状态、最近同步状态。
- 支持为单个正常成员开放移动端权限、关闭权限和重新同步。

##### 任务二：成员状态校验

- 只允许为当前租户内状态正常的成员开放移动端权限。
- 成员无手机号、已停用、已移除或非当前租户成员时阻断开通。
- 同一成员已开通时不重复创建绑定关系。

##### 任务三：权限结果沉淀

- 权限开放成功后生成成员与 Leadeep 能力的绑定记录。
- 权限开放记录需保存租户、成员、绑定会话、开放时间、操作人、当前状态。
- 权限开放失败需记录失败原因，并允许管理员重试。

##### 任务四：Web 用户卡片下载入口

- Web 左下角用户卡片增加移动端下载按钮。
- 用户 hover 移动端下载按钮时，展示 Leadeep App 下载二维码。
- 用户使用手机扫码后进入 Leadeep App 下载页或唤起安装引导。
- 只有已开放移动端权限的成员，在 Web 左下角用户卡片中展示移动端下载按钮。

##### 任务五：管理端移动端引导

- 开通页提供 Leadeep App 下载或打开引导，方便管理员通知员工。
- 对未开放移动端权限的成员，App 不展示大观入口或提示联系管理员。

#### 2.1.3 业务示例

##### 示例一：管理员为普通员工开放移动端权限

- 企业管理员进入 Leadeep 权限开放页。
- 选择状态正常的普通员工并点击开放权限。
- 系统校验成员状态和手机号后生成绑定会话。
- 该员工登录 Leadeep App 后可以进入大观问策。

##### 示例二：员工从 Web 用户卡片下载移动端

- 员工在大观 Web 左下角打开用户卡片。
- 用户卡片中展示移动端下载按钮。
- 员工 hover 按钮后看到下载二维码。
- 员工用手机扫码下载 Leadeep App，并登录进入已开放权限的租户身份。

##### 示例三：成员被停用

- 成员在企业管理后台被停用。
- 系统同步停用该成员 Leadeep 写入权限。
- 成员在 App 继续发送消息时被阻断，并提示当前租户身份不可用。

#### 2.1.4 规则清单

- 移动端权限归属于租户成员身份，不归属于纯 Leadeep C 端账号。
- 当前租户内状态正常的普通成员都可以被开放移动端权限，不限管理员角色。
- 同一自然人在多个租户内均开通时，App 必须让用户选择租户身份。
- 停用成员不得继续写入该租户下的绑定会话。
- Web 左下角用户卡片的移动端下载入口依赖成员移动端权限；二维码只负责下载或唤起 App。
- 本期不设计计费联动，不默认占用额外席位或积分；商业规则后续由订阅与权益模块单独定义。

#### 2.1.5 流程与原型截图标记

- 图 LDP-ADMIN-01 / 标记 A：Leadeep 权限开放入口。
- 图 LDP-ADMIN-01 / 标记 B：成员 Leadeep 权限状态。
- 图 LDP-ADMIN-02 / 标记 A：权限开放失败原因与重试。
- 图 LDP-ADMIN-03 / 标记 A：App 下载与使用引导。
- 图 LDP-WEB-QR-01 / 标记 A：Web 左下角用户卡片移动端下载按钮。
- 图 LDP-WEB-QR-01 / 标记 B：hover 展示下载二维码。

---

### 2.2 大观与 Leadeep 账户体系打通

#### 2.2.1 目标

- Leadeep App 登录后能识别用户对应的大观租户成员身份。
- 大观权限、会话、文件和记忆均按 `User + Tenant + Member` 生效。
- 明确 Leadeep C 端账号如何接入大观 B 端租户账户体系，处理账号冲突、多租户和未开放移动端权限场景。

#### 2.2.2 任务拆解

##### 任务一：账号识别与用户匹配

- Leadeep App 登录后读取手机号或统一账号标识。
- 大观根据账号标识查找 `User`。
- 再根据 `User` 查找可用的租户成员身份。

##### 任务二：租户成员身份判断

- 若用户只有一个已开放移动端权限的成员身份，直接进入对应大观问策。
- 若用户有多个已开放移动端权限的成员身份，要求用户选择租户。
- 若用户没有已开放移动端权限的成员身份，不展示大观问策入口。

##### 任务三：冲突处理

- Leadeep 账号手机号与大观账号不一致时，不自动合并。
- 冲突账号进入异常状态，由用户或运营按后续规则处理。

#### 2.2.3 业务示例

##### 示例一：单租户成员登录

- 用户用手机号登录 Leadeep App。
- 系统找到该手机号在大观某租户下已开放 Leadeep 移动端权限。
- App 直接进入该租户的大观问策绑定会话。

##### 示例二：多租户成员登录

- 同一手机号属于两个大观租户，且两个租户均已开放 Leadeep 移动端权限。
- App 展示租户选择。
- 用户选择后进入对应租户的绑定会话。

#### 2.2.4 规则清单

- 绑定关系以大观成员身份为准，不以 Leadeep C 端个人账号为准。
- Leadeep 账号接入大观后，使用的是大观租户成员身份下的权限、会话、历史记录和记忆机制。
- App 不得默认进入最近租户，必须保证用户明确当前身份。
- 成员被移除后，App 不展示该租户入口。
- 历史数据归属策略按租户数据权限另行确认。

#### 2.2.5 流程与原型截图标记

- 图 LDP-ID-01 / 标记 A：App 登录后的身份识别。
- 图 LDP-ID-02 / 标记 A：多租户选择。
- 图 LDP-ID-03 / 标记 A：未开放权限提示。
- 图 LDP-ID-04 / 标记 A：账号冲突提示。

---

### 2.3 Web 多 session 与绑定会话

#### 2.3.1 目标

- Leadeep 问策绑定到大观 Web 多 session 中的一条固定会话，并与 Web 绑定会话历史记录双向同步。
- 后续飞书入口也绑定到 Web 指定会话，但飞书历史记录只从飞书单向同步到 Web，Web 续聊不回写飞书。
- Web 仍是多 session 主入口；Leadeep App 和飞书都不承担 session 创建和管理。
- 用户在 Web 会话列表中能识别普通 Web 会话、Leadeep 绑定会话和飞书绑定会话。
- 历史记录只指用户在页面上看到的聊天消息、引用文件、输出产物和消息状态，不包含 Agent 背后的长期记忆机制。

#### 2.3.2 任务拆解

##### 任务一：Leadeep 绑定会话创建

- 成员被开放 Leadeep 移动端权限时，系统创建或复用一条 `leadeep_mobile` 类型 session。
- 绑定会话归属 `User + Tenant + Member`。
- 同一成员在同一租户下 V1 仅有一条 Leadeep 绑定会话。

##### 任务二：飞书绑定会话预留

- 飞书入口绑定到 Web 多 session 中的一条指定会话。
- 飞书不创建大观新 session。
- 飞书不管理大观多 session 列表。

##### 任务三：Web 会话列表展示与来源标签

- Web 多 session 列表展示 Leadeep 问策绑定会话。
- 后续飞书接入后，Web 多 session 列表展示飞书绑定会话。
- 普通 Web 会话不展示额外来源标签。
- Leadeep 绑定会话只展示 `Leadeep` 渠道标签。
- 飞书绑定会话只展示 `飞书` 渠道标签。
- 会话列表不展示“单向 / 双向”等同步方向文案，避免把产品机制说明暴露成列表噪音。
- 用户点击 Leadeep 或飞书绑定会话后，进入正常聊天记录页；消息展示、输入、继续对话和普通 Web 会话保持一致。

##### 任务四：Leadeep 历史记录双向同步

- App 发送消息时写入大观 Leadeep 绑定 session。
- Web 打开 Leadeep 绑定会话时读取同一消息流。
- 用户在 Web 的 Leadeep 绑定会话中继续提问时，新增消息写入同一 session，Leadeep App 拉取后可见。
- 消息需包含来源端、发送人、消息类型、创建时间和同步状态。
- 消息排序以服务端时间和消息序号为准。

##### 任务五：飞书历史记录单向同步

- 飞书历史记录同步到 Web 绑定会话。
- Web 在该飞书绑定会话中新增的对话记录不回写飞书。
- 系统内部需记录消息来源，用于同步判断和审计；Web 聊天页不额外区分飞书同步记录和 Web 本端续聊记录，保持普通聊天记录体验。

##### 任务六：会话边界

- Leadeep 绑定会话原文只属于该 session。
- 飞书绑定会话原文只属于该 session。
- 其他 Web 普通会话不读取 Leadeep 绑定会话原文。
- 其他 Web 普通会话不读取飞书绑定会话原文。
- 跨会话可复用的是已沉淀的长期个人记忆、任务记忆、文件线索和 Agent 配置文件。

#### 2.3.3 业务示例

##### 示例一：产品经理按版本写 PRD

- 用户在 Web 为 V1.0、V1.1 分别创建普通 session。
- 每个版本 session 的原文、临时文件和阶段上下文互相隔离。
- 版本目标、已采纳结论和 PRD 文件线索可沉淀为长期可召回资产。

##### 示例二：出差时用 Leadeep 追问

- 用户打开 Leadeep 问策询问 V1.1 PRD 内容。
- Leadeep 进入固定绑定会话。
- ME 根据长期记忆和文件线索定位此前 Web 产出的 PRD 文件并回答。

##### 示例三：移动端提问，Web 回看

- 用户在 Leadeep 问策询问“V1.1 权限边界是什么”。
- ME 返回答案并引用相关 PRD 文件。
- 用户回到电脑打开 Web 绑定会话，看到同一条提问、回复和引用文件。

##### 示例四：Web 续聊，移动端继续

- 用户在 Web 绑定会话继续追问两轮。
- 再次打开 Leadeep 问策。
- App 展示 Web 新增的聊天记录。

##### 示例五：飞书记录进入 Web

- 用户在飞书里向 ME 提问。
- 该飞书对话记录同步到 Web Session F。
- 用户打开 Web 后能看到飞书同步过来的记录。

##### 示例六：Web 续聊不回飞书

- 用户在 Web Session F 中继续追问。
- 新增对话只留在 Web 会话内。
- 飞书端不展示这部分 Web 续聊记录。

#### 2.3.4 规则清单

- Web 是唯一的大观多 session 创建入口。
- Leadeep 不允许创建大观新 session。
- Leadeep 不允许切换大观普通 session。
- 飞书不允许创建大观新 session。
- 飞书不允许切换大观普通 session。
- Leadeep 绑定会话可以使用同一套长期个人记忆、文件线索和 Agent 配置文件。
- 飞书绑定会话可以使用同一套长期个人记忆、文件线索和 Agent 配置文件。
- Leadeep 与 Web 的历史记录是双向同步。
- 飞书历史记录只从飞书单向同步到 Web。
- 历史记录同步不等于长期记忆同步；长期记忆的沉淀、召回和纠错规则见统一记忆机制章节。
- 同步对象是绑定 session 内页面可见的消息、引用文件、输出产物和消息状态。
- 消息去重以统一消息 ID 为准，不以端侧本地 ID 为准。
- 绑定会话可在 Web 展示，但 V1 不建议允许用户删除。

#### 2.3.5 流程与原型截图标记

- 图 LDP-SES-01 / 标记 A：Web 会话列表中的 Leadeep 绑定会话。
- 图 LDP-SES-01 / 标记 B：绑定会话来源标识。
- 图 LDP-SES-02 / 标记 A：App 进入固定绑定会话。
- 图 LDP-SES-03 / 标记 A：Web 展示 App 消息。
- 图 LDP-SES-04 / 标记 A：Web 续聊后 App 展示新增消息。
- 图 LDP-SES-05 / 标记 A：飞书绑定会话。
- 图 LDP-SES-06 / 标记 A：飞书记录入 Web。
- 图 LDP-SES-07 / 标记 A：Web 续聊不回写飞书。

---

### 2.4 统一记忆机制

#### 2.4.1 目标

- 建立一套面向 ME / Agent 的统一记忆机制，让 Web、Leadeep 和后续飞书入口使用同一套记忆沉淀、召回、纠错和配置生效规则。
- 记忆机制是 Agent 背后的工作机制，不等同于页面历史记录同步。
- 多 session 之间区分“可跨会话共享的记忆资产”和“必须隔离的会话原文 / 当前任务短期上下文”。

#### 2.4.2 任务拆解

##### 任务一：统一记忆文件定义

参考 Hermes 的记忆机制，本期大观 Leadeep 不把“记忆”设计成一团模糊上下文，而是拆成一组职责清晰的配置文件与记忆文件。文件名是产品口径，实际工程可落到结构化表、对象存储或配置中心，但职责边界必须保持一致。

统一记忆文件按“用户 + 租户成员 + Agent”隔离。也就是说，同一个用户在同一个租户下使用不同 Agent 时，每个 Agent 都有自己独立的一套 `Agent.md / team.md / user.md / memory.md / session.md / file-index.md / agent-config.md`，不得把 A Agent 的用户画像、长期记忆、文件线索或配置直接混入 B Agent。

| 文件              | 产品定义                                                                            | 作用范围                                   | 是否跨会话                    | 是否页面历史记录 | 更新来源                                           |
| ----------------- | ----------------------------------------------------------------------------------- | ------------------------------------------ | ----------------------------- | ---------------- | -------------------------------------------------- |
| `Agent.md`        | 当前 Agent 对当前用户生效的稳定身份、角色边界、表达风格和禁止事项                   | `User + Tenant + Member + Agent`           | 是                            | 否               | 产品配置、租户配置或用户配置，不由普通对话自动改写 |
| `team.md`         | 当前 ME 工作台已添加专家的团队清单，记录专家 ID、能力画像摘要、工作台状态和详情引用 | `User + Tenant + Member + Agent`           | 是                            | 否               | 添加专家、移除专家、专家配置变更时自动同步         |
| `user.md`         | 当前用户在当前租户成员身份、当前 Agent 下的长期画像、工作角色、偏好和协作习惯       | `User + Tenant + Member + Agent`           | 是                            | 否               | 用户主动设置、确认后的画像沉淀、运营导入           |
| `memory.md`       | 当前用户使用当前 Agent 时形成的长期记忆，包括偏好、目标、纠错、任务结论、对象事实   | `User + Tenant + Member + Agent`           | 是                            | 否               | Agent 候选沉淀、用户确认、用户纠正、迁移导入       |
| `session.md`      | 当前 Agent 的当前 session 短期任务上下文、阶段目标、待确认问题和未沉淀临时结论      | `User + Tenant + Member + Agent + Session` | 否，除非转为 `memory.md` 候选 | 否               | 当前会话过程自动生成或用户确认                     |
| `file-index.md`   | 当前用户使用当前 Agent 时可召回的文件线索，只记录文件身份、来源、摘要、权限和标签   | `User + Tenant + Member + Agent`           | 可跨会话按权限召回            | 否               | Web 上传、Leadeep 产物、飞书附件、历史迁移         |
| `agent-config.md` | 当前 Agent 对当前用户生效的专家、工具、能力开关、调用偏好和渠道能力约束             | `User + Tenant + Member + Agent`           | 是                            | 否               | 产品配置、租户配置、用户配置                       |

配套模板文件放在 `templates/daguan-leadeep-memory/`，用于后续产品评审、研发建模和测试用例对齐。

##### 任务二：长期个人记忆

- 用户偏好、稳定目标、纠错信息和长期事实按用户和租户维度生效。
- Web 普通会话、Leadeep 绑定会话和后续飞书绑定会话均可按权限召回长期个人记忆。
- 用户需具备查看、纠正、停用或删除个人记忆的入口，具体入口可后续设计。

##### 任务三：任务记忆

- 当前任务目标、阶段结论、用户采纳的决策可沉淀为任务记忆。
- 任务记忆优先服务当前 session。
- 具备长期价值的任务记忆可转为长期个人记忆候选。

##### 任务四：文件线索

- PRD 文件、Leadeep 输出文件、飞书传入文件、历史迁移文件和产物文档形成文件线索。
- 文件线索用于帮助 ME 找到文件，不替代文件本体。
- 文件是否进入回答上下文需按权限、相关性和用户意图判断。

##### 任务五：Agent 配置文件

- ME 人设、可调专家、工具策略、专家调用偏好按当前用户、租户和智能体范围生效。
- 配置更新后影响后续新轮次，不回写历史消息和已生成产物。
- `team.md` 是 ME 工作台的专家团队索引。每次用户向工作台添加专家时，系统自动写入该专家的专家 ID、名称、能力画像、可用场景、状态和详情配置引用；用户从工作台移除专家时，系统同步将该专家从 `team.md` 中移除或置为 removed。
- `team.md` 只保存专家能力画像摘要和详情引用，不保存专家完整能力配置；查看专家详细能力配置时，通过专家 ID 跳转或读取专家详情配置。

##### 任务六：运行时上下文组装

- ME 响应用户问题时，应按权限和作用范围组装上下文，而不是把全部聊天历史塞入模型。
- 建议产品口径下的组装优先级为：`Agent.md`、`agent-config.md`、`team.md`、`user.md`、`memory.md`、`file-index.md`、`session.md`、当前绑定会话页面历史记录、当前用户输入。
- 页面历史记录只作为当前 session 可见消息流参与当前会话，不自动变成长期记忆。
- 任意跨 session 召回都必须来自 `memory.md`、`file-index.md` 或明确可跨会话的配置文件。

#### 2.4.3 业务示例

##### 示例一：Leadeep 找到 Web 里的 PRD 文件

- 用户在 Web 中写过 V1.1 PRD，并形成文件线索。
- 用户在 Leadeep 问“V1.1 定价策略是什么”。
- ME 通过长期记忆和文件线索定位相关 PRD 文件，并基于权限进行引用回答。

##### 示例二：新 Web 会话复用长期记忆

- 用户在一个历史 session 中确认“我偏好先给结论再给拆解”。
- 该偏好进入长期个人记忆。
- 用户新建 Web session 后，ME 可使用该偏好，但不读取历史 session 原文。

#### 2.4.4 规则清单

- 历史记录同步和统一记忆机制是两层能力：历史记录是用户在页面看到的消息流；记忆文件是 Agent 背后用于理解用户、任务和文件的工作机制。
- 跨会话共享：同一 `User + Tenant + Member + Agent` 下的 `Agent.md`、`team.md`、`user.md`、`memory.md` 中已生效的长期记忆、`file-index.md` 中按权限可召回的文件线索、`agent-config.md` 中已生效的配置。
- 跨会话隔离：页面历史记录原文、临时文件全文、未采纳方案、工具过程、`session.md` 中尚未转为长期记忆的当前任务短期上下文。
- 文件线索只提供定位能力，不等于文件全文默认进入上下文。
- `session.md` 的内容默认只服务当前 session；只有用户确认或系统规则判定具备长期价值时，才能转为 `memory.md` 候选。
- `agent-config.md` 更新后只影响后续轮次，不追溯改写历史消息、历史产物或历史记忆证据。
- `Agent.md` 不承载用户事实和项目事实；`user.md` 不承载页面聊天原文；`memory.md` 必须保留来源、范围、置信度和状态。
- `team.md` 不承载专家详情配置本体，只承载当前工作台已添加专家的能力画像摘要、专家 ID、状态和详情引用。
- 不同 Agent 之间默认隔离：除非后续设计明确的跨 Agent 授权与复制机制，否则不得跨 Agent 直接共享用户画像、长期记忆、文件线索、session 上下文或 Agent 配置。
- 本轮不设计企业级知识治理，不把普通成员表达的企业规则直接全员生效。
- Leadeep 和飞书使用同一套记忆、文件线索和 Agent 配置规则。

#### 2.4.5 流程与原型截图标记

- 图 LDP-MEM-01 / 标记 A：统一记忆机制在不同入口下生效。
- 图 LDP-MEM-02 / 标记 A：文件线索召回与引用。
- 图 LDP-MEM-03 / 标记 A：Agent 配置更新后的后续轮次生效。

---

### 2.5 交叉用户历史资产迁移

#### 2.5.1 目标

- 只处理“用户原先下载并使用过 Leadeep，后来又开通大观”的交叉用户历史资产迁移。
- 用户完成大观账户体系打通后，不丢失其在 Leadeep 中已经沉淀的关键历史会话、文件型知识和产物资产。
- 迁移过程可追踪、可重试、可校验。
- 历史原文不默认进入新会话上下文，避免无控制污染记忆。

#### 2.5.2 任务拆解

##### 任务一：迁移对象确认

- 运营或管理员提交源 Leadeep 账号和目标大观成员身份。
- 系统校验手机号、账号关系、租户成员状态。
- 系统确认该用户同时满足：已存在 Leadeep 历史使用数据、已开通大观租户成员身份、已开放或准备开放 Leadeep 移动端权限。
- 无法确认目标成员时进入人工处理状态。

##### 任务二：迁移范围选择

- 支持选择历史会话原文、文件型知识库、上传文件、重要产物、关键摘要。
- 迁移范围为空时不允许执行。
- 本期迁移由运营后台执行，不设计用户自助确认流程。

##### 任务三：迁移执行

- Leadeep 侧提供历史数据导出或读取接口。
- 大观侧接收并转换为大观历史资产、文件资产、文件线索和记忆候选。
- 每个迁移对象记录来源、目标、状态和失败原因。

##### 任务四：迁移校验

- 迁移完成后展示源数量、成功数量、失败数量和抽样预览。
- 失败项可单独重试。
- 部分成功不影响已成功资产使用。

#### 2.5.3 业务示例

##### 示例一：领导历史文件迁移

- 某领导原先独立使用过 Leadeep，后续企业为其开通大观。
- 运营在确认 Leadeep 账号与大观成员身份后发起迁移。
- 选择历史文件型知识库和重要产物。
- 迁移完成后，用户以大观成员身份在 Leadeep 问策中可通过文件线索找到历史文件。

##### 示例二：部分文件迁移失败

- 迁移过程中部分文件读取失败。
- 系统记录失败文件、失败原因和重试入口。
- 运营重试失败项，不重复迁移已成功文件。

#### 2.5.4 规则清单

- 历史会话原文可归档查看和检索，但不默认进入新会话上下文。
- 只迁移交叉用户资产，不对未开通大观的纯 Leadeep 用户做迁移。
- 只在账户体系能确认目标大观成员身份时执行迁移；无法确认身份时不得自动合并。
- 文件型知识库进入大观统一文件/知识服务或其迁移过渡层。
- 关键摘要和偏好进入记忆候选，不直接无审核生效。
- 迁移任务必须具备日志、状态、失败原因、重试和校验结果。

#### 2.5.5 流程与原型截图标记

- 图 LDP-MIG-01 / 标记 A：迁移任务创建。
- 图 LDP-MIG-02 / 标记 A：迁移范围选择。
- 图 LDP-MIG-03 / 标记 A：迁移状态与失败重试。
- 图 LDP-MIG-04 / 标记 A：迁移结果校验。

---

## 3. 优化项

### 3.1 历史记录连续工具调用折叠展示

#### 3.1.1 目标

- 优化 Web 会话详情和 Leadeep 绑定会话的历史记录阅读体验。
- 当一轮 Agent 回复中出现连续工具调用时，页面默认折叠工具调用过程，避免历史记录被大量中间步骤打断。
- 折叠只影响页面展示，不改变消息存储、同步、审计和 Agent 记忆机制。

#### 3.1.2 任务拆解

##### 任务一：连续工具调用识别

- 同一轮 Agent 回复中，连续出现的工具调用消息可合并为一个工具调用折叠组。
- 折叠组需保留工具数量、执行状态、耗时、失败数量和关键工具名称。
- 非连续工具调用不强制合并，避免跨越用户消息或 Agent 正文造成理解断层。

##### 任务二：默认折叠与展开查看

- 历史记录默认展示折叠组摘要，例如“已调用 3 个工具，2 个成功，1 个失败”。
- 用户点击折叠组后可展开查看每个工具调用的名称、状态、简要输入、简要输出、耗时和失败原因。
- 展开或收起只影响当前页面阅读状态，不影响另一端同步结果。

##### 任务三：异常与可追溯

- 工具调用失败时，折叠组摘要需突出失败状态，用户可展开查看失败原因。
- 如果工具调用产生引用文件、输出产物或关键结果，最终 Agent 回复中仍需展示用户可理解的结果和引用，不要求用户展开工具过程才能理解回答。
- 审计、排查和调试需要的完整工具调用记录仍保留在底层历史记录或日志中。

#### 3.1.3 业务示例

##### 示例一：连续检索文件并生成回答

- 用户在 Leadeep 问策询问“V1.1 PRD 的定价策略是什么”。
- ME 连续执行文件检索、文件读取、摘要提取三个工具调用。
- Web 历史记录中默认只展示一个工具调用折叠组和最终回答。
- 用户需要排查来源时，可展开折叠组查看工具调用明细。

#### 3.1.4 规则清单

- 连续工具调用折叠属于历史记录展示优化，不属于新增同步能力。
- Leadeep 与 Web 仍同步同一份历史记录；两端可根据各自 UI 能力决定默认折叠样式。
- 折叠不得吞掉用户消息、Agent 最终回复、引用文件和输出产物。
- 工具调用折叠组需要支持失败态、部分成功态和执行中状态。

#### 3.1.5 流程与原型截图标记

- 图 LDP-HIS-01 / 标记 A：连续工具调用默认折叠。
- 图 LDP-HIS-01 / 标记 B：展开查看工具调用明细。
- 图 LDP-HIS-02 / 标记 A：工具调用失败状态。

---

### 3.2 分享弹窗无预览

#### 3.2.1 目标

- 成果分享、对话分享和会话分析分享弹窗只承担“生成分享链接”和“复制链接”的操作闭环。
- 弹窗不展示在线预览，不展示“使用 Frontis AI 创建”入口，避免分享操作被预览区域分散注意力。

#### 3.2.2 任务拆解

##### 任务一：成果分享弹窗

- 用户在成果列表或成果详情中点击分享后，弹窗展示链接生成状态、分享链接和复制按钮。
- 弹窗不展示成果文件预览卡片、在线预览标题和二次创建入口。

##### 任务二：会话分析分享弹窗

- 用户在会话分析中点击分享后，弹窗展示链接生成状态、分享链接和复制按钮。
- 弹窗不展示会话分析预览内容、在线预览标题和二次创建入口。

#### 3.2.3 规则清单

- 分享弹窗只处理分享链路，不承载成果阅读或会话分析阅读。
- 用户复制链接后，通过分享页或成果详情页查看内容，不在弹窗内预览。
- 去预览只影响弹窗展示，不改变分享链接权限、有效期和访问控制。

---

### 3.3 异常状态与重试

#### 3.3.1 目标

- 开通、登录、绑定、同步、迁移失败时都有明确状态。
- 管理员、用户和运营能知道失败原因与下一步动作。

#### 3.3.2 任务拆解

##### 任务一：开通异常

- 缺手机号、成员停用、成员不属于当前租户、创建绑定会话失败时展示对应原因。
- 支持管理员重试可恢复异常。

##### 任务二：同步异常

- App 消息写入失败时展示发送失败。
- 重试成功后不产生重复消息。
- Web 端同步异常需展示最近同步状态。

##### 任务三：迁移异常

- 迁移失败记录到对象级别。
- 失败项可单独重试。
- 迁移结果支持抽样校验。

#### 3.3.3 业务示例

##### 示例一：App 发送失败后重试

- 用户在 Leadeep 问策发送消息。
- 网络失败导致写入失败。
- App 展示失败态，用户点击重试后成功写入同一消息，不生成重复记录。

#### 3.3.4 流程与原型截图标记

- 图 LDP-ERR-01 / 标记 A：开通失败。
- 图 LDP-ERR-02 / 标记 A：消息失败重试。
- 图 LDP-ERR-03 / 标记 A：迁移失败重试。
