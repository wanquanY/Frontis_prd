# Frontis AI · 大观 Leadeep 打通 PRD

## 1. 版本范围

### 1.1 本期需求

- 企业管理后台支持为正常企业成员开放 Leadeep 移动端权限，普通员工获得权限后即可使用移动端。
- Web 左下角用户卡片提供移动端下载入口，hover 展示下载二维码，用户手机扫码下载 Leadeep App。
- 大观与 Leadeep App 完成账户体系打通，企业为成员开通 Leadeep 后，成员即可登录 Leadeep 移动端使用问策。
- 用户在 Leadeep 问策里发起对话时，系统自动同步到大观 Web 端对应的 ME 会话，用户不需要做任何同步设置。
- Leadeep 问策与 Web 端对应 ME 会话的历史记录双向同步，历史记录指用户在页面上看到的聊天消息、引用文件、输出产物和消息状态。
- 建立统一记忆机制，让 Web、Leadeep 和后续飞书在同一套 Agent 记忆、文件线索和配置机制下工作。
- 对“先使用过 Leadeep、后开通大观”的交叉用户，支持将其 Leadeep 历史会话、文件型知识、产物资产和已结算余额迁移到大观企业身份下。
- 飞书作为后续外部同步入口预留口径：与 Leadeep 使用同一套账户体系、会话和统一记忆机制，唯一差异是历史记录只从飞书单向同步到 Web，Web 续聊不回写飞书。

### 1.2 核心产品口径

- Web 是大观 ME 多会话主入口，负责创建、切换和管理会话。
- Leadeep 问策是移动端对话入口，用户在问策中的对话会自动同步到 Web 端对应 ME 会话，用户不需要创建会话或配置同步关系。
- 飞书也是外部对话入口，只把飞书侧对话记录同步到 Web 端对应会话，不让用户在飞书内创建或管理大观会话。
- Leadeep 与飞书的唯一产品差异是历史记录同步方向：
  - Leadeep：Web 与 Leadeep 双向同步历史记录。
  - 飞书：飞书历史记录单向同步到 Web，Web 在该会话下续聊不回写飞书。
- 长期个人记忆、任务记忆、文件线索、Agent 配置文件属于统一记忆机制，不为飞书或 Leadeep 分叉一套规则。
- 交叉用户开通大观后，需要同步 Leadeep 侧已结算余额，并按 `1 元 = 100 大观积分` 折算为大观积分余额；例如 Leadeep 结算后余额为 100 元，则自动充值为 10000 大观积分。

---

## 2. 新增需求

### 2.1 企业管理后台 Leadeep 权限开放

#### 2.1.1 目标

- 企业管理员可以为当前企业内状态正常的成员开放 Leadeep 移动端权限。
- 普通员工被开放移动端权限后，可登录 Leadeep App 使用问策；问策对话自动同步到大观 Web 端对应 ME 会话。
- 管理员可查看开通状态、同步状态和异常原因。

#### 2.1.2 任务拆解

##### 任务一：权限开放入口

- 在企业管理后台增加 Leadeep 权限开放入口。
- 页面展示当前企业成员列表、成员状态、手机号、开通状态、最近同步状态。
- 支持为单个正常成员开放移动端权限、关闭权限和重新同步。
> [图片]

##### 任务二：成员状态校验

- 只允许为当前企业内状态正常的成员开放移动端权限。
- 成员无手机号、已停用、已移除或非当前企业成员时阻断开通。
- 同一成员已开通时不重复开通。

##### 任务三：Web 用户卡片下载入口

- Web 左下角用户卡片增加移动端下载按钮。
- 用户 hover 移动端下载按钮时，展示 Leadeep App 下载二维码。
- 用户使用手机扫码后进入 Leadeep App 下载页或唤起安装引导。
- 只有已开放移动端权限的成员，在 Web 左下角用户卡片中展示移动端下载按钮。

#### 2.1.3 业务示例

##### 示例一：管理员为普通员工开放移动端权限

- 企业管理员进入 Leadeep 权限开放页。
- 选择状态正常的普通员工并点击开放权限。
- 系统校验成员状态和手机号后开放 Leadeep 使用权限。
- 该员工登录 Leadeep App 后可以使用问策，对话自动同步到 Web 端 ME 会话。

##### 示例二：员工从 Web 用户卡片下载移动端

- 员工在大观 Web 左下角打开用户卡片。
- 用户卡片中展示移动端下载按钮。
- 员工 hover 按钮后看到下载二维码。
- 员工用手机扫码下载 Leadeep App，并登录进入已开放权限的企业身份。

##### 示例三：成员被停用

- 成员在企业管理后台被停用。
- 系统同步停用该成员 Leadeep 写入权限。
- 成员在 App 继续发送消息时被阻断，并提示当前企业身份不可用。

#### 2.1.4 规则清单

- 移动端权限归属于企业成员，不归属于纯 Leadeep C 端账号。
- 当前企业内状态正常的普通成员都可以被开放移动端权限，不限角色。
- 同一用户在多个企业内均开通时，App 让用户选择当前进入的企业，并且可以切换企业。
- 停用成员同步禁用 Web 端和 Leadeep 账号。
- Web 左下角用户卡片的移动端下载入口依赖成员移动端权限；二维码只负责下载或唤起 App。
- 开放 Leadeep 权限本身不等于立即扣费；但交叉用户已存在 Leadeep 余额时，必须在账户体系打通后执行余额迁移和积分充值。新用户余额就直接为大观的积分余额。

---

### 2.2 大观与 Leadeep 账户体系打通

#### 2.2.1 目标

- 企业为成员开通 Leadeep 权限后，该成员可以使用自己的账号登录 Leadeep App，并进入对应企业的问策能力。
- 用户在 Leadeep App 中看到和使用的是当前企业身份下的大观能力，包括问策对话、历史记录、文件线索和记忆。
- 账户体系打通需要处理同一手机号多个企业、未开通权限、账号不一致等用户可感知问题。

#### 2.2.2 任务拆解

##### 任务一：登录识别

- 用户使用手机号或统一账号登录 Leadeep App。
- 系统识别该用户是否已被企业开通 Leadeep 权限。
- 已开通的用户可以进入对应企业的问策能力。

##### 任务二：企业身份判断

- 若用户只有一个已开放移动端权限的企业身份，直接进入对应企业的问策。
- 若用户有多个已开放移动端权限的企业身份，要求用户选择企业。
- 若用户没有已开放移动端权限的企业身份，不展示大观问策入口。

#### 2.2.3 业务示例

##### 示例一：单企业成员登录

- 用户用手机号登录 Leadeep App。
- 系统找到该手机号在大观某企业下已开放 Leadeep 移动端权限。
- App 直接进入该企业的大观问策。

##### 示例二：多企业身份登录

- 同一手机号属于两个大观企业，且两个企业均已开放 Leadeep 移动端权限。
- App 展示企业选择。
- 用户选择后进入对应企业的问策。

#### 2.2.4 规则清单

- 用户进入 Leadeep 问策前，必须明确当前使用的是哪个企业身份。
- Leadeep 账号接入大观后，使用的是当前企业身份下的权限、会话、历史记录和记忆机制。
- App 不得默认进入最近企业，必须保证用户明确当前身份。
- 成员被移除后，App 不展示该企业入口。
- 历史数据归属策略按企业数据权限另行确认。

---

### 2.3 Web 多会话与问策自动同步

#### 2.3.1 目标

- 用户在 Leadeep 问策中对话时，系统自动同步到大观 Web 端对应 ME 会话，不需要用户做任何同步设置。
- 后续飞书入口也同步到 Web 端对应会话，但飞书历史记录只从飞书单向同步到 Web，Web 续聊不回写飞书。
- Web 仍是多会话主入口；Leadeep App 和飞书本期都不承担大观会话创建和管理。
- 用户在 Web 会话列表中能识别普通 Web 会话、Leadeep 问策同步会话和飞书同步会话。
- 历史记录只指用户在页面上看到的聊天消息、引用文件、输出产物和消息状态。

#### 2.3.2 任务拆解

##### 任务一：Leadeep 问策自动同步会话

- 成员被开放 Leadeep 移动端权限后，用户在 Leadeep 问策中的对话自动同步到 Web 端对应 ME 会话。
- 用户不需要在 App 或 Web 上执行任何同步设置，系统自动完成对应。
- 同一用户在同一企业下，Leadeep 问策默认对应一条 Web 端 ME 同步会话。

##### 任务二：飞书同步会话预留

- 飞书入口同步到 Web 多会话中的一条对应会话。
- 飞书不创建大观新会话。
- 飞书不管理大观多会话列表。

##### 任务三：Web 会话列表展示与来源标签

- Web 多会话列表展示 Leadeep 问策同步会话。
- 后续飞书接入后，Web 多会话列表展示飞书同步会话。
- 普通 Web 会话不展示额外来源标签。
- Leadeep 问策同步会话只展示 Leadeep 渠道标签。
- 飞书同步会话只展示飞书渠道标签。
- 会话列表不展示“单向 / 双向”等同步方向文案，避免把产品机制说明暴露成列表噪音。
- 用户点击 Leadeep 或飞书同步会话后，进入正常聊天记录页；消息展示、输入、继续对话和普通 Web 会话保持一致。

##### 任务四：Leadeep 历史记录双向同步

- App 问策发送消息后，Web 端对应 ME 会话可看到同一条消息和回复。
- Web 打开 Leadeep 问策同步会话时读取同一消息流。
- 用户在 Web 的 Leadeep 同步会话中继续提问时，Leadeep App 也能看到新增消息。
- 消息需包含来源端、发送人、消息类型、创建时间和同步状态。
- 消息排序以服务端时间和消息序号为准。

##### 任务五：飞书历史记录单向同步

- 飞书历史记录同步到 Web 端对应会话。
- Web 在该飞书同步会话中新增的对话记录不回写飞书。
- 系统内部需记录消息来源，用于同步判断和审计；Web 聊天页不额外区分飞书同步记录和 Web 本端续聊记录，保持普通聊天记录体验。

##### 任务六：会话边界

- Leadeep 问策同步会话原文只属于该会话。
- 飞书同步会话原文只属于该会话。
- 其他 Web 普通会话不读取 Leadeep 问策同步会话原文。
- 其他 Web 普通会话不读取飞书同步会话原文。
- 跨会话可复用的是已沉淀的长期个人记忆、任务记忆、文件线索和 Agent 配置文件。

#### 2.3.3 业务示例

##### 示例一：产品经理按版本写 PRD

- 用户在 Web 为 V1.0、V1.1 分别创建普通会话。
- 每个版本会话的原文、临时文件和阶段上下文互相隔离。
- 版本目标、已采纳结论和 PRD 文件线索可沉淀为长期可召回资产。

##### 示例二：出差时用 Leadeep 追问

- 用户打开 Leadeep 问策询问 V1.1 PRD 内容。
- 系统自动把本次问策对话同步到 Web 端对应 ME 会话。
- ME 根据长期记忆和文件线索定位此前 Web 产出的 PRD 文件并回答。

##### 示例三：移动端提问，Web 回看

- 用户在 Leadeep 问策询问“V1.1 权限边界是什么”。
- ME 返回答案并引用相关 PRD 文件。
- 用户回到电脑打开 Web 端对应 ME 会话，看到同一条提问、回复和引用文件。

##### 示例四：Web 续聊，移动端继续

- 用户在 Web 端对应 ME 会话继续追问两轮。
- 再次打开 Leadeep 问策。
- App 展示 Web 新增的聊天记录。

##### 示例五：飞书记录进入 Web

- 用户在飞书里向 ME 提问。
- 该飞书对话记录同步到 Web 端对应会话。
- 用户打开 Web 后能看到飞书同步过来的记录。

##### 示例六：Web 续聊不回飞书

- 用户在 Web 端对应飞书同步会话中继续追问。
- 新增对话只留在 Web 会话内。
- 飞书端不展示这部分 Web 续聊记录。

#### 2.3.4 规则清单

- Web 是唯一的大观多会话创建入口。
- Leadeep 不允许创建大观新会话。
- Leadeep 不允许切换大观普通会话。
- 飞书不允许创建大观新会话。
- 飞书不允许切换大观普通会话。
- Leadeep 问策同步会话可以使用同一套长期个人记忆、文件线索和 Agent 配置文件。
- 飞书同步会话可以使用同一套长期个人记忆、文件线索和 Agent 配置文件。
- Leadeep 与 Web 的历史记录是双向同步。
- 飞书历史记录只从飞书单向同步到 Web。
- 历史记录同步不等于长期记忆同步；长期记忆的沉淀、召回和纠错规则见统一记忆机制章节。
- 同步对象是对应会话内页面可见的消息、引用文件、输出产物和消息状态。
- 消息去重以统一消息 ID 为准，不以端侧本地 ID 为准。
- Leadeep 问策同步会话可在 Web 展示，但本期不建议允许用户删除。
> [图片]

---

### 2.4 统一记忆机制

#### 2.4.1 目标

- 建立一套面向 ME / Agent 的统一记忆机制，让 Web、Leadeep 和后续飞书入口使用同一套记忆沉淀、召回、纠错和配置生效规则。
- 记忆机制是 Agent 背后的工作机制，不等同于页面历史记录同步。
- 多会话之间区分“可跨会话共享的记忆资产”和“必须隔离的会话原文 / 当前任务短期上下文”。

#### 2.4.2 任务拆解

##### 任务一：统一记忆文件示例模板

本期大观和 Leadeep 不把“记忆”设计成一团模糊上下文，而是拆成一组职责清晰的配置文件与记忆文件。PRD 中先定义每个文件的职责边界，具体 mock 示例模板放在 `templates/daguan-leadeep-memory/` 中，用产品经理跨 Web、Leadeep 问策、飞书三个入口推进 PRD 的场景来呈现真实运行中这些文件会沉淀成什么内容。

| 文件 | PRD 定义 | 产品职责 | 与多会话关系 | 示例模板 |
| ---- | -------- | -------- | ------------ | -------- |
| `Agent.md` | 当前 Agent 对当前用户生效的稳定工作身份、角色边界、表达风格和禁止事项 | 定义 ME 是谁、怎么工作、哪些事情不能做 | 跨会话生效，不保存页面历史记录 | `templates/daguan-leadeep-memory/Agent.md` |
| `team.md` | 当前 ME 工作台已添加专家的团队清单 | 记录专家 ID、能力画像摘要、工作台状态和详情配置引用 | 跨会话生效；添加或移除专家时同步更新 | `templates/daguan-leadeep-memory/team.md` |
| `user.md` | 当前用户在当前企业、当前 Agent 下的用户画像、工作角色、偏好和协作习惯 | 帮助 ME 理解这个用户是谁、怎么表达、如何协作 | 跨会话生效，不保存单个会话原文 | `templates/daguan-leadeep-memory/user.md` |
| `memory.md` | 当前用户使用当前 Agent 时沉淀的长期记忆 | 保存偏好、目标、纠错、任务结论、对象事实等可复用内容 | 跨会话召回；不等于聊天历史原文 | `templates/daguan-leadeep-memory/memory.md` |
| `session.md` | 当前会话的短期任务上下文 | 保存当前会话目标、阶段结论、待处理事项和临时上下文 | 默认只服务当前会话；具备长期价值的内容可转为记忆候选 | `templates/daguan-leadeep-memory/session.md` |
| `file-index.md` | 当前用户使用当前 Agent 时可召回的文件线索 | 记录文件名称、来源、用途、相关主题和可用于回答的问题 | 可跨会话按权限召回；不保存文件全文 | `templates/daguan-leadeep-memory/file-index.md` |
| `agent-config.md` | 当前 Agent 对 Web、Leadeep、飞书等入口生效的工作配置 | 定义入口能力、回答策略、专家调用偏好、工具使用偏好和渠道差异 | 跨会话生效；配置更新影响后续轮次 | `templates/daguan-leadeep-memory/agent-config.md` |

配套示例模板放在 `templates/daguan-leadeep-memory/`，包括 `Agent.md`、`team.md`、`user.md`、`memory.md`、`session.md`、`file-index.md`、`agent-config.md`。这些文件用于产品评审时说明 ME 如何在多会话、移动端问策和飞书同步下持续理解用户、任务、文件和专家配置。

##### 任务二：长期个人记忆

- 用户偏好、稳定目标、纠错信息和长期事实按用户和企业维度生效。
- Web 普通会话、Leadeep 问策同步会话和后续飞书同步会话均可按权限召回长期个人记忆。
- 用户需具备查看、纠正、停用或删除个人记忆的入口，具体入口可后续设计。

##### 任务三：任务记忆

- 当前任务目标、阶段结论、用户采纳的决策可沉淀为任务记忆。
- 任务记忆优先服务当前会话。
- 具备长期价值的任务记忆可转为长期个人记忆候选。

##### 任务四：文件线索

- PRD 文件、Leadeep 输出文件、飞书传入文件、历史迁移文件和产物文档形成文件线索。
- 文件线索用于帮助 ME 找到文件，不替代文件本体。
- 文件是否进入回答上下文需按权限、相关性和用户意图判断。

##### 任务五：Agent 配置文件

- ME 人设、可调专家、工具策略、专家调用偏好按当前用户、企业和智能体范围生效。
- 配置更新后影响后续新轮次，不回写历史消息和已生成产物。
- `team.md` 是 ME 工作台的专家团队索引。每次用户向工作台添加专家时，系统自动写入该专家的专家 ID、名称、能力画像、可用场景、状态和详情配置引用；用户从工作台移除专家时，系统同步将该专家从 `team.md` 中移除或置为 removed。
- `team.md` 只保存专家能力画像摘要和详情引用，不保存专家完整能力配置；查看专家详细能力配置时，通过专家 ID 跳转或读取专家详情配置。

##### 任务六：运行时上下文组装

- ME 响应用户问题时，应按当前问题、用户偏好、文件线索、会话内容和权限组装上下文，而不是把全部聊天历史塞入模型。
- 建议产品口径下的组装优先级为：`Agent.md`、`agent-config.md`、`team.md`、`user.md`、`memory.md`、`file-index.md`、`session.md`、当前会话页面历史记录、当前用户输入。
- 页面历史记录只作为当前会话可见消息流参与当前会话，不自动变成长期记忆。
- 任意跨会话召回都必须来自 `memory.md`、`file-index.md` 或明确可跨会话的配置文件。

#### 2.4.3 业务示例

##### 示例一：Leadeep 找到 Web 里的 PRD 文件

- 用户在 Web 中写过 V1.1 PRD，并形成文件线索。
- 用户在 Leadeep 问“V1.1 定价策略是什么”。
- ME 通过长期记忆和文件线索定位相关 PRD 文件，并基于权限进行引用回答。

##### 示例二：新 Web 会话复用长期记忆

- 用户在一个历史会话中确认“我偏好先给结论再给拆解”。
- 该偏好进入长期个人记忆。
- 用户新建 Web 会话后，ME 可使用该偏好，但不读取历史会话原文。

#### 2.4.4 规则清单

- 历史记录同步和统一记忆机制是两层能力：历史记录是用户在页面看到的消息流；记忆文件是 Agent 背后用于理解用户、任务和文件的工作机制。
- 跨会话共享：同一用户、同一企业、同一 Agent 下的 `Agent.md`、`team.md`、`user.md`、`memory.md` 中已生效的长期记忆、`file-index.md` 中按权限可召回的文件线索、`agent-config.md` 中已生效的配置。
- 跨会话隔离：页面历史记录原文、临时文件全文、未采纳方案、工具过程、`session.md` 中尚未转为长期记忆的当前任务短期上下文。
- 文件线索只提供定位能力，不等于文件全文默认进入上下文。
- `session.md` 的内容默认只服务当前会话；只有用户确认或系统规则判定具备长期价值时，才能转为 `memory.md` 候选。
- `agent-config.md` 更新后只影响后续轮次，不追溯改写历史消息、历史产物或历史记忆证据。
- `Agent.md` 不承载用户事实和项目事实；`user.md` 不承载页面聊天原文；`memory.md` 必须保留来源、范围、置信度和状态。
- `team.md` 不承载专家详情配置本体，只承载当前工作台已添加专家的能力画像摘要、专家 ID、状态和详情引用。
- 不同 Agent 之间默认隔离：除非后续设计明确的跨 Agent 授权与复制机制，否则不得跨 Agent 直接共享用户画像、长期记忆、文件线索、会话上下文或 Agent 配置。
- 本轮不设计企业级知识治理，不把普通成员表达的企业规则直接全员生效。
- Leadeep 和飞书使用同一套记忆、文件线索和 Agent 配置规则。

#### 2.4.5 流程与原型截图标记

- 图 LDP-MEM-01 / 标记 A：统一记忆机制在不同入口下生效。
- 图 LDP-MEM-02 / 标记 A：文件线索召回与引用。
- 图 LDP-MEM-03 / 标记 A：Agent 配置更新后的后续轮次生效。

---

### 2.5 交叉用户历史资产与余额迁移

#### 2.5.1 目标

- 只处理“用户原先下载并使用过 Leadeep，后来又开通大观”的交叉用户迁移。
- 用户完成大观账户体系打通后，不丢失其在 Leadeep 中已经沉淀的关键历史会话、文件型知识、产物资产和已结算余额。
- Leadeep 侧已结算余额需自动折算为大观积分余额，确保用户已付费权益能在大观内继续使用。
- 迁移过程可追踪、可重试、可校验。
- 历史原文不默认进入新会话上下文，避免无控制污染记忆。

#### 2.5.2 任务拆解

##### 任务一：迁移对象确认

- 运营或管理员提交源 Leadeep 账号和目标大观企业身份。
- 系统校验手机号、账号关系和企业成员状态。
- 系统确认该用户同时满足：已存在 Leadeep 历史使用数据、已开通大观企业身份、已开放或准备开放 Leadeep 移动端权限。
- 无法确认目标成员时进入人工处理状态。

##### 任务二：迁移范围选择

- 支持选择历史会话原文、文件型知识库、上传文件、重要产物、关键摘要和 Leadeep 已结算余额。
- 迁移范围为空时不允许执行。
- 本期迁移由运营后台执行，不设计用户自助确认流程。

##### 任务三：Leadeep 余额结算与积分折算

- Leadeep 侧需提供用户当前可迁移余额，以人民币金额为结算口径。
- 可迁移余额必须是扣除已消耗、已退款、冻结或争议金额后的已结算余额。
- 大观侧按 `1 元 = 100 大观积分` 进行折算。
- 例如 Leadeep 已结算余额为 100 元，则折算为 10000 大观积分。
- 折算后的积分自动充值到该用户开通大观后的目标企业对应的大观积分余额中。
- 系统需生成一条来源为“Leadeep 余额迁移”的充值流水，记录源 Leadeep 账号、目标大观用户、目标企业、结算金额、折算比例、到账积分、执行时间和执行结果。

##### 任务四：迁移执行

- Leadeep 侧提供历史数据导出或读取接口。
- 大观侧接收并转换为大观历史资产、文件资产、文件线索、记忆候选和积分充值流水。
- 每个迁移对象记录来源、目标、状态和失败原因。

##### 任务五：迁移校验

- 迁移完成后展示源数量、成功数量、失败数量和抽样预览。
- 余额迁移完成后展示 Leadeep 结算金额、折算比例、到账积分和充值流水编号。
- 失败项可单独重试。
- 部分成功不影响已成功资产使用。

#### 2.5.3 业务示例

##### 示例一：领导历史文件迁移

- 某领导原先独立使用过 Leadeep，后续企业为其开通大观。
- 运营在确认 Leadeep 账号与大观企业身份后发起迁移。
- 选择历史文件型知识库和重要产物。
- 迁移完成后，用户以大观企业身份在 Leadeep 问策中可通过文件线索找到历史文件。

##### 示例二：部分文件迁移失败

- 迁移过程中部分文件读取失败。
- 系统记录失败文件、失败原因和重试入口。
- 运营重试失败项，不重复迁移已成功文件。

##### 示例三：Leadeep 余额自动迁移

- 用户原先在 Leadeep 中有已结算余额 100 元。
- 用户开通大观并完成 Leadeep 账号与大观企业身份确认。
- 系统自动按 `1 元 = 100 大观积分` 折算，向大观积分余额充值 10000 积分。
- 充值流水标记为“Leadeep 余额迁移”，并与该用户的交叉用户迁移任务关联。

#### 2.5.4 规则清单

- 历史会话原文可归档查看和检索，但不默认进入新会话上下文。
- 只迁移交叉用户资产，不对未开通大观的纯 Leadeep 用户做迁移。
- 只在账户体系能确认目标大观企业身份时执行迁移；无法确认身份时不得自动合并。
- 文件型知识库进入大观统一文件/知识服务或其迁移过渡层。
- 关键摘要和偏好进入记忆候选，不直接无审核生效。
- Leadeep 余额迁移只处理已结算余额，不迁移未结算赠送额度、冻结额度、争议金额或已退款金额。
- 同一源 Leadeep 账号到同一目标大观企业身份的余额迁移只能成功执行一次；失败重试必须幂等，不能重复充值积分。
- 若同一 Leadeep 账号需要接入多个大观企业，必须先确认目标企业，不能把同一余额重复迁移到多个企业。
- 迁移任务必须具备日志、状态、失败原因、重试和校验结果，余额迁移还必须具备充值流水和对账记录。
