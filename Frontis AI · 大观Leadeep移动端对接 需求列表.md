# Frontis AI · 大观 Leadeep 打通需求列表

> 本需求列表按 `Frontis AI · V0.8 需求列表.md` 的口径整理。
> 核心范围：大观 Web 多会话与 Leadeep 移动端问策自动同步；企业后台给正常成员开放 Leadeep 权限；普通员工获得权限后可登录 Leadeep 移动端使用问策；交叉用户开通大观后同步 Leadeep 已结算余额并折算为大观积分余额。
> 核心差异：历史记录是页面上看到的聊天消息、引用文件、输出产物和消息状态；Leadeep 与 Web 历史记录双向同步，飞书历史记录只从飞书单向同步到 Web，Web 续聊不回写飞书。记忆是 Agent 背后的统一机制，不等同于历史记录同步。

## 新增需求

| PRD 章节 | 需求名称 | 涉及端 / 模块 | 需求说明 |
| -------- | -------- | ------------- | -------- |
| 2.1 | 企业管理后台 Leadeep 权限开放 | 企业管理后台、Web 用户卡片、Leadeep App | 企业管理员可为当前企业内状态正常的成员开放 Leadeep 移动端权限；普通员工获得权限后可登录 Leadeep App 使用问策，问策对话自动同步到大观 Web 端对应 ME 会话；Web 左下角用户卡片展示移动端下载按钮，hover 展示 Leadeep App 下载二维码；成员停用、移除、无手机号或非当前企业成员时需阻断开通或阻断写入。 |
| 2.2 | 大观与 Leadeep 账户体系打通 | Leadeep App、大观账户体系、企业身份 | 企业为成员开通 Leadeep 后，成员可用自己的账号登录 Leadeep App 并进入对应企业的问策能力；单企业直接进入，多企业要求选择当前企业；未开放移动端权限不展示大观问策入口或提示联系管理员；账号冲突不自动合并，需进入异常处理。 |
| 2.3 | Web 多会话与问策自动同步 | 大观 Web、Leadeep App、飞书 | 用户在 Leadeep 问策中的对话自动同步到大观 Web 端对应 ME 会话，用户不需要做任何同步设置；Web 会话列表通过 `Leadeep` / `飞书` 标签标识同步来源，点击进入后仍是普通聊天记录体验；Leadeep 与 Web 历史记录双向同步；飞书作为后续入口时，历史记录只从飞书单向同步到 Web，Web 续聊不回写飞书；Leadeep 和飞书都不创建、不切换、不管理大观多会话。 |
| 2.4 | 统一记忆机制 | 工作台、ME 记忆、文件服务、Agent 配置 | Web 普通会话、Leadeep 问策同步会话和后续飞书同步会话在同一用户、同一企业、同一 Agent 下使用同一套记忆文件体系，包括 `Agent.md`、`team.md`、`user.md`、`memory.md`、`session.md`、`file-index.md`、`agent-config.md`；每个用户使用的每个 Agent 都有独立文件体系；页面历史记录原文、临时文件全文和当前任务短期上下文按会话隔离，已确认的长期记忆、文件线索和 Agent 配置可跨会话生效。 |
| 2.5 | 交叉用户历史资产与余额迁移 | 运营后台、迁移工具、计费与积分、文件服务 | 只处理“用户原先下载并使用过 Leadeep，后来又开通大观”的交叉用户；在确认 Leadeep 账号与目标大观企业身份后，支持迁移历史会话原文、文件型知识库、上传文件、重要产物、关键摘要和已结算余额；Leadeep 已结算余额按 `1 元 = 100 大观积分` 自动折算并充值到大观积分余额，例如 100 元充值为 10000 积分；迁移需具备范围选择、执行日志、失败重试、结果校验、充值流水和对账记录，并保证失败重试不重复迁移资产或重复充值积分。 |
