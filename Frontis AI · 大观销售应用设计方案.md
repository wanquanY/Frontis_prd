# Frontis AI · 大观销售应用设计方案

## 1. 文档信息

| 项目 | 内容 |
| ---- | ---- |
| 文档类型 | 产品设计方案 |
| 版本 | V0.1 |
| 日期 | 2026-05-28 |
| 状态 | 草案 |
| 适用范围 | 运营管理后台、企业管理后台、大观销售应用、订阅购买渠道码使用链路 |

## 2. 目标理解

### 2.1 业务目标

为渠道合作方提供一套独立的销售协作应用，让渠道租户能够管理自己的销售成员、给成员分配销售个人码，并让销售成员自行生成给客户使用的渠道码。客户使用渠道码完成订阅购买或开通后，系统能完成渠道、员工和成交客户归因。

### 2.2 产品定位

`大观销售` 是与 `工作台`、`企业管理后台`、`运营管理后台` 平级的用户侧应用。它不是运营后台菜单，也不是企业管理后台中的配置页。

应用关系如下：

| 应用/后台 | 使用方 | 核心职责 |
| --------- | ------ | -------- |
| 运营管理后台 / 渠道管理 | 平台运营人员 | 管理渠道租户、渠道总码、渠道负责人、关联租户和关联销售负责人 |
| 企业管理后台 / 销售管理 | 渠道租户管理员 | 管理本租户销售成员的销售个人码 |
| 大观销售 | 渠道租户销售成员 | 生成客户可用渠道码，查看自己的渠道码记录 |

## 3. 已确认与待确认

### 3.1 已确认

- 运营管理后台保留 `渠道管理` 功能，不拆除。
- 渠道租户的企业管理后台需要新增 `销售管理`。
- 企业管理后台 `销售管理` 可以绑定本租户内某个成员，并生成一个 4 位销售个人码。
- 用户侧新增独立应用，名称为 `大观销售`。
- 普通员工在 `大观销售` 中只有一个核心功能：生成渠道码，主页面展示本人渠道码记录。
- 最终给客户使用的渠道码由三段组成：`渠道总码（6位）-销售个人码（4位）-动态随机码（6位）`，示例：`XY0001-0012-897654`。
- 完整渠道码全局唯一，可精准溯源至渠道公司、销售人员和成交客户。
- 渠道码生成后 10 分钟内有效，未使用且未失效时临时占用渠道码额度。
- 渠道码被客户支付使用后进入已使用，并永久占用对应渠道码额度。
- 渠道码超过 10 分钟未使用时进入已失效，并释放对应渠道码额度。

### 3.2 合理推断

- 渠道租户也是普通租户的一种，只是被开通了渠道销售相关权限和应用入口。
- 销售个人码由管理员生成和绑定，普通员工不能自己修改销售个人码。
- 普通员工只能查看自己的渠道码记录，不能查看同租户其他成员的渠道码记录。
- 渠道租户管理员应能在企业管理后台查看本租户全部销售码和渠道码归因结果，便于管理团队销售归因。
- 渠道码应在客户完成有效购买或开通后进入“已使用”，避免客户只输入未支付就永久占用渠道码。

### 3.3 待确认

| 问题 | 影响 |
| ---- | ---- |
| 销售个人码是否允许管理员手动填写，还是系统自动生成后允许编辑 | 影响销售管理弹窗字段和校验规则 |
| 一个成员是否只能绑定一个启用中的销售个人码 | 影响销售个人码唯一性规则 |
| 管理员是否能代某个销售成员生成客户渠道码 | 影响大观销售或销售管理是否增加代生成能力 |

## 4. 用户角色与场景

| 角色 | 核心目标 | 使用场景 | 权限边界 |
| ---- | -------- | -------- | -------- |
| 平台运营人员 | 配置和维护渠道租户 | 在运营后台创建渠道、维护渠道总码和渠道负责人 | 可管理所有渠道租户，不进入渠道员工个人销售视角 |
| 渠道租户管理员 | 管理本租户销售团队 | 在企业管理后台拉成员、分配角色、生成销售个人码 | 仅管理本租户组织、角色和销售码 |
| 渠道销售员工 | 获取客户渠道码并追踪渠道码状态 | 在大观销售中生成渠道码，发给客户使用，查看本人渠道码状态 | 只生成自己的渠道码，只看自己的渠道码记录 |
| 客户购买用户 | 使用销售给出的渠道码获得签约价或渠道权益 | 在团队扩充或订阅购买流程中输入渠道码 | 不进入大观销售应用 |

## 5. 业务闭环

### 5.1 主流程

| 步骤 | 角色 | 用户动作 | 系统响应 | 数据/状态变化 | 异常处理 |
| ---- | ---- | -------- | -------- | ------------- | -------- |
| 1 | 平台运营人员 | 在运营后台渠道管理中配置渠道租户 | 保存渠道总码、渠道负责人、关联租户等信息 | 渠道租户获得渠道销售相关能力 | 渠道总码重复时禁止保存 |
| 2 | 渠道租户管理员 | 在企业管理后台组织管理中邀请销售成员 | 成员加入租户组织 | 生成成员账号和组织关系 | 席位不足或手机号重复时提示失败 |
| 3 | 渠道租户管理员 | 在角色管理中给成员配置大观销售权限 | 成员获得大观销售应用入口 | 更新角色权限 | 无权限成员不可访问大观销售 |
| 4 | 渠道租户管理员 | 在销售管理中选择成员并生成销售个人码 | 系统绑定成员和销售个人码 | 销售个人码状态为启用 | 成员已绑定启用销售码时提示冲突 |
| 5 | 渠道销售员工 | 进入大观销售，点击生成渠道码 | 系统生成完整渠道码 | 渠道码状态为未使用，并临时占用 1 个渠道码额度 | 销售个人码停用或渠道额度不足时不可生成 |
| 6 | 客户购买用户 | 在订阅购买中输入渠道码并完成支付 | 系统校验渠道码并应用对应权益 | 渠道码状态变为已使用，沉淀订单归因 | 码不存在、停用、已使用时提示不可用 |
| 7 | 系统 | 渠道码生成超过 10 分钟仍未使用 | 自动将渠道码置为已失效 | 释放该渠道码占用的额度 | 已使用渠道码不释放 |
| 8 | 渠道销售员工 | 查看渠道码记录 | 展示本人渠道码、状态和创建时间 | 渠道码状态可追踪 | 无记录时展示空状态 |

### 5.2 状态流转

#### 销售个人码

| 当前状态 | 触发动作 | 触发角色 | 下一个状态 | 系统动作 | 是否可逆 | 异常处理 |
| -------- | -------- | -------- | ---------- | -------- | -------- | -------- |
| 未创建 | 新建销售码 | 渠道租户管理员 | 启用 | 绑定成员和销售个人码 | 是 | 成员不存在或重复绑定时失败 |
| 启用 | 停用 | 渠道租户管理员 | 停用 | 禁止该成员生成新渠道码 | 是 | 历史渠道码记录保留 |
| 停用 | 启用 | 渠道租户管理员 | 启用 | 恢复生成能力 | 是 | 成员账号停用时不可启用 |

#### 客户渠道码

| 当前状态 | 触发动作 | 触发角色 | 下一个状态 | 系统动作 | 是否可逆 | 异常处理 |
| -------- | -------- | -------- | ---------- | -------- | -------- | -------- |
| 未生成 | 生成渠道码 | 渠道销售员工 | 未使用 | 生成完整渠道码并记录归属 | 否 | 销售个人码停用时失败 |
| 未使用 | 客户支付成功或订阅开通成功 | 客户购买用户 / 系统 | 已使用 | 绑定订单、客户租户和归因信息 | 否 | 支付失败时保持未使用 |
| 未使用 | 超过 10 分钟未使用 | 系统 | 已失效 | 禁止客户继续使用，并释放对应渠道码额度 | 否 | 已使用码不可失效 |
| 已使用 | 再次使用 | 客户购买用户 | 已使用 | 拒绝重复使用 | 否 | 提示渠道码已使用 |

## 6. 功能设计

### 6.1 运营管理后台 / 渠道管理

#### 页面目标

平台运营人员管理渠道主体，决定哪些租户具备渠道销售能力，并维护渠道总码。

#### 功能范围

| 功能 | 说明 | 优先级 |
| ---- | ---- | ------ |
| 渠道列表 | 查看渠道名称、渠道负责人、手机号、关联租户、关联销售、渠道总码、状态 | P0 |
| 新建渠道 | 填写渠道名称、负责人姓名、手机号，关联租户和关联销售 | P0 |
| 编辑渠道 | 修改负责人、关联关系和状态 | P0 |
| 渠道总码 | 每个渠道租户对应一个 6 位总码，用于最终渠道码第一段 | P0 |
| 启用/停用 | 停用后该渠道下员工不能继续生成新的客户渠道码 | P1 |

#### 关键规则

- 渠道总码全局唯一。
- 渠道总码一旦产生渠道码记录，不建议允许修改；如必须修改，应只影响后续新生成渠道码。
- 运营后台不负责给普通销售员工生成客户渠道码。

### 6.2 企业管理后台 / 销售管理

#### 页面目标

渠道租户管理员管理本租户成员的销售个人码。

#### 页面结构

1. 顶部区域：页面标题 `销售管理`，主操作 `新建销售码`。
2. 筛选区：按成员姓名、销售个人码、状态筛选。
3. 列表区：展示员工、销售个人码、状态、生成渠道码数、已使用数、最近使用时间、操作。
4. 弹窗：新建/编辑销售码。

#### 新建销售码字段

| 字段 | 类型 | 必填 | 规则 |
| ---- | ---- | ---- | ---- |
| 绑定成员 | 组织成员选择 | 是 | 只能选择本租户组织内成员 |
| 销售个人码 | 输入或自动生成 | 是 | 4 位数字，同渠道租户内唯一 |
| 状态 | 单选 | 是 | 启用 / 停用 |

#### 关键规则

- 一个成员建议只能有一个启用中的销售个人码。
- 销售个人码停用后，该员工不能生成新的客户渠道码。
- 历史渠道码记录不因销售个人码停用而删除。
- 员工从组织移除后，其销售个人码自动停用，历史记录保留。

### 6.3 大观销售 / 生成渠道码

#### 页面目标

销售员工快速生成可发给客户使用的渠道码。

#### 页面结构

1. 顶部区域：当前销售姓名、所属渠道租户、销售个人码状态。
2. 主操作区：按钮 `生成渠道码`。
3. 结果区：展示最新生成的完整渠道码和复制按钮。
4. 最近生成：展示最近生成的渠道码、生成时间和状态。

#### 生成规则

最终渠道码格式：

```text
渠道总码-销售个人码-动态随机码
```

示例：

```text
XY0001-0012-897654
```

| 组成 | 来源 | 规则 |
| ---- | ---- | ---- |
| 渠道总码 | 运营后台渠道管理 | 6 位，全局唯一，用于溯源渠道公司 |
| 销售个人码 | 企业管理后台销售管理 | 4 位数字，同渠道租户内唯一，用于溯源销售人员 |
| 动态随机码 | 系统生成 | 6 位数字，与前两段组合后完整渠道码全局唯一 |

客户使用渠道码完成购买后，系统将完整渠道码与订单、客户租户 ID 绑定，用于溯源成交客户。

#### 额度规则

1. 渠道码生成成功时立即临时占用 1 个渠道码额度。
2. 渠道码在 10 分钟内未被客户使用，则自动变为已失效并释放 1 个渠道码额度。
3. 渠道码在 10 分钟内被客户支付使用，则状态变为已使用，并永久占用 1 个渠道码额度。
4. 额度计算示例：渠道总额度为 200，销售生成 10 个未使用渠道码后，可用额度为 190；如果这 10 个渠道码 10 分钟内均未使用，则全部失效，可用额度恢复为 200。

#### 异常状态

| 场景 | 页面反馈 |
| ---- | -------- |
| 未绑定销售个人码 | 不展示生成按钮，提示联系管理员开通销售码 |
| 销售个人码停用 | 不允许生成，提示销售码已停用 |
| 渠道总码停用 | 不允许生成，提示渠道状态不可用 |
| 渠道码额度不足 | 不允许生成，提示当前渠道码数量已用完 |
| 生成失败 | 保留页面状态，提示重试 |

### 6.4 大观销售 / 渠道码记录

#### 页面目标

销售员工查看自己生成的渠道码状态。

#### 页面结构

1. 筛选区：状态筛选。
2. 列表区：展示渠道码、状态和创建时间。
3. 空状态：暂无渠道码记录。

#### 列表字段

| 字段 | 说明 |
| ---- | ---- |
| 渠道码 | 完整渠道码 |
| 生成时间 | 销售员工生成渠道码时间 |
| 状态 | 未使用、已使用、已失效 |

## 7. 权限设计

| 功能/数据 | 平台运营人员 | 渠道租户管理员 | 渠道销售员工 | 客户购买用户 |
| --------- | ------------ | -------------- | ------------ | ------------ |
| 运营后台渠道管理 | 是 | 否 | 否 | 否 |
| 企业后台组织管理 | 否 | 是 | 否 | 否 |
| 企业后台角色管理 | 否 | 是 | 否 | 否 |
| 企业后台销售管理 | 否 | 是 | 否 | 否 |
| 大观销售应用入口 | 否 | 可配置 | 是 | 否 |
| 生成客户渠道码 | 否 | 待确认 | 仅本人 | 否 |
| 使用渠道码购买 | 否 | 否 | 否 | 是 |

## 8. 数据对象

### 8.1 渠道

| 字段 | 说明 |
| ---- | ---- |
| 渠道 ID | 系统生成 |
| 渠道名称 | 平台运营维护 |
| 渠道总码 | 渠道码第一段，6 位 |
| 渠道负责人姓名 | 平台运营维护 |
| 渠道负责人手机号 | 平台运营维护 |
| 关联租户 ID | 渠道租户 |
| 关联销售负责人 ID | 平台侧负责人或渠道对接人 |
| 状态 | 启用 / 停用 |

### 8.2 销售个人码

| 字段 | 说明 |
| ---- | ---- |
| 销售码 ID | 系统生成 |
| 租户 ID | 所属渠道租户 |
| 成员 ID | 绑定成员 |
| 销售个人码 | 渠道码第二段，4 位数字 |
| 状态 | 启用 / 停用 |
| 创建人 | 渠道租户管理员 |
| 创建时间 | 系统记录 |

### 8.3 客户渠道码

| 字段 | 说明 |
| ---- | ---- |
| 渠道码 ID | 系统生成 |
| 完整渠道码 | 渠道总码-销售个人码-动态随机码 |
| 渠道总码 | 冗余快照，6 位 |
| 销售个人码 | 冗余快照，4 位数字 |
| 动态随机码 | 系统生成，6 位数字 |
| 生成销售成员 ID | 归因销售 |
| 状态 | 未使用 / 已使用 / 已失效 |
| 关联订单 ID | 客户支付成功后写入 |
| 客户租户 ID | 客户支付成功后写入 |
| 生成时间 | 系统记录 |
| 失效时间 | 生成时间后 10 分钟 |
| 使用时间 | 客户支付成功后写入 |

## 9. 验收标准

| 场景 | 前置条件 | 操作 | 预期结果 | 优先级 |
| ---- | -------- | ---- | -------- | ------ |
| 渠道租户管理员创建销售个人码 | 管理员已进入企业管理后台销售管理 | 选择本租户成员并提交销售码 | 系统生成 4 位销售个人码，并绑定该成员 | P0 |
| 普通销售员工生成渠道码 | 员工已有启用中的销售个人码 | 点击生成渠道码 | 系统生成 `渠道总码-销售个人码-动态随机码` 格式的 6-4-6 位渠道码，例如 `XY0001-0012-897654` | P0 |
| 未绑定销售码员工访问生成页 | 员工没有销售个人码 | 进入大观销售生成渠道码页 | 页面提示联系管理员开通销售码，不允许生成 | P0 |
| 渠道码生成占用额度 | 渠道总额度为 200 | 销售生成 10 个渠道码 | 可用额度变为 190 | P0 |
| 渠道码超时释放额度 | 10 个渠道码生成后 10 分钟内均未使用 | 系统自动更新状态 | 10 个渠道码变为已失效，可用额度恢复为 200 | P0 |
| 客户使用未使用渠道码 | 渠道码存在且未使用 | 客户完成订阅支付 | 渠道码变为已使用，生成订单归因 | P0 |
| 客户重复使用已使用渠道码 | 渠道码已使用 | 客户再次输入该码 | 系统提示渠道码已使用，不允许再次使用 | P0 |

## 10. 原型调整建议

本设计方案确认后，原型建议分三处调整：

1. 应用入口
   - 在登录后的应用选择或平级应用入口中增加 `大观销售`。
   - 仅对具备渠道销售权限的身份展示。

2. 企业管理后台
   - 新增一级菜单 `销售管理`。
   - 包含销售码列表、新建销售码、启停销售码和渠道码归因概览。

3. 大观销售应用
   - 仅保留 `生成渠道码`，主页面展示渠道码记录列表。
   - 页面只呈现真实产品界面，不放方案说明或教学文案。

## 11. 风险与后续建议

| 风险 | 说明 | 建议 |
| ---- | ---- | ---- |
| 销售个人码重复 | 会导致销售归因混乱 | 同渠道租户内唯一，全局可重复但完整渠道码全局唯一 |
| 成员离职或停用 | 历史渠道码记录需要保留 | 成员停用后禁止新生成，历史记录不删除 |
| 渠道总码修改 | 影响历史渠道码识别 | 已有记录后不允许修改，或只影响未来生成 |
| 管理员代生成边界 | 可能影响销售归因真实性 | 默认不做代生成，除非明确业务需要 |
