# Frontis AI · 用户、组织与权限管理设计说明

> 面向对象：客户方业务负责人、企业管理员、IT 管理员、项目评审人员。
> 本文说明 Frontis AI 当前用户、组织与权限管理的产品设计逻辑，帮助客户理解账号如何进入系统、组织成员如何管理、权限如何分配，以及 AI 专家如何按组织范围开放使用。

## 1. 设计目标

Frontis AI 的用户、组织与权限管理围绕企业级使用场景设计，核心目标是：

- 让一个账号可以在不同租户、不同系统中使用对应身份，避免账号和企业归属混乱。
- 让企业管理员可以自行维护部门、成员、角色和权限，不依赖平台运营逐个配置。
- 让平台运营可以管理租户开通、初始管理员、席位和平台级权限边界。
- 让 AI 专家、Skill、MCP、企业后台等能力按角色权限和组织范围开放，保证成员只看到、只使用自己被授权的能力。
- 让成员状态、席位占用和权限变化具备明确规则，便于企业内部管理和审计。

## 2. 整体模型

当前设计中，用户、组织与权限分为六个核心对象：

| 对象 | 含义                                                                       | 管理方                                   |
| ---- | -------------------------------------------------------------------------- | ---------------------------------------- |
| 账号 | 以手机号为基础的登录主体，一个自然人通常对应一个账号                       | 用户本人、平台登录体系                   |
| 租户 | 一个企业或团队在 Frontis AI 中的数据与管理边界                             | 平台运营创建和维护，企业管理员使用和管理 |
| 身份 | 账号在某个租户、某个系统中的进入身份，例如工作台成员、企业管理员、运营人员 | 系统根据租户、角色和权限生成             |
| 组织 | 租户内的部门树和成员关系                                                   | 企业管理员                               |
| 角色 | 一组权限的集合，例如组织管理员、部门负责人、普通成员，也支持自定义角色     | 企业管理员                               |
| 权限 | 控制系统入口、菜单入口和具体操作能力的授权项                               | 平台预置权限清单，企业管理员通过角色分配 |

可以理解为：

```text
账号登录
  -> 选择租户
    -> 选择/匹配当前身份
      -> 根据角色获得权限
        -> 根据权限进入系统和操作功能
          -> 根据组织范围使用 AI 专家等业务能力
```

## 3. 系统分工

Frontis AI 当前分为平台运营侧、企业管理侧和工作台使用侧三层。

### 3.1 平台运营侧

平台运营侧负责租户级开通和平台级控制，主要包括：

- 创建租户，维护租户名称、编码、行业、部署形态、计费方式、版本和状态。
- 设置租户初始管理员，包括管理员姓名、手机号、初始角色和权限范围。
- 管理租户席位、积分、订阅、订单等权益。
- 启用或停用租户。
- 管理平台级商品、资源、AI 专家上架审核和运营配置。

平台运营侧解决的是“这个企业是否开通、开通了哪些能力、初始管理员是谁、权益是否有效”的问题。

### 3.2 企业管理侧

企业管理侧由客户自己的企业管理员使用，负责租户内部管理，主要包括：

- 维护部门结构。
- 邀请或添加成员。
- 编辑成员资料、调整所属部门、分配角色。
- 启用、禁用或移除成员。
- 创建和维护角色。
- 给角色配置工作台、企业管理后台等权限。
- 配置 AI 专家对全公司、部门或个人开放。

企业管理侧解决的是“企业内部谁能用、能用什么、能管理什么”的问题。

### 3.3 工作台使用侧

工作台使用侧面向普通成员、部门负责人和管理员中的实际使用者，主要包括：

- 使用 ME 和已授权 AI 专家。
- 从专家广场或企业专区添加 AI 专家到工作台。
- 使用被授权的 Skill、MCP、开发空间等能力。
- 查看和继续自己的对话记录、成果文件和相关工作内容。

工作台使用侧解决的是“成员在日常工作中能看到哪些入口、能调用哪些 AI 能力”的问题。

## 4. 账号与身份管理

### 4.1 一个账号可以有多个身份

Frontis AI 使用手机号作为登录账号基础。一个账号可以同时拥有多个身份，例如：

- 在 A 公司是普通成员。
- 在 B 公司是企业管理员。
- 同时拥有工作台入口和企业管理后台入口。
- 平台运营人员拥有独立的运营管理后台身份。

用户登录后，系统会根据账号拥有的身份判断进入路径：

- 如果只有一个可用身份，直接进入对应系统。
- 如果拥有多个租户身份，先选择租户。
- 如果当前租户下有多个系统入口，可在账户菜单中切换系统。
- 多租户账号可以在账户菜单中切换租户。

### 4.2 身份决定进入哪个系统

身份不是单纯的“角色名称”，而是由以下信息共同决定：

- 当前账号。
- 当前租户。
- 当前系统入口，例如工作台、企业管理后台、运营管理后台。
- 当前角色和权限。

因此，同一个手机号在不同租户中的权限可以不同；同一个人在工作台和企业管理后台中的可见功能也可以不同。

### 4.3 登录与准入

当前设计支持手机号验证码登录和密码登录。登录后系统会校验：

- 账号是否存在或是否允许自动注册。
- 用户是否已确认用户协议和隐私协议。
- 当前账号是否有可进入的身份。
- 当前身份是否具备目标系统入口权限。
- 当前成员状态是否可用。

被禁用的成员不能继续登录和使用工作台。

## 5. 租户与组织管理

### 5.1 租户是企业数据边界

租户是 Frontis AI 的企业级数据边界。组织、成员、角色、AI 专家资产、订单、积分、席位、对话记录和用量统计都归属到具体租户。

多租户账号切换租户后，系统按当前租户加载对应数据，不混用其他租户的组织和权限。

### 5.2 部门结构

每个租户都有一棵组织树：

- 根节点代表当前企业。
- 根节点下可以创建部门和子部门。
- 部门可以设置负责人。
- 成员必须归属到某个部门。
- 成员可以在部门之间移动。

部门管理规则：

- 一级根部门不可删除。
- 有子部门的部门不可直接删除。
- 有成员的部门不可直接删除。
- 删除部门前需要先处理子部门和成员归属。

### 5.3 成员生命周期

成员生命周期包括添加、启用、禁用和移除。

| 状态/动作 | 规则                                                                         |
| --------- | ---------------------------------------------------------------------------- |
| 添加成员  | 企业管理员填写姓名、手机号、所属部门和角色；团队版租户且有可用席位时才可添加 |
| 已启用    | 成员可以登录并使用自己被授权的系统和功能                                     |
| 已禁用    | 成员不能继续登录和使用工作台，但仍保留在组织中                               |
| 重新启用  | 恢复成员登录和使用能力，不重复占用席位                                       |
| 移除成员  | 成员从当前租户组织中移除，并释放 1 个有效席位额度                            |

### 5.4 席位与成员关系

组织成员管理与团队席位联动：

- 租户归属人拥有长期免费席位。
- 除租户归属人外，其他成员占用付费席位。
- 添加成员会占用 1 个有效席位。
- 禁用成员不释放席位。
- 移除成员释放席位。
- 当前有效席位不足时，不能继续添加成员。
- 企业管理员可以通过团队扩充或运营分配增加席位。

这套规则保证企业成员数量、已购权益和实际可用人员保持一致。

## 6. 角色与权限管理

### 6.1 角色是权限集合

角色用于把一组权限打包分配给成员。成员不直接逐项配置权限，而是通过角色获得权限。

当前内置角色包括：

| 角色       | 默认定位                                                    |
| ---------- | ----------------------------------------------------------- |
| 组织管理员 | 进入工作台和企业管理后台，负责组织、角色、AI 专家和后台管理 |
| 部门负责人 | 进入工作台和受限管理后台，负责部门成员与团队 AI 专家配置    |
| 普通成员   | 进入工作台，使用被授权的 ME、AI 专家、Skill 和相关能力      |

除内置角色外，企业管理员也可以创建自定义角色，并为角色选择权限项。

### 6.2 权限分组

当前权限按系统和业务模块组织，便于管理员理解和配置。

| 权限分组         | 控制范围                                                                                    |
| ---------------- | ------------------------------------------------------------------------------------------- |
| 工作台权限       | 控制 AI 专家使用、专家广场、Skill/MCP、开发空间等能力                                       |
| 企业管理后台权限 | 控制驾驶舱、订单记录、AI 专家管理、组织管理、角色管理等入口                                 |
| 运营管理平台权限 | 控制租户管理、商品中心、资源池、积分和订阅运营、订单中心、AI 专家审核、运营配置等平台级能力 |

企业客户日常主要使用工作台权限和企业管理后台权限。运营管理平台权限主要由平台运营人员或特定租户运营身份使用。

### 6.3 权限如何生效

权限在三个层面生效：

1. **系统入口**：是否能进入工作台、企业管理后台或运营管理后台。
2. **菜单入口**：进入系统后，是否显示某个模块菜单。
3. **操作按钮**：在页面内是否允许新增、编辑、删除、分配、启停等具体动作。

例如：

- 没有企业管理后台权限的成员，不能进入企业管理后台。
- 没有组织管理权限的成员，不能维护部门和成员。
- 没有角色管理权限的成员，不能创建角色或给成员分配角色。
- 没有 AI 专家上架申请权限的成员，不能提交 AI 专家进入专家广场的上架申请。

### 6.4 角色维护规则

角色管理支持：

- 创建角色。
- 编辑角色名称。
- 勾选角色权限。
- 查看角色下的成员。
- 删除无成员使用的角色。

角色删除规则：

- 角色下仍有成员时不可删除。
- 角色至少需要包含一项权限。
- 修改角色权限后，绑定该角色的成员按新权限生效。

## 7. AI 专家的组织范围授权

角色权限解决的是“用户能不能进入系统、能不能操作功能”；AI 专家的组织范围授权解决的是“某个 AI 专家开放给哪些人使用”。

### 7.1 授权主体

AI 专家支持按以下主体授权：

| 授权主体 | 含义                           |
| -------- | ------------------------------ |
| 全公司   | 当前租户全部成员可使用         |
| 部门     | 指定部门及其下级部门成员可使用 |
| 个人     | 指定成员可使用                 |

### 7.2 授权规则

- 选择全公司时，只保留全公司授权，不再重复保留部门或个人授权。
- 选择父部门时，默认包含其下级部门和成员。
- 已选择父部门时，不需要重复选择其子部门。
- 可以选择多个并列部门。
- 可以混合选择部门和个人。
- 用户是否可使用某个 AI 专家，由该用户是否命中授权范围决定。

### 7.3 与 ME 调度的关系

ME 只能调度当前用户已添加到工作台且有权限使用的 AI 专家。

因此，一个 AI 专家要真正参与用户工作，需要同时满足：

- 该专家对当前租户可见或属于当前企业专区。
- 当前用户已将该专家添加到工作台，并出现在可调度列表中。
- 当前用户命中该专家的组织范围授权。
- 该专家未被下架或停用，且运行所需模型可用。

## 8. 典型流程

### 8.1 平台为企业开通租户

1. 平台运营创建租户。
2. 填写租户基础信息、计费方式、席位和有效期。
3. 设置初始管理员姓名、手机号和管理员角色。
4. 系统生成租户、初始管理员身份和默认组织。
5. 初始管理员登录后进入企业管理后台维护组织、成员和角色。

### 8.2 企业管理员添加成员

1. 企业管理员进入企业管理后台的组织管理。
2. 选择部门并点击添加成员。
3. 填写成员姓名、手机号并选择角色。
4. 系统校验租户是否为团队版、是否有可用席位。
5. 校验通过后，成员加入组织并占用席位。
6. 成员使用手机号登录后，以该租户身份进入被授权系统。

### 8.3 企业管理员配置角色

1. 企业管理员进入角色管理。
2. 创建或编辑角色。
3. 勾选工作台、企业管理后台等权限。
4. 保存角色。
5. 在组织管理中把成员分配到该角色。
6. 成员重新进入系统后，按角色权限看到对应入口和操作。

### 8.4 企业管理员开放 AI 专家

1. 企业管理员进入 AI 专家管理。
2. 选择某个 AI 专家并进入权限配置。
3. 选择全公司可用，或按部门/个人配置可用范围。
4. 保存后，命中范围的成员可以使用该 AI 专家。
5. 未命中范围的成员无法看到或无法使用该 AI 专家。

### 8.5 成员离职或暂时停用

如果成员只是暂时不允许使用系统：

1. 企业管理员将成员状态改为禁用。
2. 成员不能继续登录和使用工作台。
3. 席位仍被占用，便于后续重新启用。

如果成员已离开组织：

1. 企业管理员移除成员。
2. 成员从当前租户组织中移除。
3. 系统释放该成员占用的席位。

## 9. 权限矩阵摘要

| 能力                     | 平台运营             | 企业管理员           | 部门负责人     | 普通成员   |
| ------------------------ | -------------------- | -------------------- | -------------- | ---------- |
| 创建和停用租户           | 支持                 | 不支持               | 不支持         | 不支持     |
| 设置租户初始管理员       | 支持                 | 不支持               | 不支持         | 不支持     |
| 分配或续约租户席位       | 支持                 | 可自助扩充或查看权益 | 不支持         | 不支持     |
| 维护企业部门             | 不负责企业内日常维护 | 支持                 | 视角色权限支持 | 不支持     |
| 添加、禁用、移除成员     | 不负责企业内日常维护 | 支持                 | 视角色权限支持 | 不支持     |
| 创建和配置角色           | 不负责企业内日常维护 | 支持                 | 视角色权限支持 | 不支持     |
| 查看企业驾驶舱           | 不负责企业内日常维护 | 支持                 | 视角色权限支持 | 不支持     |
| 配置 AI 专家组织范围     | 不负责企业内日常维护 | 支持                 | 视角色权限支持 | 不支持     |
| 使用 ME 和已授权 AI 专家 | 不作为企业成员使用   | 支持                 | 支持           | 支持       |
| 提交 AI 专家上架申请     | 平台侧审核           | 视权限支持           | 视权限支持     | 视权限支持 |

说明：部门负责人和普通成员不是固定死权限，实际能力取决于企业管理员给其绑定的角色权限。

## 10. 安全与边界规则

当前设计遵循以下边界：

- 租户之间数据隔离，账号切换租户后按当前租户加载组织、角色和业务数据。
- 系统入口由身份和权限共同控制，不仅靠角色名称判断。
- 角色只负责功能权限，AI 专家是否可用还要看组织范围授权。
- 成员禁用不释放席位，移除才释放席位。
- 部门删除必须先处理子部门和成员，避免组织结构断裂。
- 角色删除必须先移除角色下成员，避免成员权限无归属。
- AI 专家按全公司、部门、个人授权；部门授权默认包含下级部门。
- 平台运营账号与企业成员账号职责分离，平台运营负责租户和平台能力，企业管理员负责租户内部组织和权限。

## 11. 客户可理解的总结

Frontis AI 的用户、组织和权限管理可以概括为：

- **账号解决“谁登录”**：用户用手机号登录，一个账号可以拥有多个租户和系统身份。
- **租户解决“属于哪家公司”**：每个企业的数据、成员、角色、权益和 AI 资产按租户隔离。
- **组织解决“人在公司哪里”**：企业管理员维护部门树、成员和部门负责人。
- **角色解决“这个人能做什么”**：角色是一组权限，成员通过角色获得系统入口和功能操作能力。
- **组织范围解决“AI 专家给谁用”**：AI 专家可以开放给全公司、部门或个人。
- **席位解决“最多能有多少可用成员”**：添加成员占用席位，禁用不释放，移除释放。

这套设计的重点是把平台级开通、企业内部管理和用户日常使用拆开管理：平台运营不介入企业内部日常权限，企业管理员可以自主维护组织和角色，普通成员只使用自己被授权的 AI 能力。
