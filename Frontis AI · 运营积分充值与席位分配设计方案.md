# Frontis AI · 运营积分充值与席位分配设计方案

## 1. 文档信息

| 项目 | 内容 |
| ---- | ---- |
| 文档类型 | 产品设计方案 |
| 版本 | V0.1 |
| 日期 | 2026-05-29 |
| 状态 | 草案 |
| 适用范围 | 运营管理后台、租户管理、积分余额、席位管理、订单中心、企业管理后台组织管理 |

## 2. 背景与目标

### 2.1 业务背景

Frontis AI 当前已有线上积分购买和团队席位购买能力，但实际销售过程中存在大量线下成交场景。客户在线下签约或由商务确认权益后，需要运营人员在后台快速给客户租户配置积分和席位。

这类需求不能通过上传合同、审批流或单独合同模块来解决；当前阶段的目标是最小可上线闭环：运营可以按线下成交结果，把积分和席位准确配置到租户，并让系统沉淀可追溯记录。

### 2.2 产品目标

1. 租户创建时不再让运营填写席位数量，避免创建动作和商业权益开通混在一起。
2. 租户创建完成后，系统自动给初始管理员分配一个默认长期有效席位。
3. 运营后续给租户加席位时，必须从团队席位包规格中分配，不直接改席位字段。
4. 运营给租户加积分时，走运营充值能力，支持任意积分数量，不强依赖积分包。
5. 所有运营开通动作都必须沉淀流水、订单或操作日志，保证可追溯。

### 2.3 设计原则

| 原则 | 说明 |
| ---- | ---- |
| 积分和席位分开设计 | 积分是高频、数量灵活的充值动作；席位是低频、带订阅和组织容量属性的权益动作 |
| 租户创建不承载商业权益 | 创建租户只建立租户、管理员和默认管理员席位，不配置客户购买席位 |
| 席位必须走席位包 | 后续给客户开通席位时，运营选择席位包规格分配席位，保证商品和订阅口径一致 |
| 积分支持任意运营充值 | 运营可以按线下合同、赠送、补偿等场景输入任意积分数量 |
| 记录必须闭环 | 积分生成流水，席位生成订阅权益和订单，所有动作写入操作日志 |

## 3. 用户角色与场景

| 角色 | 核心目标 | 使用场景 | 权限边界 |
| ---- | -------- | -------- | -------- |
| 平台运营人员 | 给线下成交租户配置权益 | 创建租户、给租户充值积分、给租户分配席位包 | 可操作全部租户，但必须有运营后台租户管理权限 |
| 租户初始管理员 | 使用默认席位登录并管理租户 | 租户创建后首次登录、进入企业管理后台 | 默认占用系统初始化席位 |
| 租户管理员 | 使用已分配席位邀请成员 | 运营分配席位后，在组织管理中邀请成员 | 可邀请人数受总可用席位限制 |
| 财务/运营复盘人员 | 追溯线下开通记录 | 查看订单中心、积分流水、操作日志 | 只读查看，不直接变更权益 |

## 4. 总体方案

本方案拆成三条链路：

| 链路 | 入口 | 核心动作 | 是否商品化 | 是否生成订单 |
| ---- | ---- | -------- | ---------- | ------------ |
| 租户创建默认席位 | 运营后台 / 租户管理 / 新建租户 | 系统给初始管理员分配 1 个默认长期有效席位 | 否 | 否 |
| 运营充值积分 | 运营后台 / 租户详情 / 积分余额 / 运营充值 | 运营输入积分数量并给租户充值 | 否，不依赖积分包 | 生成运营充值记录，可同步为 0 元已支付积分订单 |
| 运营分配席位 | 运营后台 / 租户详情 / 席位管理 / 分配席位 | 运营选择席位包规格并分配席位数量 | 是，必须走席位包规格 | 是，生成已支付订阅订单 |

重要口径：

- 积分不统一包装成权益包。
- 席位不在创建租户时填写数量。
- 默认管理员席位是系统初始化权益，不是席位包。
- 后续所有新增席位都通过席位包分配。

## 5. 租户创建默认席位

### 5.1 页面入口

`运营管理后台 > 租户管理 > 新建租户`

### 5.2 创建字段

新建租户保留以下字段：

| 字段 | 是否必填 | 说明 |
| ---- | -------- | ---- |
| 租户名称 | 是 | 客户租户名称 |
| 租户编码 | 否 | 可系统生成或运营填写，按现有规则执行 |
| 初始管理员姓名 | 是 | 创建租户的首个管理员 |
| 初始管理员手机号 | 是 | 用于创建或绑定账号 |
| 计费方式 | 是 | 积分计费 / 成本计费 |
| 初始化权限 | 是 | 工作台、企业管理后台等能力 |

新建租户不展示以下字段：

| 字段 | 原因 |
| ---- | ---- |
| 席位数量 | 创建动作不承载商业席位配置 |
| 席位包 | 创建后再通过租户详情分配 |
| 到期时间 | 默认管理员席位长期有效，不需要配置 |
| 订阅周期 | 创建不是订阅购买 |
| 合同金额 | 线下金额在后续积分充值或席位分配中记录 |

### 5.3 系统行为

运营提交新建租户后，系统自动完成：

| 对象 | 结果 |
| ---- | ---- |
| 租户 | 创建成功 |
| 初始管理员账号 | 创建或绑定账号，并加入该租户 |
| 默认管理员席位 | 创建 1 个长期有效席位，并自动分配给初始管理员 |
| 商品席位 | 不创建 |
| 订阅订单 | 不生成 |
| 操作日志 | 记录租户创建和默认管理员席位初始化 |

### 5.4 默认管理员席位规则

| 规则 | 说明 |
| ---- | ---- |
| 数量 | 固定 1 个 |
| 有效期 | 长期有效 |
| 使用人 | 初始管理员 |
| 来源 | 系统初始化 |
| 是否可编辑 | 不允许在新建租户时编辑 |
| 是否属于席位包 | 否 |
| 是否生成订单 | 否 |
| 到期影响 | 不受后续席位包到期影响 |

### 5.5 租户创建后的席位状态

示例：

| 状态项 | 数值 |
| ------ | ---- |
| 默认管理员席位 | 1 |
| 商品席位 | 0 |
| 总可用席位 | 1 |
| 已用席位 | 1 |
| 可邀请席位 | 0 |

## 6. 运营充值积分

### 6.1 页面入口

`运营管理后台 > 租户管理 > 租户详情 > 积分余额 > 运营充值`

### 6.2 适用场景

| 场景 | 说明 |
| ---- | ---- |
| 线下合同购买积分 | 客户线下付款后，由运营按合同约定充值积分 |
| 商务赠送 | 商务给重点客户赠送积分 |
| 运营补偿 | 因故障、活动或服务补偿发放积分 |
| 测试支持 | 给演示或测试租户发放临时积分 |

### 6.3 操作字段

| 字段 | 是否必填 | 规则 |
| ---- | -------- | ---- |
| 充值积分 | 是 | 正整数，运营手动输入 |
| 来源类型 | 是 | 线下合同 / 商务赠送 / 运营补偿 / 测试支持 / 其他 |
| 线下金额 | 否 | 默认 0，可记录线下实收金额 |
| 外部单号 | 否 | 可填写合同号、收款单号或内部审批号 |
| 备注 | 否 | 操作说明 |

### 6.4 提交校验

| 校验项 | 规则 |
| ------ | ---- |
| 租户状态 | 租户存在且未停用 |
| 充值积分 | 必须大于 0 |
| 线下金额 | 不填默认为 0，填写时不能小于 0 |
| 来源类型 | 必选 |
| 重复提交 | 提交中禁用按钮，避免重复发放 |

### 6.5 系统处理

提交成功后，系统执行：

1. 租户积分余额增加。
2. 生成积分流水，方向为 `收入`。
3. 生成运营充值记录。
4. 可同步生成一条积分订单，订单状态为 `已支付`，支付渠道为 `线下/运营开通`。
5. 写入操作日志。

### 6.6 积分流水字段

| 字段 | 示例 |
| ---- | ---- |
| 流水方向 | 收入 |
| 来源 | 运营充值 |
| 来源类型 | 线下合同 |
| 积分数量 | +100000 |
| 操作人 | 当前运营账号 |
| 操作时间 | 2026-05-29 10:30:00 |
| 外部单号 | HT-20260529-001 |
| 备注 | 线下合同首充 |

### 6.7 积分订单字段

| 字段 | 示例 |
| ---- | ---- |
| 订单类型 | 积分订单 |
| 订单内容 | 运营充值积分 |
| 权益明细 | 到账 100000 积分 |
| 支付金额 | 0 或线下金额 |
| 订单状态 | 已支付 |
| 订单来源 | 运营后台开通 |
| 支付渠道 | 线下/运营开通 |
| 购买人 | - |
| 操作人 | 当前登录运营账号 |
| 关联租户 | 目标租户 |
| 外部单号 | HT-20260529-001 |

订单中心来源只展示两个值：用户自助购买、运营后台开通。运营充值积分统一归为运营后台开通，不展示线下订单、运营后台充值等过程性来源。

### 6.8 积分与积分包边界

| 项 | 线上积分包 | 运营充值 |
| -- | ---------- | -------- |
| 使用方 | 租户用户 | 平台运营 |
| 数量 | 按积分包配置 | 运营任意输入 |
| 用户侧可见 | 是 | 否 |
| 是否需要支付 | 用户线上支付 | 不走线上支付 |
| 是否依赖商品中心积分包 | 是 | 否 |
| 是否生成流水 | 是 | 是 |

积分高频且数量不确定，因此运营充值不通过 0 元积分包实现。

## 7. 运营分配席位

### 7.1 页面入口

`运营管理后台 > 租户管理 > 租户详情 > 席位管理 > 分配席位`

### 7.2 适用场景

| 场景 | 说明 |
| ---- | ---- |
| 线下合同购买席位 | 客户线下签约后，运营按合同给租户开通席位 |
| 商务赠送席位 | 给客户赠送一定数量席位 |
| 内部测试席位 | 给演示或测试租户开通席位 |

### 7.3 操作字段

| 字段 | 是否必填 | 规则 |
| ---- | -------- | ---- |
| 选择席位包规格 | 是 | 读取商品中心启用中的团队席位包规格 |
| 分配席位数 | 是 | 正整数，数量任意 |
| 有效期 | 是 | 长期有效 / 指定到期日期 |
| 到期日期 | 条件必填 | 选择指定到期日期时必填 |
| 线下金额 | 否 | 默认 0，可记录线下实收金额 |
| 外部单号 | 否 | 可填写合同号、收款单号或内部审批号 |
| 备注 | 否 | 操作说明 |

### 7.4 提交校验

| 校验项 | 规则 |
| ------ | ---- |
| 租户状态 | 租户存在且未停用 |
| 席位包规格 | 必须启用 |
| 分配席位数 | 必须大于 0 |
| 到期日期 | 选择指定日期时必须晚于当前日期 |
| 线下金额 | 不填默认为 0，填写时不能小于 0 |
| 成本计费租户 | 可按业务策略决定是否允许开通，默认建议允许运营开通但用户侧不展示自助购买入口 |

### 7.5 系统处理

提交成功后，系统执行：

1. 生成席位包权益记录。
2. 增加租户商品席位数量。
3. 更新企业管理后台组织管理的可邀请席位。
4. 生成订阅订单，状态为 `已支付`。
5. 支付渠道记录为 `线下/运营开通`。
6. 保存席位包规格快照、分配数量、有效期、操作人和备注。
7. 写入操作日志。

### 7.6 席位包规格快照

运营分配席位必须引用当时的席位包规格，并保存快照：

| 字段 | 说明 |
| ---- | ---- |
| 席位包 ID | 商品中心团队席位包 |
| 规格 ID | 月付 / 年付 / 其他启用规格 |
| 规格名称 | 当时规格名称 |
| 计费周期 | 当时规格周期 |
| 线上单价 | 当时线上单价 |
| 分配席位数 | 本次分配数量 |
| 有效期 | 长期有效或到期日期 |
| 线下金额 | 本次记录金额 |

### 7.7 订阅订单字段

| 字段 | 示例 |
| ---- | ---- |
| 订单类型 | 订阅订单 |
| 订单内容 | 运营分配团队席位 |
| 权益明细 | 分配 20 个席位 |
| 支付金额 | 0 或线下金额 |
| 订单状态 | 已支付 |
| 订单来源 | 运营后台开通 |
| 支付渠道 | 线下/运营开通 |
| 购买人 | - |
| 操作人 | 当前登录运营账号 |
| 关联租户 | 目标租户 |
| 外部单号 | HT-20260529-002 |

订单中心来源只展示两个值：用户自助购买、运营后台开通。运营分配席位统一归为运营后台开通，不展示线下订单、运营后台分配席位等过程性来源。

## 8. 席位计算规则

### 8.1 席位来源

| 来源 | 是否商品席位 | 是否长期有效 | 说明 |
| ---- | ------------ | ------------ | ---- |
| 默认管理员席位 | 否 | 是 | 租户创建时系统自动分配给初始管理员 |
| 用户线上购买席位 | 是 | 否 | 工作台团队扩充购买产生 |
| 运营分配席位 | 是 | 取决于运营配置 | 运营选择席位包规格分配 |

### 8.2 计算公式

总可用席位：

```text
总可用席位 = 默认管理员席位 1 + 有效商品席位
```

已用席位：

```text
已用席位 = 当前租户内占用席位的启用成员数
```

可邀请席位：

```text
可邀请席位 = 总可用席位 - 已用席位
```

### 8.3 示例

| 场景 | 默认管理员席位 | 商品席位 | 总可用席位 | 已用席位 | 可邀请席位 |
| ---- | -------------- | -------- | ---------- | -------- | ---------- |
| 租户刚创建 | 1 | 0 | 1 | 1 | 0 |
| 运营分配 10 个席位 | 1 | 10 | 11 | 1 | 10 |
| 管理员邀请 3 个成员 | 1 | 10 | 11 | 4 | 7 |
| 商品席位到期 | 1 | 0 | 1 | 4 | -3，需要限制新增邀请并提示席位不足 |

### 8.4 到期处理

| 场景 | 系统处理 |
| ---- | -------- |
| 默认管理员席位 | 永不过期 |
| 商品席位到期 | 从有效商品席位中移除 |
| 到期后已用席位超过总可用席位 | 保留现有成员登录，禁止继续邀请；组织管理提示席位不足 |
| 运营再次分配席位 | 有效商品席位增加，组织邀请能力恢复 |

## 9. 页面与交互设计

### 9.1 租户创建页

页面目标：创建租户和初始管理员，不配置商业席位。

关键变化：

- 移除席位数量输入。
- 移除到期时间输入。
- 提交成功后展示提示：`已创建租户，并为初始管理员分配 1 个长期有效默认席位。`

### 9.2 租户详情 / 积分余额

新增按钮：`运营充值`

页面展示：

| 信息 | 说明 |
| ---- | ---- |
| 当前积分余额 | 当前租户余额 |
| 最近充值记录 | 展示最近运营充值和用户购买到账 |
| 运营充值按钮 | 打开充值弹窗 |

充值弹窗结构：

1. 当前租户信息。
2. 充值积分、来源类型、线下金额、外部单号、备注。
3. 生效预览：当前余额、新增积分、生效后余额。
4. 确认提交。

### 9.3 租户详情 / 席位管理

新增按钮：`分配席位`

页面展示：

| 信息 | 说明 |
| ---- | ---- |
| 默认管理员席位 | 固定 1 个，长期有效 |
| 商品席位 | 用户购买和运营分配的有效席位合计 |
| 总可用席位 | 默认管理员席位 + 有效商品席位 |
| 已用席位 | 已占用席位成员数 |
| 可邀请席位 | 总可用席位 - 已用席位 |
| 最近席位记录 | 用户购买、续费、运营分配记录 |

分配席位弹窗结构：

1. 当前席位状态。
2. 选择席位包规格。
3. 分配席位数、有效期、线下金额、外部单号、备注。
4. 生效预览：当前总席位、分配席位、生效后总席位、可邀请席位。
5. 确认提交。

## 10. 权限与数据范围

| 功能 | 平台超级管理员 | 运营人员 | 租户管理员 | 租户成员 |
| ---- | -------------- | -------- | ---------- | -------- |
| 创建租户 | 是 | 是，按权限 | 否 | 否 |
| 运营充值积分 | 是 | 是，按权限 | 否 | 否 |
| 运营分配席位 | 是 | 是，按权限 | 否 | 否 |
| 查看积分流水 | 是 | 是 | 可查看本租户 | 否 |
| 查看订单记录 | 是 | 是 | 可查看本租户 | 否 |
| 邀请成员 | 否 | 可代管时允许 | 是，受席位限制 | 否 |

操作日志至少记录：

| 字段 | 说明 |
| ---- | ---- |
| 操作类型 | 创建租户 / 运营充值 / 分配席位 |
| 操作人 | 当前登录运营账号 |
| 目标租户 | 被操作租户 |
| 操作前 | 余额、席位等关键数据 |
| 操作后 | 余额、席位等关键数据 |
| 操作时间 | 时间戳 |
| 外部单号 | 合同号或内部单号 |
| 备注 | 操作说明 |

## 11. 状态规则

### 11.1 积分充值状态

| 当前状态 | 触发动作 | 下一个状态 | 系统动作 |
| -------- | -------- | ---------- | -------- |
| 未提交 | 运营填写并确认 | 已完成 | 增加余额、生成流水、生成记录 |
| 已完成 | 查看记录 | 已完成 | 只读展示，不允许删除 |

P0 不做撤销。需要修正时，通过新增一笔负向调整或后续“运营扣减”能力处理。

### 11.2 席位分配状态

| 当前状态 | 触发动作 | 下一个状态 | 系统动作 |
| -------- | -------- | ---------- | -------- |
| 未分配 | 运营确认分配 | 生效中 | 增加商品席位、生成订单 |
| 生效中 | 到期 | 已到期 | 商品席位不再计入有效席位 |
| 生效中 | 运营再次分配 | 生效中 | 新增一笔权益记录，不覆盖历史 |

P0 不做撤销、退款和复杂变更。

## 12. 与现有模块关系

| 模块 | 调整点 |
| ---- | ------ |
| 租户管理 | 新建租户移除席位数量；租户详情新增运营充值、分配席位 |
| 商品中心 / 积分包 | 不用于运营充值，继续服务用户线上购买 |
| 商品中心 / 团队席位包 | 继续作为用户购买和运营分配席位的商品规格来源 |
| 工作台 / 积分购买 | 只展示线上积分包，不展示运营充值入口 |
| 工作台 / 团队扩充 | 只展示用户可购买席位规格，不展示运营分配动作 |
| 企业管理后台 / 组织管理 | 读取总可用席位、已用席位和可邀请席位 |
| 企业管理后台 / 订单记录 | 可查看本租户由运营生成的已支付积分订单和订阅订单 |
| 运营后台 / 订单中心 | 查看运营充值积分订单和运营分配席位订阅订单 |

## 13. 验收标准

| 场景 | 前置条件 | 操作 | 预期结果 | 优先级 |
| ---- | -------- | ---- | -------- | ------ |
| 创建租户不填席位 | 运营有创建租户权限 | 新建租户并提交 | 页面无席位数量字段；系统自动给初始管理员分配 1 个长期有效默认席位 | P0 |
| 默认席位被管理员占用 | 租户创建成功 | 查看租户席位状态 | 总可用席位为 1，已用席位为 1，可邀请席位为 0 | P0 |
| 运营充值积分 | 租户存在 | 输入 100000 积分并提交 | 租户积分余额增加 100000，生成积分流水和操作日志 | P0 |
| 运营充值记录可追溯 | 已完成运营充值 | 查看订单中心或租户积分流水 | 订单中心来源为运营后台开通，租户积分流水可看到运营充值记录、操作人和备注 | P0 |
| 运营分配席位 | 租户存在，席位包规格启用 | 选择席位包并分配 10 个席位 | 租户商品席位增加 10，总可用席位变为默认 1 + 商品 10 | P0 |
| 组织邀请读取席位 | 租户已被分配席位 | 租户管理员进入组织管理 | 可邀请席位按总可用席位减已用席位计算 | P0 |
| 席位分配生成订单 | 已完成席位分配 | 查看订单中心 | 生成已支付订阅订单，订单来源为运营后台开通，购买人为空，操作人为当前登录运营账号 | P0 |
| 用户侧不可见运营动作 | 租户用户登录工作台 | 查看积分购买和团队扩充 | 不出现运营充值和运营分配入口 | P0 |

## 14. 非本期范围

| 能力 | 说明 |
| ---- | ---- |
| 合同文件上传 | 当前不做线上合同文件管理 |
| 审批流 | 当前不做运营开通审批 |
| 发票与回款 | 当前不做财务回款和发票管理 |
| 退款 | 当前不做线下权益退款 |
| 运营扣减积分 | P0 只做充值，扣减作为后续修正能力 |
| 席位撤销 | P0 不做撤销，通过到期或后续调整能力处理 |
| 多商品合同明细 | 当前不做合同级多商品报价结构 |

## 15. 待确认问题

| 问题 | 影响 | 建议 |
| ---- | ---- | ---- |
| 运营充值是否必须生成积分订单 | 影响订单中心口径 | 建议生成，状态为已支付，订单来源为运营后台开通，支付渠道为线下/运营开通 |
| 成本计费租户是否允许运营充值积分 | 影响成本计费客户展示 | 建议允许记录但用户侧不展示积分余额，需按计费方式控制展示 |
| 运营分配席位是否允许长期有效 | 影响到期与续费规则 | 建议允许，满足内部测试和特殊商务场景 |
| 线下金额是否必填 | 影响运营录入效率 | 建议非必填，默认 0 |
| 外部单号是否唯一 | 影响重复发放识别 | P0 可不强校验，后续可按来源类型增加唯一校验 |

