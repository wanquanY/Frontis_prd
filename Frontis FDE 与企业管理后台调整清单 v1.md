# Frontis FDE 与企业管理后台调整清单 v1

> **用途：** 将 2026 年 4 月 1 日会议记录整理为当前原型需要调整的事项列表。  
> **范围：** 仅聚焦 `FDE 端` 与 `企业管理后台`，忽略营销门户。  
> **关联记录：**  
> `FDE业务流程及功能配置讨论.txt`  
> `FDE系统配置及功能规划.txt`  
> `产品功能及组织管理规划.txt`

---

## 1. 会议结论摘要

本次会议对产品职责边界进行了重新收口，核心结论如下：

1. `线索 -> 成单` 前的流转先不固化在系统中，改由飞书多维表格承接。
2. `FDE 端` 不再承担复杂的商机流程管理，重点保留成单后的交付和客户持续运营查看。
3. `企业管理后台` 才是企业正式配置面的承载端，FDE 只是代企业完成前期配置。
4. 老板驾驶舱要弱化复杂统计，强化结果感和管理感。
5. Agent、设备、模型、权限的正式控制权在企业管理员，不在普通员工。
6. 组织架构先做手工维护版，不接飞书、企微、钉钉同步。
7. 版本升级必须有“可升级提示 + 推送升级 + 忽略升级”的完整闭环。

---

## 2. FDE 端调整清单

## 2.1 信息架构调整

1. 下线 `商机工作台` 作为正式系统能力。
2. 下线 `线索工单` 的系统内流转能力，改为“说明 + 跳转多维表格”。
3. 下线 `数据回流看板` 作为 FDE 业务侧一级导航。
4. 下线 `进化任务管理` 作为 FDE 业务侧一级导航。
5. FDE 一级导航收口为两个主模块：
   `配置交付`
   `客户资产管理`
6. `运营监控` 建议重命名为 `客户资产管理`。
7. Agent 开发、Skill 开发、数据回流、进化任务统一改成“开发域入口”，不在 FDE 业务工作台内展开。

## 2.2 线索与成单边界

1. 门户提交的需求表单先进入飞书多维表格。
2. 非门户来源线索也允许在多维表格中直接新增。
3. 线索在多维表格内完成指派、备注、跟进和状态更新。
4. 线索成单后，再自动同步进入 FDE 系统。
5. FDE 系统只接“已成单客户”，不再承接成单前的节点设计。

## 2.3 配置交付模块调整

1. `配置交付` 只处理成单后的正式交付单。
2. 交付流程至少包含：
   创建租户
   分配设备
   下发 Agent
   初始化测试
   完成交付
3. 当前交付逻辑中“先按设备选 Agent”的设计要移除。
4. Agent 应先按租户下发，而不是在 FDE 端先绑定到具体设备。
5. 设备配置和 Agent 下发要解耦。
6. 成员初始化不能依赖 FDE 当场补录，交付前应已有企业员工名单和账号信息。
7. FDE 交付完成后，应跳入企业管理后台完成正式配置。

## 2.4 客户资产管理模块调整

1. `客户资产管理` 改为按客户维度展示，而不是按任务维度展示。
2. 页面建议采用：
   左侧客户列表
   右侧客户详情
3. 右侧客户详情至少包含两个子视图：
   `设备`
   `Agent`
4. 设备视图需要展示：
   设备数量
   在线状态
   异常状态
   激活情况
   所属员工
5. Agent 视图需要展示：
   已下发 Agent
   当前使用状态
   当前企业版本
   Agent Store 最新版本
   是否可升级
6. 客户资产管理需要承接版本查看与版本推送。
7. 客户资产管理需要展示租户下的使用量、模型用量、设备运行情况。

## 2.5 版本管理能力调整

1. 不再把 `版本管理` 当成独立复杂研发域。
2. FDE 业务侧要有 `推送版本管理` 能力。
3. FDE 需要看到：
   客户当前版本
   最新版本
   差异状态
   是否已推送
   客户是否接受升级
4. 版本推送管理要和客户资产管理联动。
5. 进化和开发动作仍归属于 Agent 开发域，不归 FDE 业务侧。

## 2.6 设备与激活机制调整

1. 设备要明确区分三类：
   盒子
   VPS / 云节点
   员工本地设备
2. 员工本地设备必须与员工账号绑定。
3. 支持“一个员工多台设备”。
4. 短期保留激活码模式。
5. 中长期改为账号登录激活模式。
6. 即使保留激活码，也必须让管理员控制是否允许激活。
7. 设备归属和激活状态都要能被 FDE 和企业管理员看到。

## 2.7 FDE 角色与权限调整

1. FDE 角色至少拆为三类：
   平台超管
   FDE 团队管理员 / 负责人
   普通 FDE 工程师
2. FDE 工程师负责跟进和交付。
3. 团队管理员负责分派、查看整体客户和交付状态。
4. 平台超管负责跨团队配置和平台级管理。

---

## 3. 企业管理后台调整清单

## 3.1 驾驶舱调整

1. 驾驶舱维持老板视角，不做复杂经营分析后台风格。
2. 驾驶舱去掉或弱化 Token、费用类复杂指标。
3. 驾驶舱保留以下核心指标：
   完成任务数
   运行时长
   节省工时 / 节省人力
   当前活跃 AI 专家数
   风险提醒 / 异常提醒
4. 驾驶舱表达方式偏结果型和情绪型，而不是偏报表型。

## 3.2 企业后台职责调整

1. 企业管理后台要成为企业正式配置面的主入口。
2. FDE 完成交付后，后续企业配置全部在企业管理后台完成。
3. 后台需要承接：
   设备管理
   Agent 权限管理
   模型配置
   组织管理
   版本升级处理

## 3.3 设备管理调整

1. `设备管理` 支持设备绑定到员工。
2. 设备管理明确区分：
   公司统管设备
   员工本地设备
3. 本地设备激活后，必须能看到对应员工归属。
4. 支持设备换人后的归属变更。
5. 支持记录设备历史延续关系。
6. 企业级 VPS / 盒子设备由管理员统一配置。
7. 本地客户端设备由员工安装，但必须受企业权限控制。

## 3.4 AI 专家团 / Agent 管理调整

1. `我的AI专家团` 要从展示页改成配置页。
2. Agent 权限至少支持：
   全公司可用
   指定员工可用
3. 后续预留：
   指定部门可用
4. Agent 展示重点改为“哪些员工有权限使用”，不要强调“正在协作”。
5. Agent 当前版本和最新版本要清晰展示。
6. 企业管理员需要具备：
   查看版本差异
   接受升级
   忽略升级
7. 企业端是否接受升级，应该由企业管理员决定。

## 3.5 模型配置调整

1. 模型配置权归企业管理员，不开放给普通员工。
2. 模型配置至少支持：
   全局模型配置
   单 Agent 模型配置
3. 员工端使用模型时应服从管理员配置，不允许自由切换模型。
4. 如果后续支持不同 Agent 使用不同模型，要在后台先预留配置结构。
5. 如果后续支持不同员工使用不同模型权限，也应作为后续扩展考虑。

## 3.6 组织管理调整

1. 组织管理先做手工维护版。
2. 暂不接飞书、企微、钉钉组织同步。
3. 组织管理至少支持：
   部门创建
   员工归属部门
   基础层级结构
4. 后续部门概念需要能服务于 Agent 权限控制。
5. 当前阶段先把“部门”做出来，再考虑外部同步。

## 3.7 用户与权限对象梳理

1. 后台中要明确区分以下对象：
   登录用户
   企业管理员
   普通员工
   设备
   Agent
   模型权限
2. 不能把“登录账号”和“业务权限对象”混在一起。
3. 企业管理员是主要配置角色，普通员工不承担后台配置责任。

---

## 4. 当前原型中的直接改动建议

## FDE 端

1. 删除当前 FDE 的 `商机工作台` 入口。
2. 删除当前 FDE 的 `线索工单` 入口，替换为多维表格说明。
3. 删除当前 FDE 的 `数据回流看板` 入口。
4. 删除当前 FDE 的 `进化任务管理` 入口。
5. 将 `运营监控` 改名为 `客户资产管理`。
6. 重构 `配置交付` 为成单后交付页。
7. 在 `客户资产管理` 中补 `设备` 和 `Agent` 双视图。
8. 在 `客户资产管理` 中补版本对比与升级推送。

## 企业管理后台

1. 驾驶舱文案和指标收口为老板视角。
2. `设备管理` 增加员工归属和设备分层。
3. `我的AI专家团` 增加权限配置、版本差异、升级处理。
4. `模型配置` 明确为管理员配置能力。
5. `组织管理` 先做部门与员工的手工维护能力。

---

## 5. 待确认事项

1. FDE 最终导航是保留 `配置交付 + 客户资产管理` 两个主 tab，还是再额外保留一个开发域入口 tab。
2. `客户资产管理` 是否作为正式命名，还是仍沿用 `运营监控`。
3. `推送版本管理` 是客户资产管理内子能力，还是独立二级页。
4. 企业管理员是否允许给不同 Agent 配不同模型。
5. 企业管理员是否允许控制不同员工的模型权限。
6. 组织架构未来是否需要和 SaaS 权限系统联动。

---

## 6. 建议实施顺序

1. 先收口 FDE 导航和职责边界。
2. 再重做 `配置交付` 与 `客户资产管理`。
3. 再完善企业管理后台中的 `设备管理`、`AI专家团`、`模型配置`、`组织管理`。
4. 最后统一驾驶舱指标口径和版本升级交互。
