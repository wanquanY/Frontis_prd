import { MockLoginView } from "@/feature/auth/components/MockLoginView";

/**
 * 原型系统模拟登录页面。
 */
export const LoginPage = (): JSX.Element => {
  return <MockLoginView />;
};

export default LoginPage;
