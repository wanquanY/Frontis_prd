# 大模型供应商缓存计量计费现状与公式

| 项目 | 内容 |
| --- | --- |
| 调研日期 | 2026-06-08 |
| 文档定位 | 客观整理各大模型供应商的缓存 usage 字段、缓存计费现状和合理费用计算公式 |
| 覆盖供应商 | OpenAI、Anthropic Claude、Google Gemini、Kimi / Moonshot、智谱 GLM / Z.AI、MiniMax、DeepSeek、阿里云百炼 / Qwen |

## 1. 先给结论

不同供应商的缓存计费不是同一种模型，不能统一套“总输入 tokens × 输入单价 + 输出 tokens × 输出单价”。合理做法是先按供应商返回的 usage 字段拆出不同 token 类型，再按该供应商公开的计费维度分别计算。

| 类型 | 典型供应商 | 计费特点 | 公式核心 |
| --- | --- | --- | --- |
| 自动缓存 + 缓存输入折扣 | OpenAI、Kimi、智谱、DeepSeek、MiniMax OpenAI 兼容口径 | usage 返回缓存命中 tokens，未命中部分按普通输入价，命中部分按缓存输入价 | 普通输入 = 总输入 - 缓存命中 |
| 显式缓存写入 / 读取分账 | Anthropic Claude、MiniMax Anthropic 兼容口径、Qwen 显式缓存 | usage 区分普通输入、缓存写入、缓存读取、输出 | 四类 tokens 分别乘以单价 |
| 缓存读取 + 缓存存储费 | Google Gemini | 请求中有 cached content tokens；显式缓存还按缓存 tokens × 保存时长收存储费 | 请求费用 + 缓存存储费用 |
| 命中 / 未命中直接拆分 | DeepSeek | usage 直接返回命中 tokens 和未命中 tokens | 命中输入 × 命中价 + 未命中输入 × 未命中价 + 输出 × 输出价 |

本文公式中使用的价格项均按中文表达：

| 价格项 | 含义 |
| --- | --- |
| 普通输入单价 | 未命中缓存的输入 tokens 单价，单位通常为每 100 万 tokens |
| 缓存命中输入单价 | 自动缓存命中后的输入 tokens 单价 |
| 缓存写入单价 | 显式创建或写入缓存时的 tokens 单价 |
| 5 分钟缓存写入单价 | 5 分钟 TTL 缓存写入 tokens 单价 |
| 1 小时缓存写入单价 | 1 小时 TTL 缓存写入 tokens 单价 |
| 缓存读取单价 | 从显式缓存读取 tokens 的单价 |
| 输出单价 | 模型输出 tokens 单价 |
| 缓存存储单价 | 缓存资源按保存时长计费的单价，单位通常为每 100 万 tokens 每小时 |

默认公式中的“除以 1,000,000”表示供应商按百万 tokens 定价。如果供应商按其他单位定价，需要先换算成同一单位。

---

## 2. OpenAI

### 2.1 供应商现状

OpenAI 的 Prompt Caching 是自动生效的。官方文档说明，满足缓存条件的请求会在 usage 中返回 `cached_tokens`，表示本次输入中有多少 tokens 命中了缓存；缓存不会影响输出 token 生成，也不需要额外支付缓存写入费。OpenAI 价格页按 `Input`、`Cached input`、`Output` 三类价格展示。

### 2.2 关键 usage 字段

| API | 字段 | 含义 |
| --- | --- | --- |
| Chat Completions | `usage.prompt_tokens` | 本次总输入 tokens，包含缓存命中部分 |
| Chat Completions | `usage.prompt_tokens_details.cached_tokens` | 缓存命中输入 tokens |
| Chat Completions | `usage.completion_tokens` | 输出 tokens |
| Responses | `usage.input_tokens` | 本次总输入 tokens，包含缓存命中部分 |
| Responses | `usage.input_tokens_details.cached_tokens` | 缓存命中输入 tokens |
| Responses | `usage.output_tokens` | 输出 tokens |

### 2.3 合理计价公式

```text
总输入 tokens = prompt_tokens 或 input_tokens
缓存命中输入 tokens = cached_tokens
普通输入 tokens = 总输入 tokens - 缓存命中输入 tokens
输出 tokens = completion_tokens 或 output_tokens

费用 =
  普通输入 tokens × 普通输入单价 ÷ 1,000,000
+ 缓存命中输入 tokens × 缓存命中输入单价 ÷ 1,000,000
+ 输出 tokens × 输出单价 ÷ 1,000,000
```

### 2.4 注意事项

- `prompt_tokens` / `input_tokens` 是总输入，不是普通输入；如果直接整体乘普通输入单价，会把 cached tokens 重复按高价计费。
- OpenAI 当前公开 usage 里没有独立“缓存写入 tokens”字段，公式中不应设计缓存写入费用。
- 对 OpenAI 来说，缓存命中成本应该用 `Cached input` 价格，而不是普通输入价格。

### 2.5 官方来源

- [OpenAI Prompt Caching](https://platform.openai.com/docs/guides/prompt-caching)
- [OpenAI API Pricing](https://openai.com/api/pricing/)

---

## 3. Anthropic Claude

### 3.1 供应商现状

Claude 的 Prompt Caching 明确区分缓存写入和缓存读取。官方文档说明，usage 中会返回普通输入 tokens、缓存创建 tokens、缓存读取 tokens 和输出 tokens。Claude 的缓存写入还区分 5 分钟和 1 小时 TTL，价格口径不同：5 分钟写入通常是基础输入价的 1.25 倍，1 小时写入通常是基础输入价的 2 倍，缓存读取通常是基础输入价的 0.1 倍。

### 3.2 关键 usage 字段

| 字段 | 含义 |
| --- | --- |
| `usage.input_tokens` | 普通输入 tokens，不包含缓存写入和缓存读取 |
| `usage.cache_creation_input_tokens` | 创建 / 写入缓存的输入 tokens |
| `usage.cache_read_input_tokens` | 从缓存读取的输入 tokens |
| `usage.output_tokens` | 输出 tokens |
| `usage.cache_creation.ephemeral_5m_input_tokens` | 5 分钟 TTL 缓存写入 tokens，若响应包含该明细则优先使用 |
| `usage.cache_creation.ephemeral_1h_input_tokens` | 1 小时 TTL 缓存写入 tokens，若响应包含该明细则优先使用 |

### 3.3 合理计价公式

如果只能拿到汇总写入字段：

```text
费用 =
  普通输入 tokens × 普通输入单价 ÷ 1,000,000
+ 缓存写入 tokens × 缓存写入单价 ÷ 1,000,000
+ 缓存读取 tokens × 缓存读取单价 ÷ 1,000,000
+ 输出 tokens × 输出单价 ÷ 1,000,000
```

如果能区分 5m / 1h 写入：

```text
费用 =
  普通输入 tokens × 普通输入单价 ÷ 1,000,000
+ 5 分钟缓存写入 tokens × 5 分钟缓存写入单价 ÷ 1,000,000
+ 1 小时缓存写入 tokens × 1 小时缓存写入单价 ÷ 1,000,000
+ 缓存读取 tokens × 缓存读取单价 ÷ 1,000,000
+ 输出 tokens × 输出单价 ÷ 1,000,000
```

### 3.4 注意事项

- `input_tokens` 已经是普通输入，不应再减去 `cache_creation_input_tokens` 或 `cache_read_input_tokens`。
- Claude 的“缓存写入”是明确计费项，不能像 OpenAI 那样把写入价格设为 0。
- 如果使用 1 小时缓存，不能仍按 5 分钟写入价格计算。

### 3.5 官方来源

- [Claude Prompt Caching](https://docs.anthropic.com/en/docs/build-with-claude/prompt-caching)
- [Claude Pricing](https://docs.anthropic.com/en/docs/about-claude/pricing)
- [Claude Messages API](https://platform.claude.com/docs/en/api/messages)

---

## 4. Google Gemini

### 4.1 供应商现状

Gemini 的缓存分为请求侧的 cached content 命中，以及显式缓存资源的存储成本。官方 GenerateContent API 的 `usageMetadata` 会返回总 prompt tokens、cached content tokens、输出候选 tokens 和 total tokens。官方 Context Caching 文档说明，显式缓存有 TTL，缓存存储按缓存 token 数和存储时长计费。

这意味着 Gemini 的完整费用通常不是单个请求公式，而是：

```text
总费用 = 模型调用费用 + 缓存存储费用
```

### 4.2 关键 usage 字段

| 字段 | 含义 |
| --- | --- |
| `usageMetadata.promptTokenCount` | 本次总 prompt tokens；设置 `cachedContent` 时仍包含缓存内容 tokens |
| `usageMetadata.cachedContentTokenCount` | 本次 prompt 中来自缓存内容的 tokens |
| `usageMetadata.candidatesTokenCount` | 输出候选 tokens |
| `usageMetadata.thoughtsTokenCount` | thinking 模型的思考 tokens，是否单独计费需按对应模型价格页确认 |
| `usageMetadata.totalTokenCount` | 本次请求总 tokens |
| CachedContent `usageMetadata.totalTokenCount` | 缓存资源本身包含的 tokens |
| CachedContent `ttl` / `expireTime` | 缓存保留时长或过期时间 |

### 4.3 合理计价公式

请求调用费用：

```text
总输入 tokens = promptTokenCount
缓存命中输入 tokens = cachedContentTokenCount
普通输入 tokens = 总输入 tokens - 缓存命中输入 tokens
输出 tokens = candidatesTokenCount

请求费用 =
  普通输入 tokens × 普通输入单价 ÷ 1,000,000
+ 缓存命中输入 tokens × 缓存命中输入单价 ÷ 1,000,000
+ 输出 tokens × 输出单价 ÷ 1,000,000
```

缓存存储费用：

```text
缓存资源 tokens = CachedContent.usageMetadata.totalTokenCount
缓存实际保留小时数 = 按 ttl 或 expireTime 计算出的小时数

缓存存储费用 =
  缓存资源 tokens × 缓存实际保留小时数 × 缓存存储单价 ÷ 1,000,000
```

总费用：

```text
总费用 = 请求费用 + 缓存存储费用
```

### 4.4 注意事项

- `promptTokenCount` 包含 cached content tokens，因此普通输入应为 `promptTokenCount - cachedContentTokenCount`。
- Gemini 显式缓存有存储成本；如果只算请求费用，会低估实际账单。
- 缓存存储费用不应强行摊到某一次请求里，它本质上是缓存资源在一段时间内产生的费用。

### 4.5 官方来源

- [Gemini Context Caching](https://ai.google.dev/gemini-api/docs/caching)
- [Gemini Generate Content API](https://ai.google.dev/api/generate-content)
- [Gemini API Pricing](https://ai.google.dev/gemini-api/docs/pricing)

---

## 5. Kimi / Moonshot

### 5.1 供应商现状

Kimi API 兼容 OpenAI Chat Completions 风格。官方 Chat API 示例返回 `prompt_tokens`、`completion_tokens`、`total_tokens` 和 `cached_tokens`。Kimi 价格文档按缓存命中输入、缓存未命中输入和输出 tokens 分别计价。

### 5.2 关键 usage 字段

| 字段 | 含义 |
| --- | --- |
| `usage.prompt_tokens` | 本次总输入 tokens |
| `usage.cached_tokens` | 缓存命中输入 tokens |
| `usage.completion_tokens` | 输出 tokens |
| `usage.total_tokens` | 总 tokens |
| `prompt_cache_key` | 请求参数，用于提升缓存命中率；不是计费字段 |

### 5.3 合理计价公式

```text
总输入 tokens = prompt_tokens
缓存命中输入 tokens = cached_tokens
普通输入 tokens = 总输入 tokens - 缓存命中输入 tokens
输出 tokens = completion_tokens

费用 =
  普通输入 tokens × 缓存未命中输入单价 ÷ 1,000,000
+ 缓存命中输入 tokens × 缓存命中输入单价 ÷ 1,000,000
+ 输出 tokens × 输出单价 ÷ 1,000,000
```

### 5.4 注意事项

- `cached_tokens` 是输入 tokens 的子集，不应额外加到 `prompt_tokens` 之外。
- `prompt_cache_key` 影响缓存命中率，但不能作为费用计算字段。
- 如果响应未返回 `cached_tokens`，保守计算应按全部 `prompt_tokens` 为缓存未命中输入。

### 5.5 官方来源

- [Kimi Chat Completion API](https://platform.kimi.ai/docs/api/chat)
- [Kimi API Pricing](https://www.kimi.com/help/kimi-api/api-pricing)
- [Kimi API OpenAI 兼容说明](https://platform.kimi.ai/docs/guide/migrating-from-openai-to-kimi)

---

## 6. 智谱 GLM / Z.AI

### 6.1 供应商现状

智谱上下文缓存是隐式缓存，官方中文文档说明会自动识别重复上下文，无需手动配置，并在响应中通过 `usage.prompt_tokens_details.cached_tokens` 透明展示命中缓存的 tokens。Z.AI Chat Completion 文档也展示了 `prompt_tokens`、`completion_tokens`、`prompt_tokens_details.cached_tokens` 和 `total_tokens` 字段。Z.AI 价格页存在 Input、Cached Input、Cached Input Storage、Output 等价格维度。

### 6.2 关键 usage 字段

| 字段 | 含义 |
| --- | --- |
| `usage.prompt_tokens` | 本次总输入 tokens |
| `usage.prompt_tokens_details.cached_tokens` | 缓存命中输入 tokens |
| `usage.completion_tokens` | 输出 tokens |
| `usage.total_tokens` | 总 tokens |

### 6.3 合理计价公式

请求费用：

```text
总输入 tokens = prompt_tokens
缓存命中输入 tokens = prompt_tokens_details.cached_tokens
普通输入 tokens = 总输入 tokens - 缓存命中输入 tokens
输出 tokens = completion_tokens

请求费用 =
  普通输入 tokens × 普通输入单价 ÷ 1,000,000
+ 缓存命中输入 tokens × 缓存命中输入单价 ÷ 1,000,000
+ 输出 tokens × 输出单价 ÷ 1,000,000
```

如果实际账单中存在 Cached Input Storage：

```text
缓存存储费用 =
  缓存资源 tokens × 缓存实际保留小时数 × 缓存存储单价 ÷ 1,000,000

总费用 = 请求费用 + 缓存存储费用
```

### 6.4 注意事项

- 智谱请求 usage 可以支持 cached input 拆分，但“缓存存储费用”是否实际发生，要按具体模型、价格页和账单口径确认。
- 如果只做单次请求成本估算，通常先按 Input / Cached Input / Output 三项计算。
- 如果供应商账单出现 Cached Input Storage 项，需要单独补算，不能隐藏在普通输入里。

### 6.5 官方来源

- [智谱上下文缓存](https://docs.bigmodel.cn/cn/guide/capabilities/cache)
- [Z.AI Chat Completion](https://docs.z.ai/api-reference/llm/chat-completion)
- [Z.AI Pricing](https://docs.z.ai/guides/overview/pricing)

---

## 7. MiniMax

### 7.1 供应商现状

MiniMax 同时存在常规 ChatCompletion、OpenAI 兼容式自动缓存，以及 Anthropic 兼容式显式缓存。官方 Prompt Caching 文档说明，缓存命中 tokens、普通新输入 tokens 和输出 tokens 分别按不同价格计费；官方 Anthropic API Compatible Cache 文档说明，显式缓存会返回 `cache_creation_input_tokens`、`cache_read_input_tokens` 和 `input_tokens`，并给出 total input tokens 的组成。

### 7.2 关键 usage 字段

| 口径 | 字段 | 含义 |
| --- | --- | --- |
| 常规 ChatCompletion | `usage.prompt_tokens` | 输入 tokens |
| 常规 ChatCompletion | `usage.completion_tokens` | 输出 tokens |
| 常规 ChatCompletion | `usage.total_tokens` | 总 tokens |
| OpenAI 兼容缓存 | `usage.prompt_tokens_details.cached_tokens` | 缓存命中输入 tokens |
| Anthropic 兼容缓存 | `usage.input_tokens` | 普通输入 tokens |
| Anthropic 兼容缓存 | `usage.cache_creation_input_tokens` | 缓存写入 tokens |
| Anthropic 兼容缓存 | `usage.cache_read_input_tokens` | 缓存读取 tokens |
| Anthropic 兼容缓存 | `usage.output_tokens` | 输出 tokens |

### 7.3 合理计价公式

常规 ChatCompletion 无缓存拆分时：

```text
费用 =
  输入 tokens × 普通输入单价 ÷ 1,000,000
+ 输出 tokens × 输出单价 ÷ 1,000,000
```

OpenAI 兼容缓存口径：

```text
总输入 tokens = prompt_tokens
缓存命中输入 tokens = prompt_tokens_details.cached_tokens
普通输入 tokens = 总输入 tokens - 缓存命中输入 tokens
输出 tokens = completion_tokens

费用 =
  普通输入 tokens × 普通输入单价 ÷ 1,000,000
+ 缓存命中输入 tokens × 缓存命中输入单价 ÷ 1,000,000
+ 输出 tokens × 输出单价 ÷ 1,000,000
```

Anthropic 兼容缓存口径：

```text
费用 =
  普通输入 tokens × 普通输入单价 ÷ 1,000,000
+ 缓存写入 tokens × 缓存写入单价 ÷ 1,000,000
+ 缓存读取 tokens × 缓存读取单价 ÷ 1,000,000
+ 输出 tokens × 输出单价 ÷ 1,000,000
```

### 7.4 注意事项

- MiniMax 必须先判断当前调用口径，再选公式。
- Anthropic 兼容口径下，`total_input_tokens = input_tokens + cache_creation_input_tokens + cache_read_input_tokens`；不能把 `input_tokens` 当总输入。
- OpenAI 兼容口径下，cached tokens 是 prompt tokens 的子集；不能重复相加。

### 7.5 官方来源

- [MiniMax Prompt Caching](https://platform.minimax.io/docs/api-reference/text-prompt-caching)
- [MiniMax Anthropic API Compatible Cache](https://platform.minimax.io/docs/api-reference/anthropic-api-compatible-cache)
- [MiniMax ChatCompletion v2](https://platform.minimax.io/document/Chatcompletion_v2?key=68abd8bcaf3fef26183d5a33)
- [MiniMax Pricing](https://www.minimax.io/pricing)

---

## 8. DeepSeek

### 8.1 供应商现状

DeepSeek 的上下文缓存默认开启。官方 KV Cache 文档说明，usage 中直接返回 `prompt_cache_hit_tokens` 和 `prompt_cache_miss_tokens`，分别表示本次输入中命中缓存和未命中缓存的 tokens。官方价格页按缓存命中输入、缓存未命中输入和输出 tokens 分别计价。

### 8.2 关键 usage 字段

| 字段 | 含义 |
| --- | --- |
| `usage.prompt_cache_hit_tokens` | 缓存命中输入 tokens |
| `usage.prompt_cache_miss_tokens` | 缓存未命中输入 tokens |
| `usage.prompt_tokens` | 总输入 tokens，通常应等于 hit + miss |
| `usage.completion_tokens` | 输出 tokens |
| `usage.total_tokens` | 总 tokens |

### 8.3 合理计价公式

```text
缓存命中输入 tokens = prompt_cache_hit_tokens
缓存未命中输入 tokens = prompt_cache_miss_tokens
输出 tokens = completion_tokens

费用 =
  缓存命中输入 tokens × 缓存命中输入单价 ÷ 1,000,000
+ 缓存未命中输入 tokens × 缓存未命中输入单价 ÷ 1,000,000
+ 输出 tokens × 输出单价 ÷ 1,000,000
```

### 8.4 注意事项

- DeepSeek 已经直接返回 hit / miss，不需要再用总输入减缓存命中。
- 对账时可以校验：`prompt_tokens = prompt_cache_hit_tokens + prompt_cache_miss_tokens`。
- DeepSeek 的输出 tokens 可能包含推理模型的思考链相关输出，是否单独展示要按具体模型文档和账单口径确认。

### 8.5 官方来源

- [DeepSeek Context Caching](https://api-docs.deepseek.com/guides/kv_cache)
- [DeepSeek Pricing](https://api-docs.deepseek.com/quick_start/pricing-details-usd/)

---

## 9. 阿里云百炼 / Qwen

### 9.1 供应商现状

Qwen / 百炼的 Context Cache 有隐式缓存、显式缓存、会话缓存等形态。官方文档说明，隐式缓存会在命中后通过 `usage.prompt_tokens_details.cached_tokens` 返回命中 tokens；显式缓存和会话缓存会涉及缓存创建价与缓存读取价，常见口径是缓存创建按输入价的一定倍率计费，缓存读取按输入价的折扣计费。

### 9.2 关键 usage 字段

| 字段 | 含义 |
| --- | --- |
| `usage.prompt_tokens` | 总输入 tokens |
| `usage.input_tokens` | 部分接口中使用的总输入 tokens 字段 |
| `usage.prompt_tokens_details.cached_tokens` | 缓存命中输入 tokens |
| `usage.completion_tokens` | 输出 tokens |
| `usage.output_tokens` | 部分接口中使用的输出 tokens 字段 |
| `usage.total_tokens` | 总 tokens |
| 显式缓存创建 tokens | 文档示例中通过缓存创建结果或请求过程计算，不一定和 OpenAI 兼容 usage 同名 |

### 9.3 合理计价公式

隐式缓存：

```text
总输入 tokens = prompt_tokens 或 input_tokens
缓存命中输入 tokens = prompt_tokens_details.cached_tokens
普通输入 tokens = 总输入 tokens - 缓存命中输入 tokens
输出 tokens = completion_tokens 或 output_tokens

费用 =
  普通输入 tokens × 普通输入单价 ÷ 1,000,000
+ 缓存命中输入 tokens × 缓存命中输入单价 ÷ 1,000,000
+ 输出 tokens × 输出单价 ÷ 1,000,000
```

显式缓存 / 会话缓存：

```text
费用 =
  普通输入 tokens × 普通输入单价 ÷ 1,000,000
+ 缓存创建 tokens × 缓存写入单价 ÷ 1,000,000
+ 缓存命中 tokens × 缓存读取单价 ÷ 1,000,000
+ 输出 tokens × 输出单价 ÷ 1,000,000
```

### 9.4 注意事项

- Qwen / 百炼不能只按一个公式处理，必须先确认当前使用的是隐式缓存、显式缓存还是会话缓存。
- 隐式缓存下，`cached_tokens` 是总输入的一部分。
- 显式缓存下，新创建缓存的 tokens 与命中缓存的 tokens 可能分别计价，不能只看总输入。

### 9.5 官方来源

- [Qwen Cloud Context Cache](https://docs.qwencloud.com/developer-guides/text-generation/context-cache)
- [Qwen Text Generation FAQ](https://docs.qwencloud.com/resources/faq-text-generation)
- [阿里云百炼 Context Cache](https://www.alibabacloud.com/help/zh/doc-detail/2862577.html)

---

## 10. 横向对比表

| 供应商 | 总输入字段 | 缓存命中字段 | 缓存写入字段 | 输出字段 | 是否有存储费 | 推荐公式类型 |
| --- | --- | --- | --- | --- | --- | --- |
| OpenAI | `prompt_tokens` / `input_tokens` | `cached_tokens` | 无 | `completion_tokens` / `output_tokens` | 无独立写入费；扩展缓存保留不另设写入价 | 总输入拆成普通输入 + 缓存命中输入 |
| Claude | 不用总输入字段；`input_tokens` 是普通输入 | `cache_read_input_tokens` | `cache_creation_input_tokens`，可细分 5m / 1h | `output_tokens` | 无单独存储费，TTL 体现在写入价 | 普通输入 + 缓存写入 + 缓存读取 + 输出 |
| Gemini | `promptTokenCount` | `cachedContentTokenCount` | 缓存创建资源有 token 数，但请求 usage 不按写入返回 | `candidatesTokenCount` | 有，显式缓存按 token-hour | 请求费用 + 缓存存储费用 |
| Kimi | `prompt_tokens` | `cached_tokens` | 无 | `completion_tokens` | 未见独立存储费 | 缓存未命中输入 + 缓存命中输入 + 输出 |
| 智谱 / Z.AI | `prompt_tokens` | `prompt_tokens_details.cached_tokens` | 未见请求级写入字段 | `completion_tokens` | 价格页存在 Cached Input Storage 项 | 请求费用；如账单有存储费则另算 |
| MiniMax | 取决于接口口径 | `cached_tokens` 或 `cache_read_input_tokens` | Anthropic 兼容口径有 `cache_creation_input_tokens` | `completion_tokens` / `output_tokens` | 以具体模型价格页为准 | 先按调用口径选公式 |
| DeepSeek | `prompt_tokens` | `prompt_cache_hit_tokens` | 无 | `completion_tokens` | 未见独立存储费 | 缓存命中输入 + 缓存未命中输入 + 输出 |
| Qwen / 百炼 | `prompt_tokens` / `input_tokens` | `prompt_tokens_details.cached_tokens` | 显式缓存 / 会话缓存有创建 tokens 口径 | `completion_tokens` / `output_tokens` | 以具体缓存形态为准 | 隐式缓存按缓存命中拆分；显式缓存加写入项 |
